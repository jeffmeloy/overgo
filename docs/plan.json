{
  "campaign": "Indexed graph runtime and typed model authority",
  "doctrine": "One compiled graph contract owns node order, operand slots, input bindings, output bindings, and backend execution. Host and device paths consume ordered typed bindings. Shared schema traversal replaces backend-specific traversal. Runtime metadata uses typed destinations, never reflective field strings. Artifact loading materializes only inventory selected by the compiled model contract. Each wave migrates every caller and deletes displaced maps, wrappers, reflection, fallbacks, and subset copies in the same commit.",
  "census_evidence": "evidence:sha256:a920ea814f10718d02e7fbcb1b9a7c3625ddcbb4e1a4aa073a695323b3603f01",
  "items": [
    {
      "id": "graph-runtime-ownership",
      "title": "Consolidate inference and projector graph execution ownership",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Move shared build, bind, compile, and execute lifecycle into graphruntime and delete displaced runtime wrappers",
          "status": "open",
          "verify": "go test ./internal/graphruntime ./internal/inference ./internal/projector -count=1",
          "depends_on": [
            "ordered-host-feeds"
          ],
          "capabilities": [
            "runtime-ownership",
            "wrapper-removal",
            "shared-lifecycle"
          ]
        }
      ]
    },
    {
      "id": "typed-metadata-destinations",
      "title": "Replace reflective Spec destinations with typed bindings",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Compile metadata operations with typed Spec destinations and delete field-name reflection",
          "status": "open",
          "verify": "go test ./internal/model -count=1",
          "depends_on": [
            "graph-runtime-ownership"
          ],
          "capabilities": [
            "typed-metadata",
            "reflection-removal",
            "compile-time-authority"
          ]
        }
      ]
    },
    {
      "id": "selective-dense-loading",
      "title": "Load only compiled dense decoder inventory",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Select decoder tensors before materialization, validate through typed artifact policy, and delete subset maps and architecture switches",
          "status": "open",
          "verify": "go test ./internal/safetensors ./internal/densecausal ./internal/composition ./internal/trainingworkflow -count=1",
          "depends_on": [
            "typed-metadata-destinations"
          ],
          "capabilities": [
            "selective-loading",
            "peak-memory-reduction",
            "typed-artifact-policy"
          ]
        }
      ]
    }
  ]
}
