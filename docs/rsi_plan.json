{
  "campaign": "Recursive self-improvement through constrained, content-addressed artifact improvement",
  "revision": 7,
  "doctrine": "Design record, not live executable work. docs/plan.json remains the only dispatch surface; cmd/plan does not read this file. A thin roadmap command over a common Go owner may validate this record and emit a PROPOSED live-plan insertion, but never mutate or dispatch it. The governing principle: the model may propose increasingly powerful ARTIFACTS, but deterministic Go owns execution, resource limits, evidence capture, admission, promotion and rollback. RSI here is constrained artifact improvement, never unrestricted code self-modification, and proposal / evaluation / authorization / rollback stay in separate authority domains.",
  "thesis": "Overgo is a content-addressed population of artifacts with a promotion mechanism. Automated composition needs three missing operators: bridge, compose, and a runtime experiment control plane to exercise them. RSI depth is a separate axis -- how many layers of the stack are themselves artifacts under the same lifecycle. Phase D lifts that ceiling and is safe only because ground truth is externally sourced and unauthorable by the system.",
  "control_plane_status": {
    "repository_development": {
      "state": "largely built",
      "has": [
        "content-addressed artifacts and lineage",
        "typed training programs and policy identities",
        "structured run, evaluation, decision and review evidence",
        "independent developer/SQA admission",
        "vacuous-verifier detection",
        "human-approved worktree/resource leases",
        "findings, plan advancement, containment, gate evidence",
        "changed-file gofmt and repository-wide go vet"
      ],
      "gap": "Google-aligned Go readability policy is not yet a typed candidate-delta gate. Phase 0 establishes the reviewed rule catalog and enforcement before the remaining RSI Go implementation rows become ready."
    },
    "runtime_experiment": {
      "state": "missing",
      "needs": "a plane that repeatedly creates, admits, leases, executes, evaluates, refuses, promotes, recovers and rolls back model descendants through idempotent state transitions. Distinct from the development control plane and not derivable from it. Phase 0 builds the minimum lifecycle; Phase E supplies enforceable authority and resource controls."
    }
  },
  "go_implementation_quality": {
    "purpose": "Make Google-aligned Go readability a repository implementation precondition while preserving Overgo's least-mechanism, common-owner and deletion-led doctrine. This governs Go code written to build the RSI system; runtime model artifacts remain declarative and cannot emit Go.",
    "sources": {
      "reviewed_on": "2026-08-15",
      "overview": "https://google.github.io/styleguide/go/",
      "canonical_guide": "https://google.github.io/styleguide/go/guide",
      "style_decisions": "https://google.github.io/styleguide/go/decisions",
      "best_practices": "https://google.github.io/styleguide/go/best-practices",
      "freshness": "The gate never browses. A periodic advisory reports upstream source changes for owner review; local rule semantics change only through a gated code revision with source section, rationale and regression test."
    },
    "principles_in_priority_order": ["clarity", "simplicity", "concision", "maintainability", "consistency", "least mechanism"],
    "implementation": {
      "owner": "internal/gostyle consuming the existing internal/repoanalysis parse-once snapshot; cmd/gate is the enforcement adapter",
      "scope": "Compare candidate diagnostics with HEAD at changed declaration identity. Existing untouched debt is reported but never blocks unrelated work. Generated sources are identified structurally and checked only for formatting and generator-owned invariants.",
      "output": "Structured diagnostics carry rule id, enforcement tier, Google source section, file, line, symbol, candidate/base state, rationale and whether a deterministic fix exists.",
      "dependency_policy": "Use the Go toolchain and existing common owners first. Do not add a kitchen-sink linter or parallel parser merely to obtain a rule that can be expressed through go/ast, go/types, go list, gofmt or go vet."
    },
    "automation_contract": {
      "rule_lifecycle": {
        "states": ["declared", "observed", "authoritative", "enforceable"],
        "transition_authority": "A rule or independently testable subcheck advances only through a gated code revision carrying source-section identity, positive and negative regression cases, selected/skipped evidence and candidate-history dispositions. Merely having a syntax detector means observed, never enforceable.",
        "gate_rule": "Only enforceable hard subchecks may fail a candidate. Observed and authoritative-but-not-promoted subchecks emit rule-local advisories; judgment-heavy rules never promote without a deterministic predicate replacing the judgment.",
        "history": "RepoDB records useful, false-positive, exempted and unresolved dispositions by rule and detector revision. Promotion is individual, reversible and cannot be inferred from an aggregate style score."
      },
      "source_freshness": {
        "offline_authority": "A reviewed local manifest maps every rule to document, section identity, rationale and detector revision. The commit gate validates this manifest and never browses.",
        "network_advisory": "A periodic non-blocking check reports upstream document or section changes for owner review. Upstream change cannot silently alter local semantics, and a missing or stale section identity is failable before rule promotion."
      },
      "build_context": {
        "owner": "internal/repoanalysis derives build membership once from go list, go:build, legacy +build and filename constraints; analyzers do not independently reimplement build selection.",
        "rule": "Cross-file diagnostics compare only files that can coexist in a declared target context. Generated and excluded files retain explicit selected/skipped reasons. Host coverage is mandatory; other changed platform cones are selected from derived context facts."
      },
      "analysis_efficiency": {
        "reuse": "Candidate and HEAD share content-addressed per-file facts keyed by repository path, content identity, detector revision and build context. Unchanged files are never reparsed or rescanned; only cross-file aggregates are recomputed.",
        "walks": "Each analyzer performs one deliberate syntax/type fact pass per changed file. Common parsing, position, identity and build facts belong to repoanalysis; rule-specific facts remain private to gostyle until another real consumer exists.",
        "report_shape": "Rule metadata is stored once. Diagnostics carry only rule-local location, symbol, state and message; base/candidate snapshot evidence is nested rather than represented by parallel fields.",
        "telemetry": "Gate evidence records files reused and rescanned, syntax and type passes, cache hits, diagnostic and encoded byte counts, analysis wall time, peak memory when available, exports added and production consumers. These are rule-local accountability signals, never a composite score or fixed ratchet."
      },
      "surface_policy": "Existing codeprofile clone, function and export facts remain the structural owner. A new export without a production consumer is an advisory requiring ownership rationale; no fixed surface ceiling may substitute for deletion-led review."
    },
    "hard_candidate_delta_rules": [
      "gofmt for changed and generated Go; go vet ./... for any Go ownership change",
      "package names lowercase and concise with only documented test/generated exceptions; no dot imports; blank imports only in main, tests or compiler-defined embed cases",
      "MixedCaps for newly declared identifiers and constants, with a reviewed initialism vocabulary and cgo/generated exceptions",
      "receiver abbreviations consistent for every method on a type and omitted when unused",
      "context.Context first when passed; no context.Context struct fields; no custom context types in signatures; tests use testing.TB.Context for their root context",
      "error is the final failure return; exported APIs return error rather than a concrete error pointer; static error strings are lowercase and omit terminal punctuation",
      "a discarded type-known error requires an adjacent comment; presence is the hard rule because it is mechanically decidable, while whether the comment justifies the discard is a reviewer judgment tracked as an advisory",
      "importable library packages do not register command-line flags as an import side effect",
      "go mod tidy -diff is clean whenever module ownership changes"
    ],
    "advisory_rules": [
      "new interfaces without a demonstrated consumer need, producer-owned interfaces, or functions returning interfaces where a concrete type is available",
      "new mutable package-level state or exported variables controlling library behavior",
      "context.Background inside a library call chain, especially when a caller context is already available",
      "panic or Must use outside initialization and invariant-only paths",
      "Get-prefixed APIs where the operation is not HTTP GET and Fetch or Compute would reveal cost or blocking",
      "naked returns in nontrivial functions and value receivers that copy large, aliasing or synchronization-bearing structs",
      "assertion helpers, helpers that fail without testing.TB.Helper, reflect.DeepEqual in new tests, and failures that hide got/want context",
      "goroutines without mechanically visible cancellation, join or documented process-lifetime ownership",
      "large functions, branch growth, exact clones, exported API growth and new abstractions whose measured consumer count does not justify them",
      "whether an error-discard comment actually justifies the discard, as opposed to merely existing; adequacy is a reviewer judgment and cannot fail a candidate under promotion_policy's deterministic low-noise bar"
    ],
    "non_goals": [
      "no line-length rule",
      "no composite readability or style score",
      "no fixed complexity, function-size or global-state ceiling",
      "no favorable metric allowed to offset a new violation in another rule",
      "no repository-wide rewrite to pay historical style debt",
      "no automatic refactoring in the commit gate beyond an explicitly invoked formatter",
      "no packages named common, util or helper merely to satisfy shared ownership; shared packages are named for the behavior they own"
    ],
    "promotion_policy": "Only deterministic low-noise rules fail candidate commits initially. Advisory rules graduate individually only after recorded candidate history demonstrates useful findings, bounded false positives and lower review/rework cost."
  },
  "operator_set": {
    "construct": {"state": "built", "owner": "internal/scratchmodel.Compile"},
    "train": {"state": "built", "owner": "internal/densecausal, internal/trainingprogram, internal/optimizer"},
    "evaluate": {"state": "built", "owner": "internal/runrecord, recipe verify"},
    "promote_refuse": {"state": "built", "owner": "cmd/recipe activate, internal/recipe decisions, RepoDB storage"},
    "retrieve": {"state": "partial", "owner": "internal/tensorstats.ShapeFeatures + Nearest", "gap": "pool is the loaded model only; internal/server/analyze_tensors.go sources from characterizeLoadedModel"},
    "decompose": {"state": "external", "owner": "adaptive_new go/extmodel/organ_schema.go", "gap": "not ported"},
    "bridge": {"state": "missing", "gap": "component pair -> trained adapter", "closed_by": "bridge-synthesis"},
    "compose": {"state": "missing", "gap": "components + adapter + profile + recipe + checkpoint -> one executable content-addressed model artifact", "closed_by": "compose-model-artifact"},
    "experiment": {"state": "missing", "gap": "create/admit/lease/execute/evaluate/refuse/promote/recover/rollback a descendant under budget with a replayable record", "closed_by": "experiment-lifecycle"}
  },
  "metric": {
    "headline": "descendant-generation-depth",
    "definition": "Generations of self-improvement before held-out gains stop. Generation N built by the generation N-1 system.",
    "child_beats_parent_requires": [
      "identical evaluator identity and data-split identity between parent and child measurement",
      "margin exceeding the observed noise envelope for that metric, not merely a positive delta",
      "multi-seed evidence wherever training is stochastic; envelope separation at n<=3 per skill.md Distribution Discipline",
      "no critical regression on any other declared metric",
      "resource cost reported (GPU-hours, peak memory) so gains are ranked per wall-hour",
      "holdout query budget not exhausted for the split used; selection and promotion splits distinct"
    ],
    "computed_from": "the experiment/generation graph, never maintained in prose",
    "reading": "Depth 1 is automation. Depth 3+ with sustained gains is genuinely recursive. A plateau at 2 indicates the operator set is too narrow, not that the thesis is wrong."
  },
  "graph_contract": {
    "authority": "depends_on is the sole stored ordering authority. blocks, unblocks and prose hard-order lists are forbidden because duplicate graph representations drift. Human order summaries are derived from depends_on and never parsed as policy.",
    "validation": "Reject duplicate or missing ids, cycles, unknown dependencies, self-dependencies, and any row whose dependency closure does not contain every precondition required by its declared capabilities.",
    "mandatory_preconditions": {
      "rule": "Preconditions attach to declared row CAPABILITIES, never to phase membership. Phase is not a safety property: rsi-phase-c contains typed-controller-action-language and independent-rsi-admission, which define or enforce boundaries rather than crossing them, and a phase-keyed rule would reject both incorrectly. The validator unions the required sets for a row's capabilities and asserts containment in its dependency closure. A row with no capabilities declares that it neither executes, promotes, admits nor measures.",
      "by_capability": {
        "executes-autonomously": ["bounded-exploration-budget", "runtime-resource-scheduler", "experiment-lifecycle"],
        "promotes-artifact": ["rollback-invariant-and-drill", "evaluation-isolation", "independent-rsi-admission"],
        "admits-without-human": ["sealed-external-authority"],
        "measures-held-out": ["evaluation-isolation", "determinism-and-statistical-verdicts"]
      },
      "capability_definitions": {
        "executes-autonomously": "runs GPU or long-lived work without per-run human authorization",
        "promotes-artifact": "activates a candidate as champion or active recipe",
        "admits-without-human": "admission decided by the system rather than an owner",
        "measures-held-out": "consumes an evaluation split to produce a verdict"
      }
    },
    "implementation_preconditions": {
      "rule": "Every non-landed roadmap row is Go implementation work unless it explicitly declares implementation_language other than go. go-readability-gate must be LANDED before any such row lands. This is a landing precondition asserted directly against roadmap state, never an edge in depends_on.",
      "requires": ["go-readability-gate"],
      "why_not_depends_on": "depends_on is the stage-derivation authority. A universal precondition expressed there inflates every downstream stage without encoding a real ordering constraint: revision 5 carried it as 11 edges, which lengthened the critical path from 7 stages to 9 and moved composition-viability from stage 2 to stage 4 -- the cheap-test-first inversion named in open_risks, in its third occurrence. The repository already models this correctly for the controls it has: changed-file gofmt and repository-wide go vet appear in control_plane_status as capabilities, and nothing depends on them. A commit gate applies to whatever is in front of it; it does not need to precede work in the dependency sense. Build-order preference for the two style rows is expressed in execution_order.priority_within_stage.",
      "bootstrap_exceptions": ["go-hygiene-policy-baseline", "go-readability-gate"],
      "landed_exemption": "Rows already listed in execution_order.landed predate this policy and are not retroactively rewritten. Candidate changes near historical debt are checked by candidate-versus-HEAD diagnostics."
    }
  },
  "roadmap_state_contract": {
    "authority": "Roadmap state is derived from Git, RepoDB, docs/plan.json, active leases and verifier evidence; stored status values are snapshots for review, never dispatch authority.",
    "states": {
      "new": "design-only row not present in the live plan",
      "ready": "dependencies satisfied and isolated probe evidence binds the declared gap",
      "open": "row already present in docs/plan.json",
      "in-flight": "an active owner-approved lease binds work to the row",
      "landed": "Git and RepoDB contain passing completion evidence at the committed target head"
    },
    "projection": "The roadmap command proposes only ready rows. It maps ready to live-plan status open, excludes open/in-flight/landed rows, preserves the declared id/title/verify, and never copies roadmap status verbatim into docs/plan.json."
  },
  "verifier_status_legend": {
    "declared": "verifier string specified and statically valid; it is a specification, not evidence that the named test exists",
    "probe-bound": "an isolated, immutable expected-failure run selected the named check, failed for the intended reason, and did not make the protected branch red",
    "implemented": "the named verifier exists and passes on a candidate head but is not yet landed",
    "landed": "the named verifier and implementation pass at a committed target head with gate evidence",
    "note": "A declared row may be proposed for owner review, but only probe-bound evidence makes it mechanically ready. Expected failures live in isolated probe evidence, never as a permanently failing protected-branch test."
  },
  "items": [
    {
      "id": "rsi-phase-0-experiment-plane",
      "title": "Phase 0 -- Go implementation hygiene and minimum runtime experiment control plane",
      "status": "open",
      "rationale": "First make the implementation loop produce readable, least-mechanism Go without a new style ratchet. Then build the minimum experiment envelope and lifecycle so no experiment result is unattributable or unreplayable.",
      "steps": [
        {
          "id": "go-hygiene-policy-baseline",
          "title": "Encode the reviewed Google Go rule catalog through existing Go analysis owners; classify deterministic hard rules versus judgment-heavy advisories; census the repository and bind candidate-versus-HEAD baseline evidence without rewriting historical debt",
          "status": "new",
          "verify": "go test ./internal/gostyle ./internal/repoanalysis -run '^TestGoStylePolicyAndBaseline$' -count=1 -v",
          "verifier_status": "declared",
          "priority": "bootstrap first",
          "rationale": "The policy must exist before enforcement so the gate cannot silently turn subjective guidance into blockers. Reuse repoanalysis, go/ast, go/types, go list, gofmt and go vet; add no broad lint framework or second source parser.",
          "acceptance": "The baseline reports every rule and exclusion, proves generated/build-constrained handling, records source sections and candidate/base identities, and demonstrates that no aggregate score or favorable metric can hide a rule regression."
        },
        {
          "id": "go-readability-gate",
          "title": "Harden the Go policy baseline and add an observe-first candidate-delta readability phase to cmd/gate: promote only authoritative low-noise subchecks, retain semantic guidance as structured advisories, and record rule-local coverage, cost and disposition evidence",
          "status": "new",
          "verify": "go test ./internal/gostyle ./cmd/gate -run '^TestGoReadabilityCandidateDelta$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["go-hygiene-policy-baseline"],
          "priority": "bootstrap second",
          "rationale": "This converts only readability review proven safe to automate into deterministic code. The implementation first separates detector availability from enforcement readiness, honors build contexts, validates local source-section authority, reuses unchanged facts and measures its own cost. Existing debt does not block unrelated commits; changed declarations cannot add new enforceable hard-rule debt; heuristic and semantic findings retain judgment and evidence rather than becoming a score.",
          "acceptance": "The named verifier covers aliased and shadowed imports, declaration identity, mutually exclusive build-tag and filename-constrained variants, generated sources, receiver object identity, MixedCaps declaration scope, valid source-section mappings and line-only movement. Gate evidence names every selected/skipped subcheck and maturity state; only enforceable subchecks fail. Candidate-versus-HEAD analysis reuses unchanged content facts, performs one rule-fact pass per changed file, stores rule metadata once and reports analysis-cost telemetry without a composite score. The first landed gate revision may leave every new detector observe-only; promotion occurs subcheck-by-subcheck from recorded evidence.",
          "bootstrap": "The owner manually inserts the two Go-hygiene rows into docs/plan.json. Once this row lands, every remaining non-landed Go row must reach it through depends_on and every Go commit runs the gate phase automatically."
        },
        {
          "id": "roadmap-validator",
          "title": "Validate the canonical depends_on DAG, runtime safety reachability, Go implementation preconditions, cross-references, derived stages, cycles and verifier syntax; derive roadmap state from Git/RepoDB/live-plan/lease evidence and emit a PROPOSED docs/plan.json insertion without becoming a second dispatcher",
          "status": "new",
          "verify": "go test ./cmd/roadmap -run '^TestRoadmapDAGAndReadiness$' -count=1 -v",
          "verifier_status": "declared",
          "rationale": "internal/plan/plan.go has no dependencies field; this record does. A thin cmd/roadmap adapter over a common Go roadmap owner computes readiness and proposes; cmd/plan and cmd/gate remain the sole execution authority. It maps ready to live-plan open, refuses live rows with unsatisfied mandatory dependencies unless an immutable owner decision narrows the authority claim, and never copies roadmap status values.",
          "constraint": "advisory only -- emits a proposal, never mutates docs/plan.json",
          "bootstrap": "This row cannot promote itself. After the Go-hygiene bootstrap lands, the owner manually authorizes its first insertion into docs/plan.json; every later promotion uses the validator."
        },
        {
          "id": "production-consumer-census",
          "title": "Derive a build-aware production-consumer census for changed declarations: distinguish production callers, test-only callers, mechanically recognized external boundaries and zero-consumer surface; report both commit-local and cumulative plan-slice deltas without enforcing a scalar size score",
          "status": "new",
          "verify": "go test ./internal/codeprofile ./internal/repoanalysis -run '^TestProductionConsumerCensus$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["go-readability-gate", "roadmap-validator"],
          "implementation_language": "go",
          "rationale": "Start with the easier and more reliable half of refactoring automation: determine when consolidation is required and hand the model a bounded symbol/caller set. Tests are evidence, not production consumers. Reuse the build-aware source snapshot and profile owners; add no second parser, hand-maintained path table or composite quality score.",
          "acceptance": "The census resolves declarations and references across active build contexts; identifies new exports with zero non-test callers, declarations reachable only from tests, and document readers or writers without an exercised production counterpart; mechanically classifies command, interface, generated, serialization, reflection, cgo and build-variant boundaries; and reports cumulative delta from the plan slice base rather than allowing a small cleanup commit to conceal a larger preceding expansion. This row is observe-only."
        },
        {
          "id": "unconsumed-surface-admission",
          "title": "Refuse candidate-introduced unconsumed production surface after the census corpus proves its boundary classifications; leave total AST, function, export and import deltas advisory and create no score, quota, exception ledger or historical-debt rewrite incentive",
          "status": "new",
          "verify": "go test ./cmd/gate ./internal/codeprofile -run '^TestGateRejectsNewUnconsumedProductionSurface$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["production-consumer-census"],
          "implementation_language": "go",
          "rationale": "This is the narrow ratchet that removes dependence on model discipline: a complete staged vertical slice may add an owner and its real consumer together, but cannot advance by calling tests a consumer or by promising that a later row will use a public API.",
          "acceptance": "Only candidate-introduced, mechanically proven unconsumed declarations fail. Existing debt remains visible but does not block unrelated work. Once this row lands, add it to graph_contract.implementation_preconditions.requires so later Go roadmap rows cannot land without the consumer invariant."
        },
        {
          "id": "safe-privatization-reducer",
          "title": "Extend cmd/tighten only with mechanically safe consumer-driven reductions: privatize declarations whose callers are wholly package-local, delete zero-reference declarations and empty files, remove displaced imports, and retain a transaction only when formatting, package tests and AST surface improve",
          "status": "new",
          "verify": "go test ./cmd/tighten -run '^TestConsumerReducerRetainsOnlyVerifiedReduction$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["unconsumed-surface-admission"],
          "implementation_language": "go",
          "rationale": "Automate transformations only where symbol identity and caller closure make semantics mechanically provable. Numerical equivalence, ownership redesign, record consolidation, memory lifetime and execution order remain model-reviewed refactors whose targets and acceptance are supplied by the census and gate.",
          "acceptance": "The reducer updates all mechanically resolved callers, refuses reflection/interface/build-boundary ambiguity, rolls back on any compile or test failure, and keeps the edit only when the targeted unconsumed-surface condition is eliminated and production AST does not increase."
        },
        {
          "id": "experiment-generation-record",
          "title": "Typed experiment/generation aggregate referencing the immutable identities for parents, components, bridge, training plan, data split, evaluator, code/kernel/environment, seeds, budget, run, outcome and decision",
          "status": "new",
          "verify": "go test ./internal/runrecord -run '^TestGenerationRecordBindsFullProvenance$' -count=1 -v",
          "verifier_status": "declared",
          "rationale": "Descendant depth is computed from this graph. The aggregate stores references rather than copies: RepoDB remains generic storage while runrecord owns generation semantics and existing recipe, dataset, evaluator, environment and decision artifacts retain their facts.",
          "caution": "Keep the day-one schema minimal -- exactly the identities needed to replay and attribute. Extend from what the first experiments demand. Building the full envelope before the first experiment inverts cheap-test-first."
        },
        {
          "id": "refusal-ledger",
          "title": "Record every refused candidate as an immutable measured refusal decision; expose the refusal ledger as a RepoDB query rather than a second mutable document",
          "status": "new",
          "verify": "go test ./internal/runrecord ./internal/recipe -run '^TestRefusalDecisionCarriesMeasuredReason$' -count=1 -v",
          "verifier_status": "declared",
          "rationale": "Audit trail, negative training signal for proposal-ranking-from-outcomes, and the mechanism preventing the system re-proposing the same dead end indefinitely. Reuse the existing decision lifecycle and keep RepoDB domain-neutral."
        },
        {
          "id": "determinism-and-statistical-verdicts",
          "title": "Repeat deterministic claims and require agreement; measure CUDA numerical spread and derive tolerance margins from observation; require multi-seed comparison for stochastic training outcomes; classify every claim as bitwise-deterministic, tolerance-bounded, or stochastic-multi-seed",
          "status": "new",
          "verify": "go test ./cmd/gate ./internal/testevidence -run '^TestVerdictClassificationAndRepeatAgreement$' -count=1 -v",
          "verifier_status": "declared",
          "priority": "first (parallel with composition-viability)",
          "rationale": "kernels/cuda/ops_f32.cu uses 12 float atomicAdd calls, all in gradient accumulation paths (grad_lag_bias, grad_scale, dscale, d_query, d_value, d_beta, d_key, d_gate), with comments at lines 504 and 1835 confirming scatter-accumulate. Backward results are not bitwise reproducible. 'Two runs agree' is insufficient for noisy training outcomes -- that tests reproducibility, not reliability. Implement as a gate step, never as doctrine: the agent need not know it exists and the message arrives with context at the moment it binds."
        },
        {
          "id": "experiment-lifecycle",
          "title": "Implement the idempotent runtime experiment state machine: proposed, admitted, leased, running, evaluated, refused/promoted, contained, rolled-back; bind retry identity, heartbeat expiry, checkpoint recovery and reconciliation to immutable generation evidence",
          "status": "new",
          "verify": "go test ./internal/runrecord -run '^TestExperimentLifecycleIsIdempotentAndRecoverable$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["experiment-generation-record", "refusal-ledger"],
          "rationale": "A generation record describes an experiment but does not operate it. This lifecycle is the runtime analogue of the prepared/finalized gate transaction: interruption cannot create two outcomes, silently lose a run, or promote without terminal evidence. Resource leases plug into the leased/running transitions when Phase E supplies them."
        }
      ]
    },
    {
      "id": "rsi-phase-a-operators",
      "title": "Phase A -- complete the operator set",
      "status": "open",
      "steps": [
        {
          "id": "multimodal-objectives",
          "title": "Compile the training modality matrix from RepoDB objectives; run real corpora through processor/projector/codec gradients and modality-native evaluation; record all other pairs refused",
          "status": "landed",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/adaptiveparity -run '^TestMultimodalTrainingMatrix$' -count=1 -v",
          "verifier_status": "landed",
          "origin": "docs/plan.json"
        },
        {
          "id": "objective-breadth",
          "title": "Port adaptive FNS, latent, forecast, OCR, flow-matching, image-latent, and distillation objectives only through shared TrainingPrograms; delete family loops as displaced",
          "status": "landed",
          "verify": "go test ./internal/adaptiveparity -run '^TestTrainingObjectiveMatrix$' -count=1 -v",
          "verifier_status": "landed",
          "origin": "docs/plan.json",
          "rationale": "Prerequisite for composition-viability: a bridge adapter needs a shared objective to train against. Landed at 7de448b1 'Compile adaptive objectives through shared programs'; status is a review snapshot, and the validator must re-derive it from Git/RepoDB rather than trusting this field."
        },
        {
          "id": "component-decomposition",
          "title": "Derive typed components from tensor name, shape, dtype, family and modality across the eight organ axes; persist as artifacts with lineage to the source model; classification only, no retrieval index",
          "status": "new",
          "verify": "go test ./internal/... -run '^TestComponentDecompositionContract$' -count=1 -v",
          "verifier_status": "declared",
          "port_source": "adaptive_new go/extmodel/organ_schema.go classifyOrganTensor, classifyOrganSpace, classifyOrganModality, validateOrganContract, requireValidOrganContractForPromotion"
        },
        {
          "id": "composition-viability",
          "title": "Graft one classified component from a validated model into another; freeze both, train only the bridge tensor through the shared Muon plan; require held-out gain over the unmodified target beyond the observed noise envelope, or record refusal with measured reason and failure mode",
          "status": "new",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/adaptiveparity -run '^TestComponentCompositionViability$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["objective-breadth", "component-decomposition", "experiment-generation-record", "determinism-and-statistical-verdicts", "evaluation-isolation"],
          "capabilities": ["measures-held-out"],
          "priority": "first",
          "rationale": "THE PIVOT. Nothing upstream of recursive-improvement-boundary establishes that component composition produces working models, yet that step has the controller proposing composition candidates. Without this row a downstream failure cannot be attributed between a bad proposer and an unviable mechanism. Uses proven infrastructure only: Tier-1 transplant needs no profile synthesis because the target ArchitectureProfile already exists in the 135-entry catalog, and densecausal.TrainDeviceResidentFrozenLexicalBatches already proves frozen-parameter resident CUDA training. Run manually, human-reviewed, under a recorded budget.",
          "decision_point": {
            "on_pass": "proceed to Phase B as written",
            "on_refusal": "the controller action space shrinks and Phases B-D reshape BEFORE being built. Re-scope to whole-model chaining (Tier 0) through internal/workflowrecipe typed ports, which needs no latent-space bridging, and re-rank the adapter tier against measured failure modes rather than deleting it."
          }
        },
        {
          "id": "construction-profile-bridge",
          "title": "Make scratchmodel.Compile emit model.ArchitectureProfile rather than a private Config, so constructed and inherited components are interchangeable in the vocabulary the executor already runs",
          "status": "new",
          "verify": "go test ./internal/scratchmodel ./internal/model -run '^TestConstructionEmitsExecutableProfile$' -count=1 -v",
          "verifier_status": "declared",
          "rationale": "The keystone join, currently unnamed and on the critical path. internal/scratchmodel does not import internal/model; ArchitectureProfile is never constructed at runtime. controller-promotion needs a controller both scratch-built and executable through the shared runtime."
        }
      ]
    },
    {
      "id": "rsi-phase-b-automatic-composition",
      "title": "Phase B -- make composition automatic",
      "status": "open",
      "steps": [
        {
          "id": "component-catalog",
          "title": "Populate the store component catalog across every validated model and repoint the similarity endpoint at the store pool so retrieval is cross-model and inspectable",
          "status": "new",
          "verify": "go test ./internal/server -run '^TestSimilarComponentsSpanStoreCatalog$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["component-decomposition"],
          "cost": "near-zero if cmd/model-characterize is folded into the in-progress validation pass",
          "rationale": "analyze_tensors.go:113 sources its pool from characterizeLoadedModel, so similarity is within-model only. The pool source changes, the algorithm does not."
        },
        {
          "id": "bridge-proposal",
          "title": "Convert retrieval output into blocked composition candidates carrying required verifier, promotion-blocked state and blocker reason; advisory by construction, never authorizing",
          "status": "new",
          "verify": "go test ./internal/... -run '^TestBridgeProposalBlocksWithoutVerifier$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["refusal-ledger", "component-catalog", "composition-viability"],
          "port_source": "adaptive_new go/extmodel/hdc.go CandidateRecipeBridges; verifiers organ_contract_parity / universal_optimizer_bridge_parity / paired_bridge_semantic"
        },
        {
          "id": "bridge-synthesis",
          "title": "Given a blocked candidate, derive adapter geometry, train it with everything else frozen, and emit promotion evidence or a refusal record with measured reason",
          "status": "new",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/adaptiveparity -run '^TestBridgeSynthesisProducesEvidenceOrRefusal$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["composition-viability", "bridge-proposal", "experiment-lifecycle", "bounded-exploration-budget", "runtime-resource-scheduler"],
          "capabilities": ["executes-autonomously"],
          "closes_operator": "bridge"
        },
        {
          "id": "compose-model-artifact",
          "title": "Assemble components, adapter, architecture profile, recipe and checkpoint into one executable content-addressed model artifact with full lineage to every input",
          "status": "new",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/adaptiveparity -run '^TestComposedArtifactExecutesAndResolvesLineage$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["bridge-synthesis", "construction-profile-bridge"],
          "closes_operator": "compose",
          "rationale": "Revision 1 listed compose as missing and never closed it. A trained adapter is not an assembled model; without this row nothing produces the descendant artifact that descendant-improvement measures."
        },
        {
          "id": "hdc-retrieval",
          "title": "Port the bit-packed hypervector index with catalog-derived signature width, combining the lexical organ signal with the existing distributional signal",
          "status": "new",
          "verify": "go test ./internal/... -run '^TestHypervectorRetrievalRanksKnownBridges$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["bridge-synthesis", "component-catalog"],
          "rationale": "Deliberately last in phase B. Do not automate selection before composition-viability establishes that selection matters and hand-run bridges reveal what ranking needs. The signals are complementary: lexical says what a tensor is called, distributional says how it behaves."
        }
      ]
    },
    {
      "id": "rsi-phase-c-capability-loop",
      "title": "Phase C -- close the capability loop",
      "status": "open",
      "steps": [
        {
          "id": "typed-controller-action-language",
          "title": "Define an allowlisted artifact-transformation action language the controller emits; deterministic Go executors compile those actions through recipes, neutral operators and shared training programs. No code, no shell, no arbitrary runtime orchestration",
          "status": "new",
          "verify": "go test ./internal/... -run '^TestControllerActionsAreAllowlistedTransformations$' -count=1 -v",
          "verifier_status": "declared",
          "rationale": "The automation boundary. A controller emitting code or shell is unrestricted self-modification wearing a recipe costume."
        },
        {
          "id": "controller-promotion",
          "title": "Construct and train the small workflow controller from scratch; immutable Git/RepoDB corpus rather than operational-state text; typed component and workflow actions; held-out action/modality validity and multi-seed gains; pretrained challengers under the same suite; champion/challenger promotion and rollback external to the model",
          "status": "open",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/adaptiveparity -run '^TestControllerPromotionContract$' -count=1 -v",
          "verifier_status": "declared",
          "origin": "docs/plan.json",
          "depends_on": ["construction-profile-bridge", "composition-viability", "typed-controller-action-language", "rollback-invariant-and-drill", "sealed-external-authority", "evaluation-isolation", "independent-rsi-admission"],
          "capabilities": ["promotes-artifact", "measures-held-out"],
          "live_plan_conflict": "This row is dispatched in docs/plan.json now, but independent-rsi-admission does not exist. Until it does, 'champion/challenger promotion and rollback external to the model' is a naming convention rather than an enforced authority boundary. Either land independent-rsi-admission first or record explicitly that this controller's promotion is owner-authorized and not machine-separated."
        },
        {
          "id": "independent-rsi-admission",
          "title": "Bind proposer, evaluator and decider to distinct authority domains rather than merely distinct artifact IDs; re-evaluate from a clean worker against sealed inputs; require generation N-1 authority to approve generation N evaluator or policy changes",
          "status": "new",
          "verify": "go test ./internal/runrecord ./internal/recipe -run '^TestProposerEvaluatorDeciderAuthorityDomainsAndPriorGeneration$' -count=1 -v",
          "verifier_status": "declared",
          "rationale": "Distinct artifact IDs do not prevent a single process from authoring all three. Authority separation and the no-self-approval generation edge must be enforced in immutable runtime evidence, not by naming or doctrine."
        },
        {
          "id": "bounded-controller-trials",
          "title": "Permit bounded controller-driven descendant trials: controller proposes corpus, recipe, evaluator and composition candidates; independent admission, held-out evaluation, promotion and rollback required; every child bound to parent model, data, recipe, code, evaluator, run and decision identities",
          "status": "open",
          "verify": "go test ./internal/runrecord ./internal/trainingprogram -run '^TestRecursiveImprovementLineageAndExternalPromotion$' -count=1 -v",
          "verifier_status": "declared",
          "origin": "docs/plan.json (recursive-improvement-boundary)",
          "depends_on": ["controller-promotion", "determinism-and-statistical-verdicts", "bounded-exploration-budget", "evaluation-isolation", "sealed-external-authority", "independent-rsi-admission", "experiment-lifecycle", "runtime-resource-scheduler"],
          "capabilities": ["executes-autonomously", "promotes-artifact", "admits-without-human", "measures-held-out"],
          "note": "Host-only verify: this validates the lineage and admission CONTRACT, not that improvement occurred. descendant-improvement tests the outcome."
        },
        {
          "id": "descendant-improvement",
          "title": "Require a child artifact to beat its parent under the full metric contract: identical evaluator and split identities, margin beyond observed noise, multi-seed where stochastic, no critical regression, resource cost reported, holdout query budget respected",
          "status": "new",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/adaptiveparity -run '^TestDescendantBeatsParent$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["bounded-controller-trials", "compose-model-artifact", "evaluation-isolation"],
          "capabilities": ["promotes-artifact", "measures-held-out"],
          "rationale": "First row whose passing means actual self-improvement occurred rather than that the contract holds. Generation depth 1."
        }
      ]
    },
    {
      "id": "rsi-phase-d-operators-as-artifacts",
      "title": "Phase D -- deepen RSI by promoting operators from code into declarative, schema-versioned artifacts consumed by trusted Go interpreters",
      "status": "open",
      "constraint": "Phase D artifacts are declarative INPUTS to trusted Go interpreters, never executable logic. A candidate must never promote a new evaluator or gate using that same new evaluator or gate: generation N-1 authority approves generation N.",
      "phase_capabilities": ["promotes-artifact", "admits-without-human"],
      "phase_capabilities_note": "Declared once here rather than repeated on five rows. Expansion rule: a row's effective capability set is the union of its own capabilities and its item's phase_capabilities. This is shorthand for per-row declaration, not a phase-keyed safety rule -- phase membership still confers nothing by itself.",
      "steps": [
        {
          "id": "derivation-profile-as-artifact",
          "title": "Make the corpus-to-topology derivation profile a content-addressed artifact with lineage and promotion record, so the system proposes derivation profiles and is judged on the models they produce",
          "status": "new",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/scratchmodel -run '^TestDerivationProfilePromotedByDescendantQuality$' -count=1 -v",
          "verifier_status": "declared",
          "improves": "how the system constructs models",
          "depends_on": ["descendant-improvement"],
          "rationale": "AdaptiveDerivationProfile() returns eight pinned scalars frozen to reproduce the adaptive oracle. Promotion to artifact closes eight magic rows by derivation rather than a better story -- the RSI loop satisfies Magic Discipline as a side effect."
        },
        {
          "id": "evaluator-as-artifact",
          "title": "Promote an evaluator only if it correctly ranks a held-out set of models with known-good and known-bad outcomes drawn from the sealed oracle set, approved by generation N-1 authority",
          "status": "new",
          "verify": "go test ./internal/... -run '^TestEvaluatorRanksKnownOutcomesUnderPriorAuthority$' -count=1 -v",
          "verifier_status": "declared",
          "improves": "how the system judges",
          "depends_on": ["sealed-external-authority", "evaluation-isolation", "descendant-improvement"],
          "rationale": "The classic RSI failure is a system improving its evaluator into one that approves everything. Safe only because the ranking set is externally sourced and unauthorable, and because the approving authority predates the candidate."
        },
        {
          "id": "objective-composition",
          "title": "Make objectives composable typed artifacts; let the system propose objective mixtures and measure which produce better descendants",
          "status": "new",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/trainingprogram -run '^TestComposedObjectiveProducesBetterDescendant$' -count=1 -v",
          "verifier_status": "declared",
          "improves": "what the system optimizes",
          "depends_on": ["objective-breadth", "descendant-improvement"]
        },
        {
          "id": "proposal-ranking-from-outcomes",
          "title": "Train the proposer ranking on the refusal ledger so retrieval becomes a learned prior over what composes successfully rather than a fixed similarity function",
          "status": "new",
          "verify": "go test ./internal/... -run '^TestProposalRankingImprovesOnRefusalHistory$' -count=1 -v",
          "verifier_status": "declared",
          "improves": "how the system proposes",
          "depends_on": ["hdc-retrieval", "refusal-ledger", "evaluation-isolation", "descendant-improvement"],
          "rationale": "Where refusal records stop being an audit trail and become the most valuable dataset in the store: passed and refused bridges are labelled data no external source can supply. Must respect proposer blinding -- the ranking trains on refusal reasons, never on promotion or audit split results."
        },
        {
          "id": "scheduler-as-artifact",
          "title": "Make next-candidate selection under wall-clock and VRAM budget a learned artifact scored on realized capability delta per wall-hour rather than an applied formula",
          "status": "new",
          "verify": "go test ./internal/plan -run '^TestSchedulerRankedByRealizedDelta$' -count=1 -v",
          "verifier_status": "declared",
          "improves": "how the system allocates its own effort",
          "depends_on": ["runtime-resource-scheduler", "descendant-improvement"]
        }
      ]
    },
    {
      "id": "rsi-phase-e-enforced-boundaries",
      "title": "Phase E -- enforced boundaries; preconditions, not follow-on work",
      "status": "open",
      "steps": [
        {
          "id": "sealed-external-authority",
          "title": "Place goldens, evaluator definitions, promotion policy and champion aliases beyond candidate-worker reach through a distinct OS/service identity or remote protected authority; require external provenance naming the producing tool and version for any golden change",
          "status": "new",
          "verify": "OVERGO_SEALED_AUTHORITY_TEST=1 go test ./internal/testevidence ./internal/protection -run '^TestCandidatePrincipalCannotWriteSealedAuthority$' -count=1 -v",
          "verifier_status": "declared",
          "rationale": "THE FIXED POINT. Goldens originate from HF transformers 4.57.1 / torch 2.12.0+cu126 and pinned llama.cpp; fixtures/carbon_golden.json stamps its producing versions and llama_*_grad_golden.json carry torch autograd gradients. The system cannot author them. Every Phase D row is safe exactly to the degree it is ultimately scored against artifacts the system did not produce.",
          "single_machine_caveat": "Same-user ACLs, an external mirror and a bypassable gate are tamper-evidence, not authority separation. Until a distinct principal or remote protected service passes the integration verifier, promotion remains human-authorized. Capability absence such as 'the loop has no current torch path' is not an enforcement claim."
        },
        {
          "id": "evaluation-isolation",
          "title": "Separate development, selection, promotion and audit datasets; record per-split query budgets; prevent the proposer from observing promotion or audit results",
          "status": "new",
          "verify": "go test ./internal/trainingdata ./internal/runrecord -run '^TestSplitIsolationAndQueryBudget$' -count=1 -v",
          "verifier_status": "declared",
          "rationale": "An immutable held-out set is still vulnerable to adaptive overfitting through repeated querying -- immutability of the DATA does not bound information leakage from repeated QUERIES. Query budgets plus proposer blinding are the actual controls. Reuse the common typed budget accounting owner also consumed by exploration budgets rather than implementing split-local counters."
        },
        {
          "id": "bounded-exploration-budget",
          "title": "Issue GPU-hour budget tokens for autonomous proposal; enforce cancellation and containment; exceeding a budget requires an external decision",
          "status": "new",
          "verify": "go test ./internal/plan -run '^TestExplorationBudgetTokensEnforced$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["evaluation-isolation"],
          "rationale": "Bounded autonomy is the only kind that can be left running unattended. Query counts, GPU-hours and later resource dimensions use one common immutable budget/token mechanism with typed units, owner, subject, issued amount, consumed amount, expiry and external override evidence."
        },
        {
          "id": "rollback-invariant-and-drill",
          "title": "Make reversibility a checked invariant -- immutable champion retention, compare-and-set activation, decisions carrying decider identity -- and exercise a rollback drill that demonstrably restores the prior champion",
          "status": "new",
          "verify": "go test ./internal/repodb ./internal/recipe -run '^TestPromotionReversibleAndDrillRestoresChampion$' -count=1 -v",
          "verifier_status": "declared",
          "rationale": "An untested rollback is not a rollback. The drill is the row, not the invariant alone."
        },
        {
          "id": "runtime-resource-scheduler",
          "title": "Add experiment/job leases carrying GPU memory, CPU, RAM and wall-time estimates, heartbeat expiry, cancellation, checkpoint/resume, abandoned-job reconciliation, retry identity, recovery cost and actual measurements; recommendation-only at first",
          "status": "new",
          "verify": "go test ./internal/plan -run '^TestExperimentLeaseLifecycleAndMeasurement$' -count=1 -v",
          "verifier_status": "declared",
          "depends_on": ["experiment-generation-record", "experiment-lifecycle"],
          "rationale": "cmd/plan -lease-report covers worktree/resource advisory leases for development. Runtime experiments need their own lease projection, but both consumers should reuse common resource request and measured-outcome primitives now that the abstraction has two real owners. Experiment-specific lifecycle state stays in runrecord; plan owns recommendation and capacity assessment."
        }
      ]
    }
  ],
  "execution_order": {
    "derivation": "Stages are DERIVED from depends_on (stage = 1 + max stage of dependencies), then filtered to exclude rows listed in landed. Regenerated by the validator, never hand-authored; a mismatch between stored and recomputed stages is a validation failure. Stage numbers are dependency-depth labels, not global barriers: a row becomes ready when its own dependency closure is satisfied even if unrelated rows at lower stages remain open. Rows within a stage have no ordering constraint and may run in parallel.",
    "stage_1": ["component-decomposition", "construction-profile-bridge", "determinism-and-statistical-verdicts", "evaluation-isolation", "experiment-generation-record", "go-hygiene-policy-baseline", "independent-rsi-admission", "refusal-ledger", "roadmap-validator", "rollback-invariant-and-drill", "sealed-external-authority", "typed-controller-action-language"],
    "stage_2": ["bounded-exploration-budget", "component-catalog", "composition-viability", "experiment-lifecycle", "go-readability-gate"],
    "stage_3": ["bridge-proposal", "controller-promotion", "production-consumer-census", "runtime-resource-scheduler"],
    "stage_4": ["bounded-controller-trials", "bridge-synthesis", "unconsumed-surface-admission"],
    "stage_5": ["compose-model-artifact", "hdc-retrieval", "safe-privatization-reducer"],
    "stage_6": ["descendant-improvement"],
    "stage_7": ["derivation-profile-as-artifact", "evaluator-as-artifact", "objective-composition", "proposal-ranking-from-outcomes", "scheduler-as-artifact"],
    "priority_within_stage": "Stage 1: go-hygiene-policy-baseline first, so the readability gate exists before the bulk of new RSI Go lands and the landing precondition is satisfiable early. Stage 2: composition-viability first -- it is the cheapest high-information experiment in the record and its decision_point reshapes bridge, controller and Phase D work. Stage 3: production-consumer-census precedes further speculative foundations; its observe-only corpus earns the narrow admission rule, which then enables only mechanically safe reducer actions. The style and consumer rows gate LANDING, not numerical design, so they do not replace semantic review.",
    "bootstrap_exception": "The owner manually authorizes go-hygiene-policy-baseline and go-readability-gate in docs/plan.json. After they land, the owner authorizes roadmap-validator once; every later roadmap promotion is mechanical.",
    "landed": ["multimodal-objectives", "objective-breadth"]
  },
  "open_risks": [
    "The empirical bet is untested: no evidence in overgo or adaptive_new that organ-level composition yields working models. adaptive_new carries the machinery and verifiers but no observed promoted bridge.",
    "The derivation space may reproduce known topologies rather than finding new ones; derivation-profile-as-artifact would reveal this.",
    "Every property measured to date is structural. The controls have never bound under pressure -- the promotion path has never admitted something unwanted, the evidence system has never caught a costly regression, the auto-close loop has never closed a finding. The one observed control failure (a stale parity-report P0 read as current) failed silently toward false confidence.",
    "Velocity changes character past the port frontier: transcription-rate becomes discovery-rate, since neither inherited source has solved composition.",
    "Phase 0 risks inverting cheap-test-first if the envelope or lifecycle is over-built before the first experiment. Keep records referential and transitions minimal; extend from observed recovery and attribution demands.",
    "Sealed authority is weakly enforceable on a single-user host. Same-user layers provide detection only; autonomous promotion stays disabled until a distinct principal or remote protected authority is exercised.",
    "Roadmap state and verifier status will drift if manually edited. The validator must derive them and report mismatches rather than trusting this document's snapshots.",
    "Runtime lifecycle, lease and budget records can become a second governance surface. Reuse existing artifact codecs, decisions, run records and resource measurements; add no mutable ledger or duplicated fact.",
    "A broad style gate can recreate the failed ratchet dynamic by rewarding cosmetic rewrites or abstraction churn. Enforce only per-rule candidate regressions; retain semantic guidance as advisory and measure whether each rule reduces review or rework cost before promotion.",
    "Google's guidance evolves and contains contextual recommendations. The commit gate must use a reviewed local rule implementation, never live web content; upstream changes open an advisory for owner review rather than silently changing acceptance."
  ]
}
