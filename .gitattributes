# Byte-faithful transport: this repository's evidence model hashes worktree
# bytes (gate claims, artifact identities, fixture pins, golden checks), so
# git must never rewrite a byte between blob and worktree. No text
# conversion for anything, on any machine, regardless of local or system
# core.autocrlf/core.eol configuration.
* -text
