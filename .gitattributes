# All Go sources are LF in the working tree regardless of core.autocrlf, so a
# fresh checkout, merge, or worktree add never leaves CRLF for gofmt (or the
# generated-source byte-parity checks) to trip on. Subsumes the per-file
# generated-source pins.
*.go text eol=lf
