# Overgo runtime-authority tightening plan

Goal: recipes and compiled neutral operator programs are the only production runtime authorities. Each wave migrates every caller in its scope, deletes displaced orchestration in the same commit, verifies numerical behavior, then merges `master`.

## Gates

- Host: focused package tests, numerical graph parity tests, `go test -count=1 ./...`, `go vet ./...`.
- CUDA: affected Windows/CUDA executor tests plus resident/device parity lanes when hardware is available. Environment-gated tests must report skips, never silently substitute host execution.
- Change control: commit one complete vertical wave; merge `master` immediately afterward; resolve toward the newest neutral runtime.
- Paydown: production family-builder/direct-dispatch count and code surface must fall in every migration wave.

## Runtime-authority ledger

| Wave | Scope | Remaining production authority | Exit condition | Status |
|---|---|---|---|---|
| 1 | Draft/MTP layers | `compiledDraftBlock`; direct NextN and Step3.5 calls to `BuildArchitectureBlockCached` | `DraftLayerProgram` executes all draft blocks; adapter spec argument and direct calls deleted | Complete |
| 2 | Trunk host graphs | `buildLayerBlockFromPlan`; three direct runner dispatches | compiled trunk program owns construction; helper and direct dispatches deleted | Complete |
| 3 | Continuous/device cache | direct dispatch in `device_cache.go` | decode session invokes compiled program with indexed cache bindings | Complete |
| 4 | Auxiliary decoders | direct Eagle3 and Gemma 4 assistant dispatch | compiled auxiliary programs own topology and bindings | Complete |
| 5 | Encoder/decoder | exported T5 encoder/decoder builders | typed encoder/cross-attention programs own ordered stages; builders deleted | Complete |
| 6 | Diffusion/video | `BuildConditionedDiffusionBlock` | compiled diffusion program owns block/head stages; direct builder deleted | Complete |
| 7 | Compatibility dispatch | exported `BuildArchitectureBlockCached` and direct-construction tests | all callers execute sealed programs; compatibility entry point deleted | Complete |
| 8 | Leaf operators | family-named attention/recurrent/FFN leaves selected inside neutral instructions | exact mathematical operators replace mirror policies; redundant instruction fields deleted | Complete |
| 9 | Catalog authority | `BlockPolicy` family facts still influence weight/cache/state compilation | typed mathematical and tensor-catalog facts replace runtime family orchestration where semantics match | Complete |
| 10 | Recipe boundaries | remaining command/server task switches or runtime reconstruction | active compiled recipe selected once at assembly; no serving fallbacks/probes | Complete |
| 11 | Auxiliary graph islands | family-owned feature, cache, paired-input, terminal, and sequence-output graph methods | ordered neutral compiled programs own every auxiliary stage; family methods deleted | Complete |
| 12 | Residency authority | CLI preload/cache flags can change execution beneath one recipe identity | compiled residency/precision policy owns storage and fallback; runtime binds hardware only | Complete |
| 13 | Profile compatibility | serving accepts provenance-free v1 profiles | legacy data upgrades at import; active serving requires current provenance-bound profiles | Complete |
| 14 | Ownership/paydown | getter-plus-disown transfer and dead panic wrappers | one-shot immutable serving payload; unused/sharp compatibility APIs deleted | Complete |
| 15 | Capability assembly | VQA remains specialized and capability execution remains command-local | shared recipe assembly executes verified capabilities; unsupported video remains fail-closed | Complete |
| 16 | Error boundaries | projector attention and missing cache plans can panic during graph/cache use | validated runners retain attention plans; absent compiled cache authority returns an error | Complete |
| 17 | Capability execution | VQA assembles workflow values in a command; catalogs clone per accessor/runtime; raw definitions can compile during execution | shared VQA executor owns lineage; immutable catalog identity admits sealed programs in O(1) | Complete |
| 18 | Composition authority | generic capability runtime imports/selects concrete model families and owns VQA contracts | generic runtime contains mechanics only; composition roots bind implementations from compiled descriptors | Complete |
| 19 | Top-level topology authority | inference and draft loading switch on named forward families; instruction overflow collapses to an empty program | compiled neutral forward/catalog programs own selection; over-capacity programs fail validation | Complete |
| 20 | Image/VQA topology | image and VQA recipes each wrap a complete vertical pipeline in one opaque node | ordered typed stages expose preparation, model execution, and decode boundaries | Complete |
| 21 | Stage-registration paydown | VQA repeats model checks, scalar output assembly, persistence, and adapter registration | one context-aware resolver owns stage output mechanics; composition root resolves inputs only | Complete |
| 22 | Root catalog authority | root weight loading selects topology through CatalogFamily, Forward, MLA, and a second draft dispatcher | one typed root-catalog fact selects an indexed loader; duplicate dispatch deleted | Complete |
| 23 | Forward-program authority | serialized family-forward enum is translated into a second neutral runtime program | profile stores the neutral operation/session program directly; translation and compatibility enum deleted | Complete |
| 24 | Go API ownership | dead, test-only, and compatibility exports enlarge permanent package contracts | tests inspect owner state or public behavior; unused wrappers and publication paths deleted | Complete |
| 25 | Document codec ownership | domain documents duplicate byte-only encoding beside authoritative artifact content | callers consume `artifact.Content`; redundant byte wrappers deleted | In progress |

## Current inventory

- Production direct/compatibility block dispatchers: 0.
- Exported model-family graph constructors: 0.
- Production T5 and free conditioned-diffusion builder calls: 0.
- Trunk, draft, encoder, decoder, split-activation, feature/cache projection, per-layer input, and sequence-output paths enter through sealed model programs.
- Runtime family block policies: 0; embedded profiles contain only typed capability, validation, tensor-layout, and mathematical operator facts.
- Top-level inference binds a neutral compiled forward operation/session contract. Draft weight loading consumes a typed tensor-catalog policy rather than a forward-family name. Fixed-capacity layer programs preserve overflow as an invalid count; split AltUp programs retain their explicit non-monolithic contract.
- `capabilityruntime` contains only JSON/scalar execution mechanics and imports no concrete model family. `cmd/recipe` is the explicit composition root for forecast, tabular, seq2seq, speech, and image bindings. The VQA composition root derives module and port bindings from the compiled recipe before entering its specialized GPU adapter. Video remains fail-closed until its complete production pipeline exists.

## Wave log

- Baseline: branch includes current `master`; worktree clean; full repository tests and vet passed before this campaign.
- Wave 1: sealed `DraftLayerProgram.Build` owns Qwen3.5, Cohere2, NextN, Step3.5, and HYV3 draft block construction. Deleted `compiledDraftBlock`, its translation result, the adapter callback, ignored spec input, and three direct generic dispatches. Production delta: +40/-50. Host model/inference tests and vet pass. CUDA parity gates compile and report explicit `OVERGO_CUDA_TEST=1` skips on this environment.
- Waves 2-4: replaced mutable draft-only authority with sealed `CompiledLayerProgram`; migrated trunk host, non-causal, device-cache, Eagle3, and Gemma 4 callers; deleted `buildLayerBlockFromPlan` and every production compatibility-dispatch call. Production delta: +114/-118. Model, inference, and CUDA-executor package gates pass.
- Wave 5: extended the same sealed program with compiled encoder/decoder roles; migrated T5 runner and CUDA parity caller; deleted exported T5 builder APIs. Consolidated Q/K/V projection geometry, gated/plain FFN, and required-weight validation. Production delta: +133/-104; the +29 authority cost replaces two public family runtimes and is tracked for paydown in Waves 7-8. Host model/inference and CUDA-executor parity graphs pass.
- Wave 6: compiled conditioned-diffusion geometry once; the sealed program now owns cross-context projection, adaptive blocks, and the terminal head. Deleted three public free-form builders, moved static geometry checks out of the layer loop, and changed axis-position construction to exact preallocation. Production delta: +59/-6 before caller simplification, net +53; tracked for leaf-operator paydown. Model, latent-video, and CUDA-executor graph gates pass.
- Wave 7: deleted `BuildArchitectureBlockCached`; migrated model tests, CUDA graph fixtures, and the previously missed `cmd/block-check` production bypass. The command now compiles the loaded model plan and honors recurrent cadence instead of forcing a dense layer. Production delta: +13/-14. Model/CUDA executor gates and repository-wide symbol scan pass.
- Wave 8a: sealed the split activation/projection seam behind `CompiledLayerProgram`; migrated Gemma3n inference and CUDA/model parity fixtures; deleted both exported family-stage builders. Production delta: +35/-33 after consolidation; the two-line authority cost replaces caller-owned spec/layer dispatch. Model, inference, and CUDA-executor gates pass.
- Wave 8b: moved all Qwen3.5, Cohere2, NextN, Step3.5, and HY-V3 draft input/output policy into `CompiledLayerProgram`; deleted ten exported family APIs, five wrapper files, two runtime callbacks, and the Step/HY runtime branch. Policy lookup is indexed and carries no duplicate spec. Production delta: +68/-199, net -131. Model, inference, and CUDA-executor gates pass.
- Wave 8c: made `ModelPlan` retain its compiled spec and changed trunk, draft, encoder, and decoder program lookup to index-only. Migrated all 14 callers; runtime code can no longer resupply mismatched architecture geometry during program admission. Production delta: +26/-24; the two-line sealed-spec cost removes four public duplicate-authority parameters. Model, recipe, inference, command, and CUDA-executor gates pass.
- Wave 8d: moved Eagle3/DFlash feature fusion, DFlash cache projection, Gemma4 per-layer projection, Gemma4-assistant input/output projection, and WavTokenizer sequence output behind `ModelPlan`. Deleted the family builders, runtime profile probe, unused DFlash scale helper, compatibility cache-schema constructor, and the last exported family graph symbol. Production delta: +85/-92, net -7. Shared feature fusion selects only linear or linear+RMS math from the sealed forward policy. Targeted model, recipe, inference, and CUDA gates pass.
- Wave 8e: deleted `RecurrentMixPolicy` and both recurrent instruction fields; `StateSpacePlan` now solely selects selective-scan, grouped-scan, WKV, gated-delta, short-convolution, and keyed-delta execution. Fixed plan compilation to honor weight-derived recurrent layers even when metadata cadence is absent. Production delta: +47/-62, net -15; each layer instruction is two policy fields smaller. Model, inference, and CUDA-executor gates pass.
- Wave 8f: deleted the single-value `HybridMixPolicy` and its instruction field. `LayerOperatorHybridMix` now directly denotes the only neutral parallel attention/state-space operator. Production delta: +6/-23, net -17; each fixed-capacity instruction is one field smaller. Model, inference, and CUDA-executor gates pass.
- Wave 8g: encoded all attention, FFN, and token-shift mathematics directly in `LayerOperator`; deleted `AttentionMixPolicy`, `FeedForwardMixPolicy`, and both instruction fields. Tests now assert exact operators rather than compatibility categories. Production delta: +105/-121, net -16; total delta +140/-166, net -26. Model, inference, and CUDA-executor gates pass.
- Wave 9a: removed `BlockPolicy` from executable `LayerPlan`. Cache topology, dense-graph eligibility, and weight loading now consume compiled attention/cache/state-space facts; family policy exists only while compiling the neutral program. Kimi Linear records keyed-delta capability for both attention and recurrent layers while retaining the per-layer recurrent bit, preserving its latent-cache geometry. Production delta: +8/-11, net -3; total delta +34/-32 because fixtures now assert the replacement program contract. Model, inference, and CUDA-executor gates and vet pass.
- Wave 9b: replaced all weight, metadata, and state-space comparisons against profile `BlockPolicy` with existing typed validation, cache, attention, and compiled state-space facts. PLaMo attention layers now compile their per-head tensor layout as the existing configured/no-bias Q/K operator, preserving weighted RMS math while deleting the loader exception. Removed unused state-space fields and helper. Production delta: +16/-20, net -4. Model, inference, and CUDA-executor gates and vet pass.
- Wave 9c: deleted `BlockPolicy`, both architecture-profile block fields, all 16 serialized family selectors, the ordinal checks, and the compiler translation function. `PlanLayer` now derives composition, cache writes, state-space, MoE behavior, and ordered operators solely from typed facts. Replaced family-loop fixtures with exact typed-profile coverage and updated sealed profile provenance digests. Production delta: +20/-67, net -47; total delta +108/-172, net -64. Model, recipe, inference, and CUDA-executor gates and vet pass.
- Wave 10a: deleted the unused registry-vs-profile compatibility compiler, duplicate model-plan parity probe, parity evidence document/codec/importer, parity-only lifecycle APIs, and the redundant `Active` projection. Serving already resolves verifier-gated active records and compiles the bound profile/model definition once; tests now use private fixtures without exporting fallback authority. Production delta: +0/-305; total delta +34/-417, net -383. Model-recipe, RepoDB importer, inference, and recipe-command gates pass.
- Wave 10b: deleted the file-based model-definition wrapper that rescanned tensor inventory and reparsed the full GGUF spec. Activation and every fixture now pass the already-bound inventory and single parsed spec into `NewModelDefinitionDocument`; the sealed definition still rebinds and validates the profile. Production delta: +2/-27, net -25; total delta +18/-31, net -13. Model-recipe, inference, serving-test, and recipe-command gates and vet pass.
- Wave 10c: removed the advisory video recipe module, compiler entry, catalog registration, command activation entry, and unused inventory loader. `TaskVideoGen` remains a schema kind, but definition/activation now fails until the real prompt-to-context, CUDA denoise, VAE decode, and video-output adapter exists. Production delta: +2/-18, net -16. Model-recipe, recipe-command, and latent-video gates and vet pass.
- Wave 11: replaced Eagle3/DFlash feature fusion and both Gemma 4 assistant projection APIs with one indexed neutral projection program; compiled DFlash K/V geometry into a cache-projection program; and made WavTokenizer catalog loading and execution consume one ordered typed residual program instead of duplicate block-number switches. Deleted the family graph entry points and consolidated model/CUDA projection fixtures. Production Go delta: +163/-167, net -4. Full repository tests and vet pass; CUDA gates compile and retain their explicit hardware skip contract.
- Waves 12-14: made weight residency a typed recipe fact included in program identity; activation now selects stream, host-cache, device F32/native/native+BF16, hybrid fallback, or host-reference execution. Deleted serving preload/cache flags and the `OVERGO_HOST_EXECUTE` runtime backdoor, so hardware binding cannot replace the active policy. Current profiles are mandatory at serving; v1 profiles upgrade to provenance-bound v2 only during RepoDB import. Replaced `ConsumedProgram` getters/`Disown` with atomic `LoadedProgram.Take` and deleted unused panic shape wrappers. Production Go delta: +295/-324, net -29 before this ledger entry. Targeted recipe, import, inference, model, command, and CUDA compile gates pass.
- Waves 15-16: moved capability discovery, HF/safetensors inventories, adapter construction, and active-program resolution from `cmd/recipe` into `capabilityruntime`; the VQA lane now consumes that same resolution path instead of duplicating artifact binding. Removed the projector attention `must` constructor: validated runners retain compiled attention geometry, while dynamic Gemma 3n geometry uses its already-validated tensor relationship. Missing cache authority now returns an explicit validation error instead of panicking behind a dead fallback. Production Go delta: +194/-222, net -28. Capability, recipe-command, VQA compile, projector, model, and inference gates pass.
- Wave 17: moved VQA input binding, adapter registration, and answer lineage into `capabilityruntime`, then deleted the one-use `visionqa` package and command-owned workflow assembly. Image evidence now records raw typed bytes instead of a transient JSON/base64 copy. Sealed recipe catalogs expose no mutator and are shared safely; runtime creation no longer clones them twice. `Program` retains its immutable catalog authority, so execution performs an O(1) identity check and no longer recompiles the recipe DAG or accepts raw definitions. Production Go delta: +106/-142, net -36. Recipe, workflow, model-recipe, capability, recipe-command, and VQA compile gates pass.
- Wave 18: corrected the over-centralized capability registry. Concrete forecast, tabular, seq2seq, speech, and image bindings now live only at `cmd/recipe`; VQA contracts and adapter composition live only at its command boundary and derive ports/module identity from the compiled program. `capabilityruntime` imports no model family or `modelrecipe`; runtime construction derives the immutable catalog directly from `recipe.Program`. Added shared raw-content and HF-directory inventory mechanics while deleting duplicate identify/content and open/inventory/close sequences. Production Go delta: +260/-264, net -4. Capability, recipe-command, VQA, artifact, inventory, importer, recipe, and workflow gates pass.
- Wave 19: compiled a neutral forward operation/session into `ModelPlan`; inference now uses indexed executors and session requirements without switching on named forward families. Draft weight loading uses an explicit model-catalog policy. Layer-program overflow retains its invalid count and fails plan validation instead of comparing as an empty program; intentionally split AltUp execution remains explicit. Full repository tests and vet pass; committed as `4e520b6` and merged with `master`.
- Wave 20: replaced the opaque image node with prepare, integrate/readout, and decode stages that also back the direct generator API. Replaced the opaque VQA node with host image/prompt preparation and device prefill/decode stages; the compiled mixed-placement recipe now owns that boundary. Deleted the former monolithic module identities and kept the complete numerical pipeline unchanged. Production Go delta: +191/-107, net +84; test delta: +77/-28, net +49. The growth records previously hidden typed boundaries rather than parallel implementations; follow-up paydown should generalize multi-input/context stage registration. Focused model-recipe, oscillator-image, recipe-command, and VQA gates pass.
- Wave 21: introduced one context-aware resolved-stage registration path and routed scalar stages through it. VQA now resolves only its typed inputs and computations; shared runtime mechanics own model identity, output validation, value construction, optional persistence, and adapter registration. Production delta: +53/-45, net +8; the VQA composition root itself is -19 lines. Full repository tests and vet pass; committed as `730459e` and merged with `master`.
- Wave 22: replaced root weight selection through `CatalogFamily`, `Forward`, MLA validation, and the separate draft dispatcher with one typed `ModelCatalog.Weights` fact and indexed loader table. Production Go delta: +29/-42, net -13; the draft dispatcher is deleted. Full repository tests and vet pass; committed as `6dc13ce` and merged with `master`.
- Wave 23: made the neutral forward operation/session program the serialized profile fact. Deleted `ForwardPolicy`, its eight family routes, and the translation switch; metadata, projections, layers, sequence execution, and command assembly now consume neutral operation/session facts directly. Production Go delta: +32/-60, net -28. Full repository tests and vet pass.
- Wave 24a: encoded the binding Go engineering doctrine, then removed an unused device-tensor alias, a dead device graph-input wrapper, an unused cache-schema accessor, a dead candidate-publication branch, and three test-only `LoadedProgram` inspection methods. Tests now inspect sealed state at the owning package or verify the consumed runner boundary. Production Go delta: +4/-74, net -70; test Go delta: +12/-23, net -11. Focused model, model-recipe, and inference tests pass.
- Wave 24b: deleted the unused registered-profile alias/catalog authority, bootstrap publisher, test-only profile compiler, and five inspection/parse exports. Kept the active model-definition publication path and made its provenance facts directly prove missing-lineage rejection. Embedded-profile fixtures are now explicitly test-owned. Production Go delta: +2/-136, net -134; test Go delta: +56/-127, net -71. Focused model-recipe tests pass.
- Wave 24c: deleted three test-only draft-policy predicates, an unused host argmax wrapper, and an aggregate layer catalog with no runtime consumer. Existing tests now assert the authoritative `DraftPlan`, draft catalogs, and complete streamed row-score vector directly. Production Go delta: +0/-53; test Go delta: +7/-37, net -30. Focused model and inference tests pass.
- Wave 24d: deleted the redundant model-only recipe constructor, artifact digest inspector, RepoDB recovery/snapshot inspectors, and their retained store fields. Tests now construct canonical dependency recipes and prove recovery/snapshot behavior through persisted results rather than instrumentation state. Production Go delta: +10/-37, net -27; test Go delta: +13/-46, net -33. Focused artifact, recipe, RepoDB, and capability-runtime tests pass.
- Wave 25a: deleted thirteen domain-level `ContentBytes` wrappers that had no production callers. Finding, run-record, model-definition, dataset, tensor-inventory/measurement, closure-ledger, and recipe tests now validate canonical bytes through their authoritative `artifact.Content`. Production Go delta: +0/-49; test Go line count unchanged. Focused document-owner tests pass.
- Wave 25b: deleted test-only membership, environment, and tensor-measurement parsers; removed the unused default measurement policy and derived sample-fraction accessor; made raw measurement-document construction package-owned behind GGUF/Safetensors measurement. Production Go delta: +5/-33, net -28; test Go delta: +6/-5, net +1. Focused dataset, run-record, model-artifact, and importer tests pass.
