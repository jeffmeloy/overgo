{
  "campaign": "Professional operator GUI: a consumer-simple front page over the served model with model selection and live switching, multimodal import and modality-adaptive inference, and the analysis workbench retained behind it",
  "doctrine": "This campaign lives in the professional_overgo_gui worktree (owner directive 2026-09-04): every row is built, verified and committed there through the plan-bound gate, never in the master worktree, and this lane plan.json never merges (lane merge protocol: first-parent-target projection, retired rows dead on the first-parent chain). The GUI stays a thin client: no build step, no external assets, same-origin content-security policy, embedded in cmd/server, and every panel projects server records and submits bounded actions; state that must survive a reload lives in the server's interaction and run records, never only in the browser. The front page is the product: one conversation surface a first-time user reads without instruction, with the served model, its evidence and its live switch in view, and attachments accepted or refused by the served recipe's declared capabilities, never by a client-side guess. The existing workbench (app.html, its 24 modules, the operations shell, the analysis inspectors) keeps every capability it has today and keeps passing its real-browser acceptance lane at every commit; a workbench capability the front page absorbs is retired from the old shell only after the front page's own acceptance step proves it. Verification is machine-checked: hermetic Go tests over the static routes and the JSON contracts, and go run ./cmd/webui-lane driving a real headless browser for every user-facing behavior; a missing browser, model or device reports UNAVAILABLE, which is never passing evidence. Rows whose exact verify depends on execution-time facts (installed models, browser presence) bind their final verify through plan -setverify before work starts, never through prose. Simplification precedes capability (owner rule 2026-09-04): the gui-simplify rows land before the front page is built on them, each names the capability it gains and the ratchet it lowers, and every ratchet is a Go test with a declared ceiling that only tightens, so capability improvement by simplification is measured on the client the way the clone and structure budgets measure it on the Go tree. Patterns adopted from the go-micro review of 2026-09-04 (the appendix of docs/gui/ANALYSIS.md: typed stream events with tool-call cards, checkpointed run resume, observable memory compaction, the empty state as the getting-started card, the first-run flow as a verified contract, run inspection with a status vocabulary, an API explorer derived from the route table, dev-time asset reload) enter as behaviors and contracts only; no code is copied or imported from that tree, and tool calling stays UTCP through the store's manuals. Design records are written (docs/gui/ANALYSIS.md, the row rationales); reports of evidence are generated. Findings outside a row are parked with cmd/finding and scheduled by the close-out row, not absorbed silently. The rows after gui-closeout (long-context-collapse, gemma-12b-accuracy, benchmark-completion, model-regression-gate, model-regression-baseline, modality-verification, failure-recovery, the simplify-* rows, campaign-closeout) are the master worktree's validation campaign retained at the lane base 9dce4fca because the plan authority protects every identity committed at the protected revision; they are never worked in this lane and drop out at merge.",
  "census_evidence": "evidence:sha256:4757a6529e4ca6a47ae01f4ad94978209e5c94188ed2f0f04714c9adf36ec7ff",
  "items": [
    {
      "id": "gui-shell",
      "title": "The front page: one conversation surface, the served model in view, the workbench one click away",
      "status": "open",
      "steps": [
        {
          "id": "model-switch",
          "title": "Model selection and live switching on the front page: the selector lists the store's servable catalog (/catalog/models) with each model's evidence line (benchmark prompt and decode tok/s, latest evaluation scores) and its declared task capabilities, switches the served model live through the swap proxy (/health?swap=) with load progress and the new model's context length and modalities shown on arrival, explains itself when the proxy is absent, and remembers the last chosen model per browser; a stale activation is listed as unservable with the loader's reason, never as a choice",
          "status": "open",
          "verify": "go test ./internal/server -run 'TestFrontPageModelSwitch' -count=1",
          "rationale": "The pill and the swap proxy already do the work; the front page must make choosing a model as visible as choosing a conversation, with evidence beside each entry.",
          "depends_on": [
            "gui-shell/front-page"
          ]
        }
      ]
    },
    {
      "id": "gui-conversations",
      "title": "Durable conversations owned by the server",
      "status": "open",
      "steps": [
        {
          "id": "durable-sessions",
          "title": "The front page lists, resumes, renames and deletes conversations through the server's interaction records (the same records the chat workspace and agent sessions replay from), so a conversation survives a reload, a server restart and a model switch, and every turn shows the model that produced it; an in-flight turn survives too: after a reload or a dropped connection the page reattaches to the turn's run record and receives the remainder of the stream, with completed parts replayed from the record and nothing re-executed; the conversation's generation settings and system prompt live in the record, not the browser; a context meter shows tokens used against the model's declared context, and when the server compacts or shifts context the event is visible in the thread with the compacted summary inspectable; the browser holds no conversation state a re-fetch cannot rebuild",
          "status": "open",
          "verify": "go test ./internal/server -run 'TestFrontPageConversations' -count=1",
          "rationale": "The README already claims conversations continue across restarts because the interaction record owns the state; the front page must expose that record, resume through it, and make what the server does to the context visible rather than silent.",
          "depends_on": [
            "gui-shell/front-page"
          ]
        }
      ]
    },
    {
      "id": "gui-multimodal",
      "title": "Multimodal import and modality-adaptive inference",
      "status": "open",
      "steps": [
        {
          "id": "import",
          "title": "Import by attach, drag-and-drop and paste: images (png, jpeg, gif, webp), audio (wav, and any format the server's media policy decodes), video (mp4, gif), and documents (pdf, plain text, markdown, csv, json) with previews and byte and dimension limits read from the server's media policy before upload; attachments travel as the served protocol's own content parts (image_url, input_audio, input_video, and a document part the server extracts to text) with no side channel; the composer accepts or refuses each attachment from the served recipe's declared capabilities and shows the reason",
          "status": "open",
          "verify": "go test ./internal/server -run 'TestFrontPageImport' -count=1",
          "rationale": "chat.js already sends attachments as protocol parts; what is missing is discoverability (drop and paste), documents, previews, and refusal by declared capability instead of by server error.",
          "depends_on": [
            "gui-shell/model-switch"
          ]
        },
        {
          "id": "dynamic-inference",
          "title": "The front page derives its abilities from the served recipe: text chat, vision questions, audio input, image generation, video generation and editing, speech synthesis, embeddings and reranking appear as composer modes only when the active recipe declares them (the capability document), generated images, audio and video render inline in the conversation as media stream events and are saved as artifacts with provenance, and a model switch re-derives the composer without a reload",
          "status": "open",
          "verify": "go test ./internal/server -run 'TestFrontPageModalities' -count=1",
          "rationale": "The generation workspaces exist as separate tabs (image.js, video.js, speech.js); a user should reach them from the conversation as modes of one composer, gated by evidence, not by tab discovery.",
          "depends_on": [
            "gui-multimodal/import"
          ]
        }
      ]
    },
    {
      "id": "gui-workbench",
      "title": "The analysis, agent and operations capabilities retained, and reached from the conversation",
      "status": "open",
      "steps": [
        {
          "id": "inspect-turn",
          "title": "Inspect this turn: from any assistant turn the front page opens the logits, hidden-state and attention inspectors for that exact prompt and completion (the same /analyze endpoints the workbench uses), the model, vocabulary and tensor inspectors for the served model, and the turn's run record (status from one declared vocabulary: running, done, cancelled, timeout, refused, error; timings; the receipt and trace identities) in a side panel that does not leave the conversation; the existing analysis tabs stay intact",
          "status": "open",
          "verify": "go test ./internal/server -run 'TestFrontPageInspect' -count=1",
          "rationale": "The investigation capability is the workbench's distinguishing feature; today it is disconnected from the conversation that produced the output being investigated, and the run behind a turn is not visible at all.",
          "depends_on": [
            "gui-conversations/durable-sessions"
          ]
        },
        {
          "id": "agent-in-thread",
          "title": "Agent mode inside the conversation: the user picks an agent definition (the immutable binding of prompt, model configuration, UTCP tools and policies the store holds) and the same thread runs the session through the existing /agents surface; tool calls render inline as the composer's collapsible cards, an action outside the session's standing authority renders as an inline approval card bound to the inbox record with the exact tool identity and arguments (nothing executes on a stale approval), the session's guardrails are visible (step budget and loop limit with what remains), a plan the agent records renders as a live checklist, and a delegated sub-session renders as a nested card with its own receipt; the workbench agent tab stays intact",
          "status": "open",
          "verify": "go test ./internal/server -run 'TestFrontPageAgent' -count=1",
          "rationale": "Agent sessions, the approvals inbox and the authority maps exist; the front page must let a user run an agent from the same thread they chat in and see every tool call and approval where it happens, with tool calling staying UTCP.",
          "depends_on": [
            "gui-simplify/one-composer",
            "gui-conversations/durable-sessions"
          ]
        },
        {
          "id": "operations-strip",
          "title": "Long operations on the front page: model load and switch, generation jobs, evaluations and training sessions show in the operations strip with progress, cancel, the live log tail of the running operation and the durable receipt, reusing operations_shell.js and /operations/evidence; the header carries status dots for the server, the swap proxy and the device (with peak memory) and the approvals inbox count, which opens the inbox",
          "status": "open",
          "verify": "go test ./internal/server -run 'TestFrontPageOperations' -count=1",
          "rationale": "The operations shell already exists for the workbench; the front page must not hide a running swap or evaluation, or the health of what serves it, from the user who started it.",
          "depends_on": [
            "gui-shell/model-switch"
          ]
        }
      ]
    },
    {
      "id": "gui-quality",
      "title": "Professional finish: accessibility, responsiveness, and a real-browser acceptance lane for the front page",
      "status": "open",
      "steps": [
        {
          "id": "accessibility-responsive",
          "title": "Keyboard-only operation of the whole front page (composer, selector, conversation list, inspector), visible focus, reduced-motion respect, light and dark themes from the existing token set, and layouts for desktop, tablet and phone widths driven through the browser lane's viewport emulation",
          "status": "open",
          "verify": "go run ./cmd/webui-lane",
          "rationale": "A consumer-simple page that cannot be used from the keyboard or on a narrow window is not simple; the existing lane already emulates viewports.",
          "depends_on": [
            "gui-workbench/inspect-turn",
            "gui-workbench/operations-strip"
          ]
        },
        {
          "id": "acceptance-lane",
          "title": "Extend the real-browser acceptance lane to drive the front page end to end against a served model, and make the first-run flow a verified contract: starting from no served model, choose one from the catalog, send the first message and receive a streamed reply, attach an image and receive a grounded reply from a vision-capable model, switch models and see the composer re-derive, open the inspector for a turn, run an agent turn with a tool call, cancel a running operation; the lane is selected by the code manifest for every commit that touches the webui, the existing workbench acceptance steps keep running, and a missing browser or model reports UNAVAILABLE",
          "status": "open",
          "verify": "go run ./cmd/webui-lane",
          "rationale": "The campaign's claims are user-facing behaviors; only a real browser proves them, and the first five minutes are the behavior most worth proving on every change.",
          "depends_on": [
            "gui-multimodal/dynamic-inference",
            "gui-workbench/agent-in-thread",
            "gui-quality/accessibility-responsive"
          ]
        }
      ]
    },
    {
      "id": "gui-docs",
      "title": "Documentation, launcher and manifest follow the front page",
      "status": "open",
      "steps": [
        {
          "id": "readme-launcher-manifest",
          "title": "The README's workbench section describes the front page and the retained workbench as they are, overgo_gui.bat opens the front page, and the API manifest records every route the front page uses (go run ./cmd/api-manifest -update, then -check)",
          "status": "open",
          "verify": "go run ./cmd/api-manifest -check",
          "depends_on": [
            "gui-quality/acceptance-lane"
          ]
        }
      ]
    },
    {
      "id": "gui-closeout",
      "title": "Close the campaign and prepare the lane for merge",
      "status": "open",
      "steps": [
        {
          "id": "closeout",
          "title": "Dispose every finding parked during the campaign, confirm the workbench acceptance lane and the front-page acceptance lane both pass on the lane head, generate the simplification report (shells, hand-listed scripts, modules, webui JavaScript lines, fetch and SSE call sites, authorization blocks and GUI routes at the fork and at the head, beside the acceptance-lane behaviors proven), and record the merge readiness for the master worktree under the lane merge protocol (the lane plan.json does not merge; the merge commit binds to the target plan)",
          "status": "open",
          "verify": "go run ./cmd/plan -status",
          "depends_on": [
            "gui-docs/readme-launcher-manifest"
          ]
        }
      ]
    },
    {
      "id": "long-context-collapse",
      "title": "Long-context collapse past 4096 tokens (owner rule 2026-09-04: fix speed and accuracy before a full run): the long-form ladder of 2026-09-04 read Qwen2.5-0.5B at rung 8192 decoding 9 tokens at 2.1 tok/s with the true continuation's NLL at 6.69 under the long context against 3.45 under the short (gain -3.2), and at rung 16384 decoding at 1.0 tok/s with gain -3.1, where rungs 1024 to 4096 decode at 125 to 295 tok/s with positive gain; find the path the decode takes past 4096 tokens of context (cache paging, the decode attention kernel's key extent, the causal prefill span) with a profile of the ladder, fix it, and climb the ladder again on the 0.5B and the E4B as the verify",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Long-context collapse past 4096 tokens (owner rule 2026-09-04: fix speed and accuracy before a full run): the long-form ladder of 2026-09-04 read Qwen2.5-0.5B at rung 8192 decoding 9 tokens at 2.1 tok/s with the true continuation's NLL at 6.69 under the long context against 3.45 under the short (gain -3.2), and at rung 16384 decoding at 1.0 tok/s with gain -3.1, where rungs 1024 to 4096 decode at 125 to 295 tok/s with positive gain; find the path the decode takes past 4096 tokens of context (cache paging, the decode attention kernel's key extent, the causal prefill span) with a profile of the ladder, fix it, and climb the ladder again on the 0.5B and the E4B as the verify",
          "status": "open",
          "verify": "go test ./internal/longform -count=1"
        }
      ]
    },
    {
      "id": "gemma-12b-accuracy",
      "title": "Chat-shaped long-form rung (owner rule 2026-09-04: long-form output verification): the raw-continuation NLL of the instruction-tuned gemma-4-12B twins (6.94, 6.97 nat/token against the E4B's 4.21) is the model answering the document instead of continuing it, its greedy output an assistant synthesis of the README, so the raw shape cannot judge a chat model's long-form output; the long-form run adds, for every model that declares a chat template, one chat-shaped generation at the judged rung (the rung's text as the user turn with an instruction to continue it, the model turn opened, thinking off, 256 greedy tokens) and records its text, distinct 4-gram ratio, longest repeated span, decode tok/s and the teacher-forced NLL of the text's own continuation under that framing; the chat-shaped NLL is recorded beside the raw one, the fingerprint compares both, and the floors stay as declared",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Chat-shaped long-form rung (owner rule 2026-09-04: long-form output verification): the raw-continuation NLL of the instruction-tuned gemma-4-12B twins (6.94, 6.97 nat/token against the E4B's 4.21) is the model answering the document instead of continuing it, its greedy output an assistant synthesis of the README, so the raw shape cannot judge a chat model's long-form output; the long-form run adds, for every model that declares a chat template, one chat-shaped generation at the judged rung (the rung's text as the user turn with an instruction to continue it, the model turn opened, thinking off, 256 greedy tokens) and records its text, distinct 4-gram ratio, longest repeated span, decode tok/s and the teacher-forced NLL of the text's own continuation under that framing; the chat-shaped NLL is recorded beside the raw one, the fingerprint compares both, and the floors stay as declared",
          "status": "open",
          "verify": "go test ./internal/longform -count=1"
        }
      ]
    },
    {
      "id": "benchmark-completion",
      "title": "Complete the pinned benchmark set for every text model, all suites except MATH",
      "status": "open",
      "steps": [
        {
          "id": "mmlu-pro-pass",
          "title": "Record MMLU-Pro (12032 cases) for every servable text model under both protocols, smallest first; the 27B runs as its own operator-launched pass with -budget 3h",
          "status": "open",
          "verify": "go run ./cmd/benchmark-report -check",
          "rationale": "MMLU-Pro is the suite the model cards report, so it is the comparison that matters; it is also the largest, and the 27B at about 0.7 s per case needs over two hours. The 27B pass is launched by the owner, never unattended by the loop, and the smaller models complete first.",
          "depends_on": [
            "benchmark-completion/dna-pass"
          ]
        },
        {
          "id": "published-comparison",
          "title": "Compare every recorded suite against its declared published references and the local lm-eval oracle runs, extend the reference declarations to every model that has a model card, and regenerate docs/BENCHMARK.md with a per-model comparison section",
          "status": "open",
          "verify": "go run ./cmd/benchmark-report -check",
          "rationale": "A benchmark number without its reference is a bare fact; the references are store declarations with sources and protocols, and the report prints measured beside published with the protocol difference in view. MATH stays excluded and the report says so.",
          "depends_on": [
            "benchmark-completion/mmlu-pro-pass"
          ]
        }
      ]
    },
    {
      "id": "model-regression-gate",
      "title": "Gate selection of the model regression check (owner rule 2026-09-04): the code manifest selects longform -check when execution code changes (internal/cuda, internal/tensor, internal/inference, internal/model, internal/gguf, internal/quant, kernels/), on the exclusive device resource ahead of the device lane, scoped to the servable models whose recipes carry a weight type the changed kernels serve when the kernel manifest proves that scope and to the full servable set otherwise, one model per weight type at most; inapplicable, never skipped, for commits outside execution code. The structure-budget test (internal/repoanalysis TestStructureBudgetRatchets, 3 s) runs unconditionally in the gate's test step: it caught the executor.go overrun the fp8 gate missed",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Gate selection of the model regression check (owner rule 2026-09-04): the code manifest selects longform -check when execution code changes (internal/cuda, internal/tensor, internal/inference, internal/model, internal/gguf, internal/quant, kernels/), on the exclusive device resource ahead of the device lane, scoped to the servable models whose recipes carry a weight type the changed kernels serve when the kernel manifest proves that scope and to the full servable set otherwise, one model per weight type at most; inapplicable, never skipped, for commits outside execution code. The structure-budget test (internal/repoanalysis TestStructureBudgetRatchets, 3 s) runs unconditionally in the gate's test step: it caught the executor.go overrun the fp8 gate missed",
          "status": "open",
          "verify": "go test ./internal/automationcheck -run 'ModelRegress' -count=1"
        }
      ]
    },
    {
      "id": "model-regression-baseline",
      "title": "Record the regression fingerprints for every servable text model from the clean evidence worktree (owner rule 2026-09-04; both shapes, smallest first, 27B last, inside -budget), fold perf-sweep's wall and peak-device baseline (docs/performance_sweep_baseline.json) into the same evidence so one check owns model-level regression, and bind longform -check as this row's verify through -setverify once the evidence identities exist",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Record the regression fingerprints for every servable text model from the clean evidence worktree (owner rule 2026-09-04; both shapes, smallest first, 27B last, inside -budget), fold perf-sweep's wall and peak-device baseline (docs/performance_sweep_baseline.json) into the same evidence so one check owns model-level regression, and bind longform -check as this row's verify through -setverify once the evidence identities exist",
          "status": "open",
          "verify": "go run ./cmd/model-regress -check -repo overgodb-store"
        }
      ]
    },
    {
      "id": "modality-verification",
      "title": "Verify every registered model's activated modalities against a declared expectation",
      "status": "open",
      "steps": [
        {
          "id": "capability-census",
          "title": "Publish the capability census: every registered model with its activated tasks, evidence tier, staleness, and on-disk presence, as a store record and a generated table",
          "status": "open",
          "verify": "go run ./cmd/overgodb-query -repo overgodb-store -verifications",
          "rationale": "The catalog holds about 28 models across 9 tasks and the verification matrix names 5; the census is the denominator every following row reports against. A stale activation is listed as stale, never dropped.",
          "depends_on": [
            "benchmark-completion/published-comparison"
          ]
        },
        {
          "id": "declared-smoke-expectations",
          "title": "Replace the non-empty-output smoke oracle with a declared expectation per model: an instruct model gets a chat probe with a substring oracle, a base model a likelihood floor, a domain model its domain probe; a model without a declaration is reported unverified",
          "status": "open",
          "verify": "go run ./cmd/smoke-lane",
          "rationale": "The verified-matrix row overclaimed correctness on a liveness check; a fix surfaced by the drill is admitted under the correct-as-you-go directive. The lane's exit code is the verdict and its audit line names every model that ran without an expectation.",
          "depends_on": [
            "modality-verification/capability-census"
          ]
        },
        {
          "id": "text-and-vision",
          "title": "Verify the chat, embedding, rerank, and vision-language activations (Qwen, gemma-4, MiniCPM, Qwen3-VL, SenseNova) through smoke-lane, cmd/embedding, cmd/rerank, and vqaparity or sensenovaparity with their declared oracles",
          "status": "open",
          "verify": "go run ./cmd/smoke-lane",
          "rationale": "These are the largest activation group; each owner already exists and reports UNAVAILABLE when an oracle or fixture is missing. Bind the exact lane sequence with -setverify when the row starts.",
          "depends_on": [
            "modality-verification/declared-smoke-expectations"
          ]
        },
        {
          "id": "image-and-video",
          "title": "Verify the image and video generation activations (Krea-2-Turbo, SimpleDiffusion, Wan2.1-T2V, LiveEdit, Un-0 latent video) through the composite generation lane and latentvideo-run against the media verifier fixtures, exporting samples",
          "status": "open",
          "verify": "go run ./cmd/composite-generation-lane",
          "rationale": "The media-gen campaign left measured phases, peaks, and verifier runs; this row re-runs them at the current commit so the media report reflects code that exists. Bind the exact lane sequence with -setverify when the row starts.",
          "depends_on": [
            "modality-verification/text-and-vision"
          ]
        },
        {
          "id": "speech-ocr-tabular-forecast",
          "title": "Verify the speech synthesis (pocket-tts), OCR (Unlimited-OCR, SenseNova infographic), tabular (tabular-train-probe), series forecast, and embodied (Hy-Embodied-RxBrain) activations through their owners, each against a declared oracle or reported unverified",
          "status": "open",
          "verify": "go run ./cmd/eval-lane",
          "rationale": "These activations are the ones the memory notes as stale ('active recipe lacks verified evidence'); the row either verifies them at the current commit or records exactly which oracle is missing as a finding. Bind the exact owner sequence with -setverify when the row starts.",
          "depends_on": [
            "modality-verification/image-and-video"
          ]
        },
        {
          "id": "media-report",
          "title": "Regenerate docs/MEDIA_REPORT.md from the store's verification records and link the benchmark report from it, so one generated document summarizes every model's verified modalities and benchmark rows",
          "status": "open",
          "verify": "go run ./cmd/compatibility -check",
          "rationale": "The report is generated by cmd/compatibility -update-media from store activations and verification claims; hand-written summaries are refused. Its check is the verdict that the document matches the store.",
          "depends_on": [
            "modality-verification/speech-ocr-tabular-forecast"
          ]
        }
      ]
    },
    {
      "id": "failure-recovery",
      "title": "Deliberate failure and recovery drills carried over from phase 1",
      "status": "open",
      "steps": [
        {
          "id": "rollout-plan-author",
          "title": "Add the rollout-plan authoring door (recipe rollout-plan: baseline, candidate, admission evidence, cohort evidence, rollback authority) that derives the cohort share and observation contract from the admission evidence, so rollout projection, quarantine re-entry, and live rollback have a committed plan to run against (finding c9c88020)",
          "status": "open",
          "verify": "go test ./internal/runrecord -run 'TestRolloutPlan' -count=1",
          "rationale": "Three control-plane owners parse a committed rollout plan and no command authors one; the phase-1 drill stopped on this prerequisite. It is construction admitted under the correct-as-you-go directive because the drill cannot run without it.",
          "depends_on": [
            "modality-verification/media-report"
          ]
        },
        {
          "id": "control-plane-drills",
          "title": "Exercise circuit-breaker escalation, candidate quarantine and re-entry, rollback to the predecessor recipe, peer timeout and partial response, and workflow restart with persistent obligations",
          "status": "open",
          "verify": "go build ./cmd/loop ./cmd/agent-loop",
          "rationale": "The control plane's refusal and recovery paths are capability claims like any other; each has an evidence door under cmd/loop and cmd/agent-loop. Bind the drill sequence via -setverify when it starts.",
          "depends_on": [
            "failure-recovery/rollout-plan-author"
          ]
        },
        {
          "id": "gate-recovery-drill",
          "title": "Interrupt a gate run against a scratch clone and recover through -recover-preparation, -recover-interrupted, and the watchdog",
          "status": "open",
          "verify": "go build ./cmd/gate",
          "rationale": "The gate is the most state-bearing process in the repository -- plan mutation, commit, and store writes in one transaction -- and its recovery paths deserve the same drill as checkpoints. Bind the scripted drill via -setverify when it starts.",
          "depends_on": [
            "failure-recovery/control-plane-drills"
          ]
        }
      ]
    },
    {
      "id": "simplify-prefill-paths",
      "title": "Prefill-path unification (owner rule 2026-09-04, capability improvement by simplification): mul_mat prefill now has four paths (exact F32 staging with SGEMM, f16 staging with the tensor-core GEMM for quantized and fp8 weights, direct tensor-core for half-precision weights, the span kernels below the column floor); derive one prefill policy from the weight type, the column count and the measured parity bounds already committed, retire the exact F32 staged path and the per-type duplicates it leaves behind, and let the regression ladder show the prompt tok/s curve against context length on every servable model before and after, the curve recorded as evidence and quoted in the commit; a path may be retired only where the f16-staged parity bound already holds, and the fp8 12B's 1646 prompt tok/s against its bf16 twin's 2850 is the gap the unification is measured against",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Prefill-path unification (owner rule 2026-09-04, capability improvement by simplification): mul_mat prefill now has four paths (exact F32 staging with SGEMM, f16 staging with the tensor-core GEMM for quantized and fp8 weights, direct tensor-core for half-precision weights, the span kernels below the column floor); derive one prefill policy from the weight type, the column count and the measured parity bounds already committed, retire the exact F32 staged path and the per-type duplicates it leaves behind, and let the regression ladder show the prompt tok/s curve against context length on every servable model before and after, the curve recorded as evidence and quoted in the commit; a path may be retired only where the f16-staged parity bound already holds, and the fp8 12B's 1646 prompt tok/s against its bf16 twin's 2850 is the gap the unification is measured against",
          "status": "open",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/cuda/executor -run 'TensorCore|Staged' -count=1"
        }
      ]
    },
    {
      "id": "simplify-command-surface",
      "title": "Command-surface fold (owner rule 2026-09-04, simplification): the nineteen single-purpose probe, lane and parity commands (the *-train-probe family, the *-lane family, sensenovaparity, vqaparity) fold into a train-probe command and a lane command with typed subcommands and the parity probes into eval-lane, each retired command's behavior reachable through the survivor and recorded as a move in the API manifest; a command-package ceiling enters docs/structure_budgets.json at the new count and only tightens; the plan, gate, loop and hook adapters keep their commands",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Command-surface fold (owner rule 2026-09-04, simplification): the nineteen single-purpose probe, lane and parity commands (the *-train-probe family, the *-lane family, sensenovaparity, vqaparity) fold into a train-probe command and a lane command with typed subcommands and the parity probes into eval-lane, each retired command's behavior reachable through the survivor and recorded as a move in the API manifest; a command-package ceiling enters docs/structure_budgets.json at the new count and only tightens; the plan, gate, loop and hook adapters keep their commands",
          "status": "open",
          "verify": "go run ./cmd/preflight"
        }
      ]
    },
    {
      "id": "simplify-execution-core",
      "title": "Execution-core deduplication (owner rule 2026-09-04, simplification): lower the duplicate-excess ceiling in docs/clone_baseline.json by a reviewed, measured amount concentrated in internal/cuda and internal/inference (the launch-layout, staging and attention paths that grew by copy during the prefill rows), retire the executor.go 2400-line standing exception in docs/structure_budgets.json by decomposition, and bind the new ceiling as this row's verify through -setverify before work starts; the model regression check must hold on every servable model at every step, so the reduction is proven not to move a token or a rate",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Execution-core deduplication (owner rule 2026-09-04, simplification): lower the duplicate-excess ceiling in docs/clone_baseline.json by a reviewed, measured amount concentrated in internal/cuda and internal/inference (the launch-layout, staging and attention paths that grew by copy during the prefill rows), retire the executor.go 2400-line standing exception in docs/structure_budgets.json by decomposition, and bind the new ceiling as this row's verify through -setverify before work starts; the model regression check must hold on every servable model at every step, so the reduction is proven not to move a token or a rate",
          "status": "open",
          "verify": "go run ./cmd/clone-census -summary -max-excess 10012"
        }
      ]
    },
    {
      "id": "simplify-checkpoint-locations",
      "title": "Checkpoint relocation (owner rule 2026-09-04, simplification): the servable checkpoints still resolve to paths under the retired adaptive_new tree; relocate them under overgo's models directory, re-record every model's location in the store through the existing location owner, retire the adaptive_new path from every baseline and fixture that names it, and prove the servable predicate holds for every model at its new location, so the living system no longer depends on the retired tree's disk layout",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Checkpoint relocation (owner rule 2026-09-04, simplification): the servable checkpoints still resolve to paths under the retired adaptive_new tree; relocate them under overgo's models directory, re-record every model's location in the store through the existing location owner, retire the adaptive_new path from every baseline and fixture that names it, and prove the servable predicate holds for every model at its new location, so the living system no longer depends on the retired tree's disk layout",
          "status": "open",
          "verify": "go run ./cmd/overgodb-query -repo overgodb-store -servable"
        }
      ]
    },
    {
      "id": "simplify-capability-report",
      "title": "Capability-versus-mechanism report (owner rule 2026-09-04, simplification campaign close): a generated report sets the committed benchmark records and regression fingerprints (short shape, context ladder, capability sample) recorded before the simplification rows against those recorded after, beside the mechanism deltas the ratchets measure (duplicate-excess nodes, command packages, production lines, standing budget exceptions), so the campaign's claim that capability held or rose with less mechanism is a number, not a narrative; a row whose after-numbers fall below its before-numbers is listed as a regression finding, never averaged away",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Capability-versus-mechanism report (owner rule 2026-09-04, simplification campaign close): a generated report sets the committed benchmark records and regression fingerprints (short shape, context ladder, capability sample) recorded before the simplification rows against those recorded after, beside the mechanism deltas the ratchets measure (duplicate-excess nodes, command packages, production lines, standing budget exceptions), so the campaign's claim that capability held or rose with less mechanism is a number, not a narrative; a row whose after-numbers fall below its before-numbers is listed as a regression finding, never averaged away",
          "status": "open",
          "verify": "go run ./cmd/benchmark-report -check"
        }
      ]
    },
    {
      "id": "campaign-closeout",
      "title": "Close the campaign",
      "status": "open",
      "steps": [
        {
          "id": "closeout",
          "title": "Publish the campaign census evidence, bind it with plan -bind-census, record the final verification matrix and benchmark and media reports at the closing commit, write the owner-facing summary into the commit body, and clear the recorded stop so the plan is empty",
          "status": "open",
          "verify": "go run ./cmd/plan -status",
          "rationale": "A campaign ends with the plan empty and every claim bound to evidence at one commit; the close-out row is where the loop proves it has nothing left to do.",
          "depends_on": [
            "failure-recovery/gate-recovery-drill"
          ]
        }
      ]
    }
  ]
}
