overgo training verification refusals: the archaeology batch
Archaeology across all 9,343 reachable commits of the reference repo
(adaptive_new) establishes that no Go training code ever existed for the
families below; their checkpoints are unmodified vendor artifacts (safetensors
metadata carries no Go-training provenance; no checkpoints/ output exists).
Inventing a training objective where the reference never had one would
contradict the archaeology-first doctrine; each family below records a typed
refusal whose closure route is a deliberate future port, not a smoke.

LiveEdit (model:sha256:618e6c57...): upstream cp-cp/LiveEdit .pt checkpoints
(base Wan2.1-T2V, Ditto-1M data); reference recipe is generation-only.
Closure: port a video-edit diffusion training graph as its own campaign, or
declare training-unsupported for vendor edit checkpoints.

Wan2.1-T2V-1.3B (model:sha256:96b6b242...): reference has ~70 wan_* files,
zero with training; generation recipe only. Closure: text-to-video flow
training campaign or training-unsupported declaration.

Krea-2-Turbo (model:sha256:a7d90955... ledger row Krea-2-Turbo): forward
denoiser only upstream; generation recipe only. Closure: latent-flow
finetune campaign or training-unsupported declaration.

SenseNova-U1-8B: generation recipe only upstream (the one training-adjacent
commit renames Supernova/neo_chat identifiers, not SenseNova). Closure:
image-generation finetune campaign or training-unsupported declaration.

Bonsai-27B-Q1 (ternary GGUF): decode-only upstream; no recipe scope at all;
no safetensors. Ternary quantized weights are not a gradient substrate.
Closure: requires a dequantized master-weight lane (quantization-aware
training campaign) or training-unsupported declaration for ternary serving
artifacts.

Qwen3.5-9B-Q8 (hybrid recurrent, GGUF): upstream refusal on record
("training dispatch unsupported for family qwen3_5_hybrid"); no backward for
the hybrid recurrent decoder in any commit. Extends the existing overgo
Qwen3.5-4B typed refusal (hybridtrain closure routes) to the 9B GGUF row.

gemma-4-E4B: structurally blocked upstream ("E4B is not trainable because it
is not natively SERVED... a chain, not a rename"). Overgo DOES serve E4B
natively (capability-e4b-inference), so the chain is unblocked here: the
closure route is porting the live gemma4_unified training semantics (tiled-
head CE, frozen tied embedding, MuonBridge) onto overgo's E4B spine -- a
port campaign, not a smoke.

RxBrain (Hy-Embodied-RxBrain-1.0): upstream refusal on record (same commit
as the hybrid refusal); all deleted rxbrain files were serving routes.
Closure: training graph for the modality-transformer mixture as its own
campaign once the port stabilizes, or training-unsupported declaration.

guard: each family's finding carries a check that fails if a training cell
ever appears for it without a closure route landing first -- silent cells
are impossible. No silent skips: this document is the sweep's typed outcome
for all eight rows.

refused-model: LiveEdit
refused-model: Bonsai-27B-Q1
refused-model: Qwen3.5-9B-Q8

CORRECTIONS 2026-08-19 from deep reference archaeology (the dispatch table
moved three times and was never a reliable trainability index -- TabFM and
Un-0 both trained through generic lanes while declared unsupported):
- SenseNova-U1-8B: a bounded fm_head gradient probe DID exist (the
  supernova_organ_stream / FlowOrganStream lane, 2026-07-03 to 2026-08-04,
  probe only, never full training, gated on vendor 8-GPU readiness;
  recoverable at f4ee9ef34). SenseNova IS neo_chat; the separate Supernova
  was an unrelated dead timeseries lane. Refusal stands with the corrected
  evidence and a concrete recovery source for the probe route.
- gemma-4-E4B: at reference HEAD the label reconciled -- gemma4 maps to
  training: gemma4_unified. E4B is no longer refused: the gemma4_unified
  port covers it (overgo serves E4B natively, dissolving the upstream
  serving blocker), so its marker is withdrawn and it becomes a port
  target beside gemma-4-12B.
- RxBrain: was briefly addressable through the dense-causal fallthrough
  (leftover candidate row, never promoted, reclassified by bc96e7d3a);
  refusal stands with that nuance recorded.
- Wan2.1, Krea-2-Turbo, LiveEdit, Bonsai-27B, Qwen3.5 hybrid: confirmed
  never-trained across all three dispatch-table eras and every generic
  lane's actual model coverage.

WITHDRAWN 2026-08-19: SenseNova-U1-8B refusal closes -- the recovered probe
route landed as a real training cell (fm_head organ, 29.4M params, monotone
descent, derived hyperparameters, internal/routedlm FlowHeadTrainer +
cmd/flow-organ-train-probe), exceeding the reference probe bar of synthetic
slab gradients. Full-pipeline MoT training remains the named promotion gate
inside the cell evidence.

WITHDRAWN 2026-08-19: RxBrain refusal closes -- the family FIRST training
evidence lands as a real latent-bridge cell (llm2vae/vae2llm reconstruction
through the rank-64 bottleneck, 264K params, monotone descent, derived
hyperparameters, LatentBridgeTrainer + flow-organ-train-probe -organ
latent-bridge). Full modality-transformer training remains the named
promotion gate inside the cell evidence.

WITHDRAWN 2026-08-19: Qwen3.5-4B refusal closes -- the hybridtrain lane
(built by the converged lanes as this refusal's named closure route) binds
the real artifact recurrent gated-delta layer and trains it device-resident
with derived hyperparameters; the cell records the first training evidence
for the qwen3_5_hybrid family in either tree. Qwen3.5-9B-Q8 stays refused:
its Q8 serving artifact is master-weight-lane territory (Bonsai-class).

WITHDRAWN 2026-08-19: Krea-2-Turbo refusal closes -- the family FIRST
training evidence lands as a real output-head cell (zero-centered RMSNorm +
AdaLN modulation + velocity linear, 411,712 params, monotone descent,
derived hyperparameters, FinalLayerTrainer + flow-organ-train-probe -organ
denoiser-final). Full 24-block MMDiT flow training remains the named
promotion gate inside the cell evidence.

WITHDRAWN 2026-08-19: Wan2.1-T2V-1.3B refusal closes -- the family FIRST
training evidence lands as a real output-head cell (LayerNorm + AdaLN
modulation + velocity linear, 101,440 params, monotone descent, derived
hyperparameters, FinalLayerTrainer LayerNorm variant + flow-organ-train-
probe -organ video-head). Full video-diffusion training remains the named
promotion gate inside the cell evidence.
