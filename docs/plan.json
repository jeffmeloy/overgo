{
  "campaign": "GUI capability roadmap: conversation-first workbench, local audio input/output and the training loop",
  "doctrine": "Active owner workflow, 2026-09-13: repeat master-to-GUI integration while retaining this plan, reconcile new capabilities and prioritize the greatest GUI benefit, implement eligible plan steps, exercise their operations in a real browser, inspect screenshots at desktop/mobile/keyboard sizes, validate and commit through the existing gate, then repeat. Preserve independent work and source-store ownership. Each interaction change requires real actions and graphical inspection of relevant progress, success, failure and cancellation states; record exact viewport, fixture/native boundary and unresolved physical-device checks. Fix findings before declaring the slice accepted. No timer or external-lane wait blocks independent eligible GUI work.\n\nOwner audio direction, 2026-09-13: plan the complete text-file-to-audio workflow with TTS, recorded or stored voice samples and reusable cloned voice state, using existing local models only; no external speech services or new downloads. The audio worktree is registered as C:/Users/jeffm/overgo_audiocpp (the supplied C:/Users/jeffm/overgo/_audiocpp path is absent), inspected at 3adcb14f. Coordinate native speechsynth/codec/conditioning/ASR/VAD/speaker work with that lane; do not edit its checkout from this GUI task or create a parallel backend. Its audio-generation-expansion synthesis-lifecycle and voice groups remain the native implementation owners. The GUI-native-voice-contract prerequisite accepts their delivered sample-cloning contract; it neither copies nor dispatches the audio lane's backend plan. Only merge/validated contract evidence can enable dependent GUI functions. Prioritize usable preset text-file TTS and repair transcription independently of voice cloning, multilingual qualification, training, CUDA and physical phones. Capability labels distinguish installed bytes, native support, admitted recipes, measured output quality and GUI availability. Original Pocket-TTS encoder tensors are nonzero; newer language copies have zeroed encoders, so qualify the exact checkpoint rather than repeating stale comments or inferring cloning from a model name. Keep the existing flat conversation-first design, on-demand settings and compact selectors. The owner has now authorized implementation and repeated gated commits of these functions.\n\nOwner direction, 2026-09-13: the roadmap now also covers reachability of Train, Model Builder and Evaluations in the documented launch; correctness of the serving contracts the workbench depends on (declared sampler chain, credential-less GET admission, native video routes, Library validation with a projector); workbench consumers for capabilities that have none (inference sessions, agent recovery and streaming tools, model merge, declared speculative decoding); the workbench side of the training loop (conversation dataset preview, adapter training served from the picker, run export, preference training); and the style floor Impeccable's craft floor, audit, typeset, harden and clarify references describe (C:/Users/jeffm/impeccable, read-only concept source), applied after functionality is verified and only as measurements the browser lane computes and ratchets, never as review prose. Training internals, converters, objectives and their evidence stay master-owned under the train-serve-loop reference row; a lane row that consumes them waits for the master step it names. Each new row binds its own browser journey marker and, where it changes a server contract, a file-guarded owning-package test; comprehensive capability signoff requires every marker; local baseline signoff and each capability bundle remain independently executable. Implement missing native prototypes for models already present when required by the owner's preparation workflow; no unrelated optimizer or orchestration framework; derive adapter rank, draft depth and chain defaults from declarations rather than entering them.\n\nCurrent owner direction: merge master into professional_overgo_gui while retaining this plan, then continue with the next GUI capability. Work exclusively in this worktree. Publishing subsequent GUI changes into master requires separate owner authorization.\n\nPreserve the conversation-first design: flat messages, lists and separators; no cards; independently scrolling messages; reachable Send/Stop; one compact model selector with explicit details; mobile navigation drawer; settings on demand; status only for relevant progress, failure or required action. Keep the embedded same-origin client, CSP, authenticated shared transport and existing workbench/state owners. Apply Impeccable guidance to clarity, focus, typography and touch behavior after functionality is verified.\n\nPrevent lost drafts, incorrect cancellation and hidden device capture. Responses, lineage, operations, artifacts, admission and decisions remain server-owned. Browser drafts/settings exclude secrets and captured bytes. Distinguish Stop from disconnect, resume from retry, branch creation from mutation, shared memory admission from exclusive measurement reservations, and recorded attachments from live streams. Reuse existing transactions, evidence handles and abandoned-admission recovery.\n\nEarlier master baseline b10cc6e11002 (superseded by the d279be41b7fb intake below): the image and video runtime lane is merged with its frozen image cases, Wan and LiveEdit validation and the focused media report, checkpoint reuse binds to original execution inputs, census results reuse immutably under live gate admission and publish idempotently, the gate re-ranks by impact, dependent checks are preserved when owner results are reused, ranked test costs are retained from required gate executions, scoped operator stops persist until explicit resumption, plan steps edit through validated typed input, plan work is claimed before an executable dispatch, retained gate costs are queryable through plan history, imported Go types are reused within each census, generic CLI helpers no longer depend on inference or the CUDA driver (the served child types its own device-memory exit), the preparation census is computed once, the gate reports actual per-package verification cost and bounds each active test instead of the suite duration, publication cost is measured against retained gate attempts, instruction evaluation progress persists across resumption, and Qwen4 and Qwen9 native instruction validation is complete; exact artifact identity in model selection, declared chat framing and selected model evidence stay preserved. The master-owned reference rows mirror master's open plan at that revision (publication cost, gate selection, modality verification, regression, benchmark, recovery and simplification); the lane never dispatches them, and a lane row that needs master's work names the master step it waits on. Native NDJSON transcription streaming now has production session/checkpoint owners; browser live input still needs a compatible bounded transport and source-identity contract. CPU and server tests do not prove browser live streaming, physical-phone operation, model understanding or held-out quality.\n\nOnly professional_overgo_gui rows are local work. Retain master-owned historical identities, dependencies and verifier floors as coordination references; reconcile their disposition in master before execution. The 27B benchmark remains operator-launched. The four inherited audio/training surface obligations remain under audio-staged-surface pending owner's reconciliation; no parallel audio backend.\n\nFuture named browser tests are implementation obligations, not present evidence. Assert actual actions, encoded bytes, request/terminal state and cleanup. Preserve existing GUI regression floors. Use synthetic devices and fixtures for focused acceptance, then the full real-model lane for local signoff. Separate physical iOS/Android/Safari/screen-reader evidence from Chromium emulation; missing hardware stays outstanding and does not block implementable local rows. Gate commits through cmd/gate. Bind inherited audio and smoke reference fixtures read-only; keep general data root and browser writes local to this worktree. No arbitrary source-size ceilings or silently skipped supported cases.\n\nGate reference binding: set OVERGO_SMOKE_REFERENCE_STORE=C:/Users/jeffm/overgo/overgodb-store for the existing read-only retained-evidence tests. Keep the general data root, browser output and writable fixtures local. A missing local copy must not trigger model remeasurement or a weakened acceptance check.\n\nMaster intake d279be41b7fb includes 99c142ee: numeric sampler presence, transcription cancellation during shared attachment invalidation/disposal, and video camera-facing selection are implemented upstream. Reuse the actual owning tests and preserve outstanding mode/read-only/page lifecycle checks. GUI transcription now resolves and submits the exact admitted ASR recipe; native multipart/lineage and graphical draft-action acceptance is retained in gui-transcription-selection. Mode/read-only/page lifecycle cleanup remains a separate obligation. Baseline browser operation and screenshot review passed conversation scrolling/navigation/settings at desktop, phone and keyboard widths, and capture preview/retake/attachment at small sizes; fixtures do not establish microphone hardware or ASR model quality.\n\nCurrent master intake 11b13e106ca8 moves declared cheap static checks ahead of expensive gate preparation. An inherited catalog-progress test now uses an explicit virtual duration and failure-safe caller release after a real-clock zero-duration failure; production progress semantics are preserved. Continue the GUI capability loop from this integrated source while retaining the GUI plan and prior acceptance floors.\n\nCurrent master intake 73b4c3ff retains per-package gate selection causes and cost explanation, separates analysis graph export from durable artifact limits, and shares census rewrite test setup while preserving independent audits. The GUI's transcription lifecycle cleanup completed at b802eb62. Graphical speech review after d2067642 found required voice controls clipped despite a clean geometric audit. Track a bounded speech-control/request-recovery slice before full document audio. Keep viewport screenshots and actual visibility/focus/request assertions together; a passing generic layout audit alone does not establish usable interactions.\n\nOwner clarification: attempt to run already present models using matching prototypes, generate missing prototypes from available reference implementations when necessary, and derive recipes dynamically through the existing model/task owners. Generate a candidate recipe before a trial, retain actual output and validation, then admit the verified capability. Missing recipe/prototype status must lead to a preparation attempt or a concrete retained implementation obligation, not an inert unavailable label. A prototype is executable native support, not an invented metadata declaration. Keep the GUI clean: one Prepare model action when needed, meaningful progress/failure/retry, and technical identity/evidence behind Details. This authorizes the required native support for existing local models and supersedes older blanket no-new-family or external-lane waiting interpretations; do not edit other worktrees or download new models. Preserve all task correctness, source identity and validation obligations.",
  "lane": "professional_overgo_gui",
  "census_evidence": "evidence:sha256:4757a6529e4ca6a47ae01f4ad94978209e5c94188ed2f0f04714c9adf36ec7ff",
  "items": [
    {
      "id": "gui-text-to-speech",
      "title": "Turn a text file into a complete downloadable audio file with existing voices",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Turn a text file into a complete downloadable audio file with existing voices",
          "status": "open",
          "verify": "test -f internal/server/speech_document_test.go \u0026\u0026 go test ./internal/server -run '^TestSpeechDocumentLifecycle$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(TextToSpeech|LayoutAudit)$' -require 'text to speech leg'",
          "rationale": "Choose a local .txt file or paste text, inspect/edit it, choose an admitted local Pocket-TTS recipe and exported voice, Generate, Stop/retry, preview and download one complete WAV. Keep the flat conversation/composer layout and advanced sampling in Options. Preset speech must not wait for sample cloning, broad catalog presentation or unrelated audio training receipts. Reuse /artifacts/intake: acceptedMedia and its existing test already cover text/plain, immutable identity, repeat uploads and the declared byte limit. Add document-specific UTF-8/BOM, empty/invalid input, editable-source binding and draft-safe replacement; preserve original file bytes separately from edited/normalized text. Reuse existing generation operations, artifact lineage, authenticated Blob loader, cancellation/retry and lifecycle cleanup; no browser-only job queue, extra downloader or unmanaged output directory. Whole-document execution must preserve ordered source/segment/output identities and progress, reject exhausted generation rather than label a truncated preview complete, and assemble consistent-rate/channel audio under explicit resource and artifact bounds. Preserve intentional bounded legacy requests and existing numerical goldens. Qualify any chunk/context/frame policy through existing native recipe owners against the exact local checkpoint. The read-only Pocket-TTS 2.1.0 reference at C:/Users/jeffm/adaptive_new/models/pocket-tts/pocket-tts declares a 50-token chunk policy and token-derived frame estimate; these are source policies, not established architecture limits. Inspect models/tts_model.py (SHA256 f0afff61e4370e31983eea6b09b8801bcaba77766711adff526a8c0c74b644bf) and default_parameters.py; do not copy warning-only oversized chunks or lose separators/UTF-8 when splitting tokenizer output. The independent completion probe distinguishes the real post-EOS stop from budget exhaustion and preserves bounded previews, but is not document acceptance. Native speech currently publishes JSON PCM before separate WAV encoding, so account for both intermediate and final artifact.MaxContentBytes (64 MiB). OvergoDB external blobs separate bytes from journal envelopes but still use bounded artifact.Content; they do not remove this contract. Accept short and multi-segment local-model fixtures with complete source coverage, EOS per segment, correct voice/source identities, cancellation before/after acceptance, recovery, decoded output and byte-exact named authenticated download. Inspect desktop/narrow/software-keyboard interaction captures. Coordinate native engine/codec changes with the existing audio lane and reuse unchanged evidence by identity; no physical-phone, voice-cloning or quality claim follows from a bounded sample. Do not close this row for text import, a playable preview, controls or a player alone. Recipe composition probe: the existing compiled recipe graph can repeat the native tokenize/generate/decode stages and use RegisterResolvedStage with one static many-input segment port and source-ordered receipt bindings to persist one final WAV directly. An isolated two-segment native graph produced 115200 samples (230444 WAV bytes) and replayed byte-for-byte after reopening the store with one generation attempt per segment. Prefer this existing graph and receipt ownership over a second server loop or browser queue. Bind the exact source document, ordered source spans, exported voice and parent execution recipe in the derived definition and request artifacts; qualify and admit the document recipe through existing prototype/recipe owners before exposure. Native stage completion/recovery remains the prerequisite. Bound intermediate per-segment content and final WAV separately; do not require an aggregate JSON PCM artifact for the entire recording. The probe is not a production activation or browser acceptance. Preserve the complete document lifecycle and graphical verifier above. Current implementation: text import browser actions pass, including long expanded previews at 390x480; the native document graph produced two EOS-complete segments covering all 328 source bytes and an 879404-byte WAV, with exact replay after reopening and no repeated segment generation. These checks do not close this step. Finish pre-execution artifact and working-memory admission, durable live progress, source/voice mismatch and cancellation/retry tests, production prototype/recipe integration, and the authenticated file-to-edit-to-complete-WAV browser lifecycle. Keep the full lifecycle verifier unchanged. Master integration at 1f93e10c passed its merge gate; implement this slice before the next master intake. Durable-progress follow-up: the shared workflow owner now publishes and observes each completed stage before waiting for its siblings. A barrier test proves the callback follows the committed receipt; native document Stop/reopen/retry preserves completed generation and then replays the exact WAV. Codec-derived maximum sample counts reject recordings exceeding the artifact bound before generation. A 100-line imported document remains editable with Read Aloud reachable at 390x480. Working-memory admission and full production/browser acceptance remain outstanding.",
          "depends_on": [
            "gui-speech-controls/do",
            "gui-audio-output/do",
            "gui-speech-cancellation/do",
            "gui-speech-completion-recovery/do",
            "gui-text-to-speech/foundation"
          ]
        }
      ]
    },
    {
      "id": "gui-model-preparation",
      "title": "Prepare local models through prototype matching, candidate recipes and real trial runs",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Turn an installed model into a validated runnable task through the existing preparation owners",
          "status": "open",
          "verify": "test -f internal/server/model_preparation_test.go \u0026\u0026 go test ./internal/server -run '^TestModelPreparationLifecycle$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(ModelPreparation|LayoutAudit)$' -require 'model preparation leg'",
          "rationale": "Owner direction: attempt local models when a matching prototype exists; generate a prototype when one is missing, and derive recipes dynamically. Missing activation is a preparation opportunity, not a permanent refusal. Extend existing libraryintake/modelintake, modelrecipe profile/prototype/candidate compilation/materialization, mediacapability and operation owners. Discover exact weights/configuration/tokenizer/tensor facts, match the task's compiled prototype, derive and publish an identity-bound candidate recipe before execution, run a bounded real trial with the shared resource owner, retain output and independent correctness/quality checks, and activate only the capabilities the evidence establishes. Reuse valid results by exact source/prototype/recipe/code/environment identity; stale bytes or incompatible shapes require re-preparation. A missing prototype must create and attempt a concrete native implementation candidate from the existing local reference code, not merely emit a refusal or a metadata-only prototype. If new operators/codec/profile behavior is needed, compile and test it through the existing candidate/code and gate workflow before trial execution; recording a ModelPrototype JSON document does not implement the missing math. ModelPrototype presently binds an existing compiled architecture profile, libraryintake.Register uses PrepareInferenceCandidate, and DeriveCandidateRecipes currently includes the inference recipe: extend the correct task owner for speech/ASR instead of forcing audio through a chat graph. Respect overgo_audiocpp's current native work and reuse/integrate delivered implementations without editing its checkout or creating a parallel audio backend. GUI offers a compact Prepare model action with progress, Stop, retry and the resulting task selection; keep detailed prototype/recipe/evidence behind Details. Preserve stored source identity and resumable operations through cancellation, process exit, failure and repeated clicks. Verify a matching prototype path with actual local execution, missing-prototype candidate generation followed by its native qualification, failed/stale/unsupported candidates, memory admission and cleanup, and successful capability refresh without losing the input draft. Fixtures prove GUI requests only; retain real trial output and qualification separately. Use only already present models and references; no new model downloads or external provider. Implement working matched-prototype preparation independently while retaining missing-prototype generation as an explicit outstanding obligation, rather than marking this full row complete early. Existing implementation reuse found during GUI startup qualification: cmd/recipe/main.go prepareCapability already resolves the task via mediacapability.Catalog, publishes model/related/component facts and derives source.Define or modelrecipe.CapabilityDefinition; verifyCapability publishes the candidate, compiles candidate execution and runs the native executor before PublishMeasuredVerification. Extract or extend that existing lifecycle for Library operations instead of recreating it beside the CLI. Keep clean source revision, exact input/output evidence, cancellation and retry ownership. Successful execution and measured verification do not alone prove semantic audio quality or complete source coverage; require task-appropriate output checks and explicit qualification before activation. Read-only audio-worktree review at 3adcb14f confirmed existing retained native proofs: Granite CPU transcription baseline a9561f67, Nemotron offline/mel-streaming parity d7c95584, raw-wave/session/HTTP streaming f7085016, production ASR model switching ba6cb1ec, and FireRedVAD offline/streaming detection 5f33d0b3. These commits are already ancestors of the GUI source; do not reimplement their engines or call test-fixture recipes production activations. Exact registered inventories name Granite model 1712f360981e18b30efad1e17611d16066549b820da88c0eb7c7b6f6ad39c098, Nemotron e61f34926cd36a73ee0814b0a01ee4e515d983c204676df0e9bc0652a571869d, and separate FireRedVAD offline/streaming artifacts. The generic verification matrix currently omits these audio proofs; its absence is not evidence that execution is unsupported. mediacapability.Catalog includes speech synthesis but does not include transcription or activity: extend the existing task-specific ASR/activity preparation and serving owners and bridge their qualified artifacts into Library discovery instead of assuming the media CLI can already derive all audio recipes. Preserve source/profile/tokenizer/tensor/license identities and task-specific admission. Voice conditioning from recorded/stored samples and broader generative audio remain open in the audio plan. The inspected worktree is C:/Users/jeffm/overgo_audiocpp; the historical C:/Users/jeffm/overgo/_audiocpp path is absent."
        }
      ]
    },
    {
      "id": "gui-audio-catalog",
      "title": "Expose existing local audio models by task and actual readiness",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Expose existing local audio models by task and actual readiness",
          "status": "open",
          "verify": "test -f internal/server/audio_catalog_test.go \u0026\u0026 go test ./internal/server -run '^TestAudioCatalogReadiness$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(AudioCatalog|LayoutAudit)$' -require 'audio catalog leg'",
          "rationale": "Owner request 2026-09-13: use only models already on this machine, with no external speech API or new model download. Extend the existing capability/Library owners to distinguish files present, registered artifacts, admitted recipes and runnable tasks. Inventory exact source identities across the GUI store and the declared read-only audio reference store; use normal registration/activation and merge ownership to make missing GUI declarations available, never copy model bytes or mutate another worktree. Include Pocket-TTS; Granite Speech 5.0 470M TurboCTC; native Safetensors Nemotron 3.5 ASR; FireRed offline/streaming VAD and separately qualified AED; Sortformer; Gemma E4B and 12B audio input. The audio.cpp source catalog is not installed native support. Keep one compact task/model choice with details on demand and actionable unavailable states; use a flat audio view inside the existing workbench and conversation modes. Browser acceptance must prove task filtering, actual recipe selection, missing/retired artifact refusal and cold-start reachability without requiring an unrelated chat audio projector. Operational source review: the global header continues to show the chat model while generation uses a separate recipe picker. Make the primary task/model choice reflect the active task, retain the chat selection for return and keep the per-attachment ASR selector explicitly labelled when it assists a chat draft. Do not make a speech choice load an unrelated chat runner or imply that installed bytes are admitted. Put inventory, capabilities and evidence behind details; the default surface should take input, perform the selected operation and present its result. Include the single-enabled-task case: composer currently omits its selector with one mode and then reports an empty mode, while chat only calls renderMode for a restored draft. Default to the actual declared enabled task and initialize its controls, including first mount without any saved draft; never route a sole Speech or Transcribe task as chat. Cold-start source review: IdleShell.manifest enables only Library, and cmd/swap idleWorkbench embeds GenerationRefused without the StoreGenerationWorkspace already assembled by cmd/server. API delegation alone does not make native audio runnable. Reuse that generation workspace and admitted recipe catalog, publish truthful model-less task capabilities and enable the conversation surface for those tasks while continuing to refuse unsupported chat. Preserve the bare-shell refusal and existing store-route delegation contracts. The header refresh and its click handler both assume chat serving; update their shared owner so background health refresh cannot overwrite the active task selection or launch an unrelated chat model. New owner direction supersedes passive installed-but-unavailable presentation: offer the gui-model-preparation operation for unprepared local models, match or generate the task prototype, derive candidate recipes and attempt real bounded execution, then refresh selectable capabilities after validation. Keep an already admitted task usable while other models prepare. No prerequisite on preparation should block the independent cold-start and task-selector corrections. The bounded gui-audio-startup prerequisite makes admitted tasks reachable and binds the compact selector without satisfying the broader installed-model inventory, preparation and readiness acceptance here. Read-only audio-worktree review at 3adcb14f confirmed existing retained native proofs: Granite CPU transcription baseline a9561f67, Nemotron offline/mel-streaming parity d7c95584, raw-wave/session/HTTP streaming f7085016, production ASR model switching ba6cb1ec, and FireRedVAD offline/streaming detection 5f33d0b3. These commits are already ancestors of the GUI source; do not reimplement their engines or call test-fixture recipes production activations. Exact registered inventories name Granite model 1712f360981e18b30efad1e17611d16066549b820da88c0eb7c7b6f6ad39c098, Nemotron e61f34926cd36a73ee0814b0a01ee4e515d983c204676df0e9bc0652a571869d, and separate FireRedVAD offline/streaming artifacts. The generic verification matrix currently omits these audio proofs; its absence is not evidence that execution is unsupported. mediacapability.Catalog includes speech synthesis but does not include transcription or activity: extend the existing task-specific ASR/activity preparation and serving owners and bridge their qualified artifacts into Library discovery instead of assuming the media CLI can already derive all audio recipes. Preserve source/profile/tokenizer/tensor/license identities and task-specific admission. Voice conditioning from recorded/stored samples and broader generative audio remain open in the audio plan. The inspected worktree is C:/Users/jeffm/overgo_audiocpp; the historical C:/Users/jeffm/overgo/_audiocpp path is absent. Additional retained native speaker evidence: 5bbc75b8 ports nvidia/diar_sortformer_4spk-v1 revision 9f17b10df44c0a4c8f3c86fbddc9ee2d6ab9ac08, with 156 independent captures and 148 boundary traces; three AMI windows have high diarization error (approximately 0.885, 0.379, 0.504). Expose exact task and qualification rather than implying reliable speaker identification or cloning from numerical parity. Preserve the artifact's CC-BY-NC-4.0 metadata. The broad upstream audio.cpp census is implementation-reference inventory, not verified native Overgo model support. Additional owner inspection: libraryintake.ModelFiles and Register currently admit GGUF inference candidates only; mediacapability.Catalog includes TaskSpeech but not TaskTranscription. Native ASR browser fixtures are not evidence that normal Library registration or launcher preparation already supports those Safetensors audio models. Extend these existing task-specific registration/preparation owners and bind exact declared execution profiles/prototypes and stored verification; do not route ASR through an unrelated GGUF inference recipe or activate models from file presence alone.",
          "depends_on": [
            "gui-audio-startup/do"
          ]
        }
      ]
    },
    {
      "id": "gui-audio-transcription",
      "title": "Bind recorded and stored audio transcription to the selected ASR recipe",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Bind recorded and stored audio transcription to the selected ASR recipe",
          "status": "open",
          "verify": "test -f internal/server/transcription_selection_test.go \u0026\u0026 go test ./internal/server -run '^TestTranscriptionSelectedRecipe$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(TranscriptionDraft|TranscriptionSelection|TranscriptionCleanup|LayoutAudit)$' -require 'transcription selection leg'",
          "rationale": "Prerequisite gui-transcription-selection fixes multipart identity and verifies native request/run lineage and graphical transcript actions. Remaining work is independent audio intake/cold-start reachability, selected stored audio and lifecycle integration; reuse that selector. Resolve model from the selected admitted transcription recipe, not the active chat display name; keep reference identities exact when multiple ASR models are available. Accept recorded, uploaded or stored audio under the ASR declaration independently of chat-projector support. Submit through the authenticated real multipart endpoint, expose progress/Cancel/retry, and offer Insert/Replace/Discard without sending or overwriting the draft automatically. Extend the existing transcription browser journey: its intercepted API response is useful for timing tests but cannot establish production endpoint compatibility. Require at least one real handler/runtime fixture request with the model field, wrong-model/invalid-file refusal, stored-file reuse and late-result suppression. A separate startup prototype browser probe now executes the existing native ASR fixture end to end without a chat model: WAV attachment intake, exact configured recipe, stored transcription and displayed fixture text x. This is wiring evidence, not production ASR quality. Phone review exposed duplicate task/header and per-attachment transcription selectors, a raw audio-artifact input, and both Transcribe and Send in the standalone transcription mode. Collapse standalone ASR into one task/model selector, one file or microphone input and one action; retain the per-attachment helper only when it assists another task's draft. Put stored artifact identity/recent outputs behind Details. Publish the actual ASR model identity and a meaningful source label through its existing workspace capability owner instead of using the recipe hash as the primary model label. Further source/graphical checks: files sent through native artifact intake never populate dataURL, so the current attachment preview does not show their stored audio. Reuse the authenticated artifact resource owner for stored audio preview, with a usable timeline and existing release/pause semantics. The ASR request declares one audio artifact; the chooser must make replacement of that clip explicit and prevent multiple displayed files from implying that all will be transcribed while only one artifact field is submitted. Verify replacement during upload, exact selected bytes, stale callbacks and no unrelated draft loss. An isolated tmp/gui-asr-surface-preview already demonstrates hiding the redundant helper and moving artifact controls/recent outputs to Options while the native fixture transcript still succeeds; it is not promoted or complete acceptance.",
          "depends_on": [
            "gui-audio-catalog/do",
            "gui-composer-cleanup/do",
            "gui-transcription-selection/do",
            "gui-audio-input/do"
          ]
        }
      ]
    },
    {
      "id": "gui-voice-library",
      "title": "Record, upload and reuse named local voice samples",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Record, upload and reuse named local voice samples",
          "status": "open",
          "verify": "test -f internal/server/voice_sample_test.go \u0026\u0026 go test ./internal/server -run '^TestVoiceSampleArtifacts$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(VoiceLibrary|LayoutAudit)$' -require 'voice library leg'",
          "rationale": "Add Record voice sample, Upload sample and Choose stored sample to the existing speech surface. Reuse media_capture, audio intake and artifact browsing; show a flat searchable list with user labels, sample playback, duration/format and source identity, not model-detail cards or raw IDs as the main labels. Keep recorded sample audio separate from exported model voice state; store each through the existing artifact/lineage owners, with model/preprocessor binding for derived state. Validate duration/rate/channels/silence according to the selected voice-encoder declaration; do not apply ASR policy indiscriminately to generated speech. Stored samples remain available for later reuse and unavailable derived voices are explained. Raw bytes and credentials never enter browser draft storage. Remove/reselect/dispose/pagehide/permission failure must release tracks, URLs and requests. No sample may be passed off as a cloned voice before the native conditioning contract accepts it.",
          "depends_on": [
            "gui-audio-catalog/do",
            "gui-composer-cleanup/do"
          ]
        }
      ]
    },
    {
      "id": "gui-native-voice-contract",
      "title": "Accept the audio lane's exact recorded-sample cloning contract",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Bind GUI admission to validated native sample-to-voice-state delivery",
          "status": "open",
          "verify": "test -f internal/server/speech_voice_capability_test.go \u0026\u0026 go test ./internal/server -run '^TestSpeechVoiceCapabilityAdmission$' -count=1",
          "rationale": "GUI integration prerequisite, not a second native implementation plan. Coordinate with audio-generation-expansion/sync-tts-contract-consolidation, tts-contract-consolidation, sync-voice-capability and voice-capability in C:/Users/jeffm/overgo_audiocpp (inspected at 3adcb14f). Reconcile their current disposition and consume the validated backend changes through normal integration; this row does not authorize publishing GUI changes or editing the audio checkout. The audio lane owns encoder/codec/conditioning implementation, independent numerical parity and speaker-quality evidence. Its generic voice election can select multilingual synthesis or conversion, which does not satisfy sample cloning. Through existing recipe/capability/server owners, require an admitted exact local Pocket-TTS checkpoint, usable PCM-to-voice-state encoding, state-to-speech synthesis and retained independent evidence binding source sample, preprocessing, model/configuration/tokenizer and output identity. Acceptance must exercise two distinct recorded/stored samples through the real native contract and reject absent, zeroed or incompatible encoders, stale evidence and mismatched voice state; mocked success cannot qualify the capability. Keep this row open until that delivery exists. Preset text-to-audio, sample-library management and transcription have no dependency on this row. Owner now explicitly authorizes missing-prototype generation and trial runs for local models. Absence of admission must trigger preparation against a matching prototype or a concrete missing-prototype implementation candidate in the existing owner; do not merely wait on an external lane when useful local integration or qualification can proceed. Sample-to-voice numerical and speaker-quality acceptance remains unchanged.",
          "depends_on": [
            "gui-audio-catalog/do"
          ]
        }
      ]
    },
    {
      "id": "gui-voice-cloning",
      "title": "Generate document speech using a recorded or stored voice sample",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Generate document speech using a recorded or stored voice sample",
          "status": "open",
          "verify": "test -f internal/server/speech_voice_reference_test.go \u0026\u0026 go test ./internal/server -run '^TestSpeechVoiceReferenceBinding$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(VoiceCloning|LayoutAudit)$' -require 'voice cloning leg'",
          "rationale": "Connect the text-file TTS flow to the native sample-to-voice-state capability delivered by the audio worktree, retaining user choice between preset/exported voices and a recorded/stored sample. Existing-model evidence from 2026-09-13: C:/Users/jeffm/adaptive_new/models/pocket-tts/tts_b6369a24.safetensors has 42 nonzero mimi.encoder tensors; all 12 inspected language/model.safetensors copies have those 42 tensors zeroed. The speechsynth comments claiming every local encoder is amputated are stale. Nonzero tensors establish a candidate, not working cloning: require exact checkpoint/config/tokenizer/encoder/conditioning admission and an independent sample-to-state-to-PCM oracle before enabling Generate with a sample. Coordinate with audio-generation-expansion/sync-voice-capability and voice-capability in C:/Users/jeffm/overgo_audiocpp. Their current generic voice election may choose multilingual synthesis or conversion; that alone cannot close this sample-cloning row. Persist reusable derived voice state with sample/model/preprocessing lineage; test two distinct samples, stored-state reload, wrong-model/zeroed-encoder rejection, state invalidation, Stop/retry and downloadable output. GUI fixtures validate requests and interaction, while the audio lane owns numerical parity and speaker-similarity/quality evidence. No new model family, provider API or parallel inference service.",
          "depends_on": [
            "gui-text-to-speech/do",
            "gui-voice-library/do",
            "gui-native-voice-contract/do"
          ]
        }
      ]
    },
    {
      "id": "gui-speech-languages",
      "title": "Expose qualified existing Pocket-TTS language checkpoints and their voices",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Expose qualified existing Pocket-TTS language checkpoints and their voices",
          "status": "open",
          "verify": "test -f internal/server/speech_language_test.go \u0026\u0026 go test ./internal/server -run '^TestSpeechLanguageVoiceBinding$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(SpeechLanguages|LayoutAudit)$' -require 'speech languages leg'",
          "rationale": "Qualify the already present Pocket-TTS English, French, German, Italian, Portuguese and Spanish checkpoints through existing artifact/recipe admission before offering them. There are 12 local language directories, including dated English and 24-layer variants, and 26 exported voice files per language directory; the root voice collections offer 21 distinct names. File presence or matching names does not make weights/configuration/tokenizer/voice-state combinations interchangeable. Coordinate native qualification with the audio worktree's existing voice-election group and retain exact per-variant evidence; decline incompatible layer counts or missing declarations explicitly. Show only admitted language/voice combinations in one compact selector, preserve source text on changes, and disable recorded-sample cloning for the zeroed-encoder variants unless another explicitly admitted compatible path exists. Acceptance verifies the served selection and full text-to-audio/download path for every exposed variant; unsupported variants remain listed only in on-demand details. Do not block working English preset TTS or sample cloning on all languages.",
          "depends_on": [
            "gui-text-to-speech/do"
          ]
        }
      ]
    },
    {
      "id": "gui-audio-analysis",
      "title": "Make existing speech activity, speaker, alignment and audio-understanding functions usable",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Make existing speech activity, speaker, alignment and audio-understanding functions usable",
          "status": "open",
          "verify": "test -f internal/server/audio_analysis_test.go \u0026\u0026 go test ./internal/server -run '^TestAudioAnalysisTaskBinding$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(AudioAnalysis|LayoutAudit)$' -require 'audio analysis leg'",
          "rationale": "Use recorded/uploaded/stored audio across task-specific views in the existing workbench: FireRed VAD speech regions; Sortformer speaker turns; validated alignment/word timestamps only when the corresponding native contract exists; Gemma audio questions with text answers. Keep transcript, speech activity, speaker diarization and audio-event detection separate. FireRed AED weights/ONNX exports do not establish a native event detector; list the missing native contract as a refusal instead of inventing output. Reuse the audio worktree's admitted operation/session/output owners and gemma-audio-proof/serving-recovery evidence; E4B acceptance must not imply 12B quality or block it unnecessarily. Render a seekable audio player with flat timestamped transcript/turn rows and speaker labels; keep technical details on demand. Export text and structured records; offer subtitle export only for validated alignment, never reinterpret VAD boundaries or decoder frame steps as word timestamps. Test output/source binding, playback seeking, overlap, cancellation/reload, unsupported tasks and actual selected model. Missing one optional task must not block supported transcription, VAD or TTS.",
          "depends_on": [
            "gui-audio-catalog/do",
            "gui-audio-transcription/do"
          ]
        }
      ]
    },
    {
      "id": "gui-sampler-presence",
      "title": "Resolve sampler defaults from parameter presence, not zero values",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Resolve sampler defaults from parameter presence, not zero values",
          "status": "open",
          "verify": "go test ./internal/server -run '^(TestRequestWithoutSamplersUsesDeclaredChain|TestRequestSamplingPresence)$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(SamplingSettings|LayoutAudit)$' -require 'sampling settings leg'",
          "rationale": "Owner review 2026-09-13 (P2): newSampler in internal/server/server.go fills TypicalP, TopNSigma, XTCThreshold, RepeatPenalty, DryBase, DryAllowedLength, DryPenaltyLastN, MirostatTau and MirostatEta with cmp.Or, which cannot distinguish an omitted parameter from an intentional zero: dry_penalty_last_n: 0 means disable DRY and is replaced by the declared default, while the remaining parameters never receive a declared default at all. Make request sampling fields presence-aware (decoded presence, not zero sentinels) and resolve every parameter through one default-resolution path shared by the chat, completion and responses bodies, so an omitted value takes the declared default, an explicit zero stays zero, and a nonzero value is kept. Keep the resolved values in the response sampling record. Acceptance: the owning-package test covers omitted, explicit-zero and nonzero values independently for each defaulted parameter; the sampling settings journey sends an explicit zero and reads it back unchanged in the response details. Master 99c142ee now implements pointer-presence resolution with the named owning regression. Reconcile browser explicit-zero coverage against the merged implementation; do not implement a second default owner."
        }
      ]
    },
    {
      "id": "gui-capture-facing",
      "title": "Apply the front or rear camera choice to video capture",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Apply the front or rear camera choice to video capture",
          "status": "open",
          "verify": "go run ./cmd/webui-lane -run '^TestWebUIBrowser(CaptureFacing|LayoutAudit)$' -require 'capture facing leg'",
          "rationale": "Owner review 2026-09-13 (P2): the capture device selector offers Front camera and Rear camera for every camera kind, but constraints() in media_capture.js applies facingMode only to photographs; a video capture with Rear camera selected requests true and the browser picks its default camera, while explicit device ids work for both. Build one constraint from the selection for image and video capture alike (facingMode ideal for the two named choices, exact deviceId for a listed device) and keep the recorder's MIME negotiation unchanged. Acceptance: the journey records a fake getUserMedia and asserts the video constraint carries the selected facing mode and the deviceId for a listed camera; the video capture journey still passes. Master 99c142ee now applies facingMode to video and verifies rear-camera requests in TestWebUIBrowserVideoCapture. Retain physical-phone checks separately; reconcile this row against the merged proof."
        }
      ]
    },
    {
      "id": "gui-video-source-edit",
      "title": "Edit a captured or attached clip through the reference-video recipe's source port",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Edit a captured or attached clip through the reference-video recipe's source port",
          "status": "open",
          "verify": "test -f internal/server/video_source_route_test.go \u0026\u0026 go test ./internal/server -run '^TestVideoRouteSourceDocument$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(VideoSourceEdit|LayoutAudit)$' -require 'video source edit leg'",
          "rationale": "Master b10cc6e11002 validates reference-guided video editing as a video-gen recipe whose reference-video stages read a typed source-video document (internal/latentvideo/production.go: ReferenceEditInputContent binds the condition and source ports; SourceVideo is planar float pixels with declared channels, frames, height and width; CompileSourceCodecBoundary refuses a shape the causal VAE cannot encode). The native video route (internal/server/native_media_protocols.go) carries only the prompt, so the workbench cannot reach that consumer, while the composer already records bounded camera clips and attaches video files. Close the loop: derive the accepted source shape from the served recipe's declared codec profile and expose it in the workspace capability document; sample the attached or recorded clip into that shape in the browser (the photo capture already draws video frames to a canvas; no server-side container decoder exists, and MP4/WebM decoding is not added); upload the typed source-video document with the prompt through the native video route; route it to the recipe's source port; refuse a clip whose frame count or extent exceeds the declared shape before upload, naming the bound. Acceptance: the journey attaches a fixture clip, runs the edit on the reference-video fixture recipe, and reads an output whose frame count matches the request; an oversized clip is refused with the declared bound; a text-only video-gen recipe shows no source control; the owning-package test pins the route contract with the source document.",
          "depends_on": [
            "gui-capture-facing/do"
          ]
        }
      ]
    },
    {
      "id": "gui-live-media",
      "title": "Stream microphone input through native transcription sessions with bounded queues",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Stream microphone input through native transcription sessions with bounded queues",
          "status": "open",
          "verify": "go run ./cmd/webui-lane -run '^TestWebUIBrowser(LiveMedia|LayoutAudit)$' -require 'live media leg'",
          "rationale": "Reuse native session/checkpoint ownership. First prove a browser-compatible request/response adapter: Fetch half-duplex cannot expose live replies during an unfinished upload. Derive PCM format, resampling and chunk bounds from server capabilities. Bind valid source identity, backpressure, disconnect/Stop cleanup and ordered partial/final transcripts. Require synthetic chunk-to-partial-output acceptance before microphone UX. Camera streaming/frame sampling needs a separately declared compatible consumer; no unbounded upload or model-understanding claim. Use the selected admitted native Nemotron/Granite session contract, not whichever chat model happens to serve. Provide explicit Start/Stop, microphone/source selection, visible recording state, ordered partial/final text and a choice to insert final text into the draft; reconnect/resume only with exact source/checkpoint identity. Keep recorded-file transcription available when duplex transport, permissions or streaming admission is unavailable. Browser tests must exercise actual incremental request/response delivery, bounded queues and aborted capture across navigation/backgrounding; no external API, prefix re-upload or unbounded retained PCM.",
          "depends_on": [
            "gui-transcription-draft/do",
            "gui-audio-catalog/do",
            "gui-audio-transcription/do"
          ]
        }
      ]
    },
    {
      "id": "gui-agent-sessions",
      "title": "Resume agent sessions after interruption and stream tool output in the thread",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Resume agent sessions after interruption and stream tool output in the thread",
          "status": "open",
          "verify": "test -f internal/server/agent_session_recovery_test.go \u0026\u0026 go test ./internal/server -run '^TestAgentSessionRecoveryAndStreaming$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(AgentRecovery|LayoutAudit)$' -require 'agent recovery leg'",
          "rationale": "Agent recovery and checkpoints (internal/agentloop/recovery.go) and the streaming NDJSON tool transport have no production caller; operations are in-memory and evictable (internal/operation/runtime.go:526-543), so a server restart loses the session the README promises to recover. Persist agent sessions and their receipts through the existing interaction store, restore them on startup, and carry tool output through the duplex transport into the thread as it arrives. Acceptance: a session interrupted mid-tool resumes after restart without duplicating the tool effect; streamed tool output appears incrementally; approvals survive the restart."
        }
      ]
    },
    {
      "id": "gui-chat-sessions",
      "title": "Continue a stopped or resumed conversation exactly through inference sessions",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Continue a stopped or resumed conversation exactly through inference sessions",
          "status": "open",
          "verify": "test -f internal/server/response_session_test.go \u0026\u0026 go test ./internal/server -run '^TestResponsesContinueSavedSession$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(SessionContinuation|LayoutAudit)$' -require 'session continuation leg'",
          "rationale": "Resumable inference sessions (StartSession, ContinueSession, SaveSession, LoadSession) and KV cache serialization have no caller outside internal/inference; Chat stop and resume replay stored text and re-prefill. Map previous_response_id on /v1/responses to a saved session bound to the model fingerprint and sampler state, persist it through the existing session codec, and release it with the conversation. Acceptance: continuing after Stop produces the same tokens as an uninterrupted run under greedy settings with no re-prefill; a session from another model or sampler configuration is refused with a reason; the journey stops, continues and reads the resumed reply."
        }
      ]
    },
    {
      "id": "gui-model-merge",
      "title": "Merge registered models from Model Builder into a new artifact with lineage",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Merge registered models from Model Builder into a new artifact with lineage",
          "status": "open",
          "verify": "test -f internal/server/model_builder_merge_test.go \u0026\u0026 go test ./internal/server -run '^TestModelBuilderMergeRun$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(ModelMerge|LayoutAudit)$' -require 'model merge leg'",
          "rationale": "The modelmerge operators have no production caller, and the Model Builder workspace already runs scratch construction through /model-builder/run. Add merge and task-arithmetic between registered models of one base as a Model Builder run producing a new artifact with both parents in its lineage, registered through the existing intake and eligible for Library validation. Acceptance: a merge of two small registered models publishes an artifact whose lineage names both parents and whose weights equal the operator's definition on a fixture; the journey selects two models, runs the merge and finds the result in the Library.",
          "depends_on": [
            "gui-launch-reachability/do"
          ]
        }
      ]
    },
    {
      "id": "gui-speculative-decoding",
      "title": "Offer speculative decoding only when the served recipe declares a draft head",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Offer speculative decoding only when the served recipe declares a draft head",
          "status": "open",
          "verify": "test -f internal/server/speculative_option_test.go \u0026\u0026 go test ./internal/server -run '^TestSpeculativeDecodingDeclaredOption$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(SpeculativeDecoding|LayoutAudit)$' -require 'speculative decoding leg'",
          "rationale": "NextN/MTP speculative decoding runs only through benchmark -speculative; the host draft and verify sessions have no caller. Expose it as a generation option in Chat and Generate that appears only when the served recipe's capability document declares a draft head, carry it through the request, and report accepted draft length in the response usage. The draft depth is derived from the declaration rather than the 27B-tuned literal. Acceptance: the option is absent for a model without a draft head, present and effective for one that declares it, and greedy output is unchanged with it on."
        }
      ]
    },
    {
      "id": "gui-style-design-document",
      "title": "Publish the workbench token and consumer inventory as structured data and refuse hard-coded style",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Publish the workbench token and consumer inventory as structured data and refuse hard-coded style",
          "status": "open",
          "verify": "test -f internal/webuilane/design_document_test.go \u0026\u0026 go test ./internal/webuilane -run '^TestDesignDocumentMatchesStylesheet$' -count=1",
          "rationale": "Owner direction 2026-09-13: no further generated Markdown contracts. The workbench's single design record is a structured inventory, not a rendered document: the existing webui-lane report owner reads the :root token declarations in style.css (both themes, the spacing scale, the type scale) and every consumer of each token across style.css and the panel modules, and publishes docs/gui/design_tokens.json with the token, scale and component inventories plus per-token consumer counts, checked fresh the way the other generated JSON documents are. A human rendering is optional and derived from that document on demand, never committed. The same inventory is the theming ratchet: a colour, size or spacing literal outside :root in style.css, or an inline style attribute in the panel modules, is a finding whose count only falls. Acceptance: the published inventory equals the committed one; every token has at least one consumer or is listed as unused; the literal census is zero and a seeded literal fails."
        }
      ]
    },
    {
      "id": "gui-style-craft-floor",
      "title": "Measure the Impeccable craft floor in the browser lane at every state, viewport and scheme",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Measure the Impeccable craft floor in the browser lane at every state, viewport and scheme",
          "status": "open",
          "verify": "test -f internal/webuilane/craft_floor_test.go \u0026\u0026 go test ./internal/webuilane -run '^TestCraftFloorTokenContrast$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(CraftFloor|LayoutAudit)$' -require 'craft floor leg'",
          "rationale": "The layout audit that CaptureStates runs at desktop and phone under dark and light already measures horizontal overflow, overlapping surfaces, clipped text and AA contrast. Extend it with the rest of the floor Impeccable verifies on the built result, as computed measurements rather than review notes: no text below the smallest type token (the .sub label renders at 8.5px today); interactive targets at or above 44 CSS pixels on the phone viewport; a visible focus indicator on every focusable element; a heading hierarchy without skipped levels; easing functions without overshoot; placeholder text at body contrast; and, under emulated prefers-reduced-motion, state changes that still complete, which the current global 0.01ms transition kill does not guarantee for streamed status. Findings are a ratchet baseline the way the composer budget is: the count only falls. Acceptance: the token contrast test computes every text-on-surface pair for both themes from style.css; the browser journey audits every captured state and its baseline holds; a seeded 40px target and a seeded 9px label fail."
        }
      ]
    },
    {
      "id": "gui-style-typeset",
      "title": "Set one role scale and reading measure for an operate surface and remove the eyebrow label",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Set one role scale and reading measure for an operate surface and remove the eyebrow label",
          "status": "open",
          "verify": "go run ./cmd/webui-lane -run '^TestWebUIBrowser(Typeset|LayoutAudit)$' -require 'typeset leg'",
          "rationale": "Impeccable's typeset guidance for an operate-and-read surface is one well-tuned family with a fixed role scale; the craft floor sets the reading measure at 65 to 75 characters and bans the kicker or eyebrow above a heading outright. The workbench keeps its system family by that rule, recorded as the decision rather than a default, and fixes what the rule finds: the conversation and composer share a 720px measure, which at the 14px body is beyond 75ch; the .sub pill is an uppercase letter-spaced eyebrow at 8.5px; role sizes are pinned in pixels with no fluid step between phone and desktop. Derive the measure from the body size, give each type token one role, and delete the eyebrow. Acceptance: the journey measures the reading width in characters at both viewports, finds no uppercase tracked label above a heading, and finds every text node on a token size."
        }
      ]
    },
    {
      "id": "gui-style-hardened-states",
      "title": "Prove every workspace under extreme input, empty, loading and failure states with copy that names the recovery",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Prove every workspace under extreme input, empty, loading and failure states with copy that names the recovery",
          "status": "open",
          "verify": "go run ./cmd/webui-lane -run '^TestWebUIBrowser(HardenedStates|LayoutAudit)$' -require 'hardened states leg'",
          "rationale": "Impeccable's harden, clarify and onboard checks are journeys, not styling: names of 100+ characters, emoji, CJK and RTL text, a thousand-row list, ten rapid submits, forced request failures, and every workspace with no data. Each state must render without clipped or overflowing text, keep Send and Stop reachable, and show copy that states what failed and how to recover or what to do first; an empty workspace names what will appear there and the action that creates it. Reuse the existing conversation, attachment and background-work journeys for the inputs and add the extreme-input and empty-state passes. Acceptance: the journey feeds each input class to each workspace at both viewports and both schemes, the layout audit reports no overflow or clipping, and every failure and empty state carries an action."
        }
      ]
    },
    {
      "id": "gui-dataset-preview",
      "title": "Preview conversation datasets as the model will train on them",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Preview conversation datasets as the model will train on them",
          "status": "open",
          "verify": "go run ./cmd/webui-lane -run '^TestWebUIBrowser(DatasetPreview|LayoutAudit)$' -require 'dataset preview leg'",
          "rationale": "Once master's conversation dataset processor renders examples through the served model's chat template with response-only loss masking, the Datasets tab previews an example as rendered tokens with the masked and trained spans distinguished, using the existing dataset preview route. Acceptance: the journey opens a conversation dataset, reads a rendered example, and sees which spans train; a dataset the processor refuses shows the refusal reason.",
          "depends_on": [
            "train-serve-loop/chat-sft-data"
          ]
        }
      ]
    },
    {
      "id": "gui-adapter-training",
      "title": "Train an adapter from the Train tab and serve it from the model picker",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Train an adapter from the Train tab and serve it from the model picker",
          "status": "open",
          "verify": "test -f internal/server/training_adapter_run_test.go \u0026\u0026 go test ./internal/server -run '^TestTrainingAdapterRunPublishesServableAdapter$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(AdapterTraining|LayoutAudit)$' -require 'adapter training leg'",
          "rationale": "Once master's adapter trainer emits a GGUF LoRA adapter, the Train tab offers the adapter recipe kind for a registered base and dataset, the run publishes the adapter through /lora-adapters with its base and dataset lineage, and the model picker lists it beside its base. Rank and scale are derived from the declared parameter budget, not entered. Acceptance: a run on the small fixture base produces an adapter the served model loads; the journey starts the run, follows progress in Runs, selects the adapter and receives a reply with it applied.",
          "depends_on": [
            "train-serve-loop/adapter-gguf",
            "gui-launch-reachability/do"
          ]
        }
      ]
    },
    {
      "id": "gui-run-export",
      "title": "Export a finished training run to a servable GGUF from the Runs tab",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Export a finished training run to a servable GGUF from the Runs tab",
          "status": "open",
          "verify": "test -f internal/server/run_export_test.go \u0026\u0026 go test ./internal/server -run '^TestRunExportPublishesServableModel$' -count=1 \u0026\u0026 go run ./cmd/webui-lane -run '^TestWebUIBrowser(RunExport|LayoutAudit)$' -require 'run export leg'",
          "rationale": "Once master's checkpoint-to-GGUF path exists, the Runs tab exports a finished dense run through the existing converter and quantizer into a registered model with the run in its lineage, ready for Library validation. Acceptance: the exported model validates and serves; the journey exports a fixture run and selects the result.",
          "depends_on": [
            "train-serve-loop/checkpoint-export",
            "gui-launch-reachability/do"
          ]
        }
      ]
    },
    {
      "id": "gui-preference-training",
      "title": "Start DPO and GRPO runs from the Train tab on declared preference and rollout datasets",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Start DPO and GRPO runs from the Train tab on declared preference and rollout datasets",
          "status": "open",
          "verify": "go run ./cmd/webui-lane -run '^TestWebUIBrowser(PreferenceTraining|LayoutAudit)$' -require 'preference training leg'",
          "rationale": "Once master records device evidence for the DPO and GRPO objectives, the Train tab offers them for datasets whose declared shape matches (preference pairs, rollout groups with an evaluator) and refuses others with the reason. Acceptance: the journey starts a fixture DPO run, follows it in Runs, and reads the terminal outcome; a mismatched dataset is refused before submission.",
          "depends_on": [
            "train-serve-loop/preference-device-evidence",
            "gui-launch-reachability/do"
          ]
        }
      ]
    },
    {
      "id": "gui-audio-acceptance",
      "title": "Accept audio workflow bundles with real local model evidence",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Accept audio workflow bundles with real local model evidence",
          "status": "open",
          "verify": "go run ./cmd/webui-lane -run '^TestWebUIBrowser(MicrophoneFormat|NativeASRMicrophone|AudioCatalog|AudioStartup|AudioOutput|SpeechControls|TextToSpeech|VoiceLibrary|VoiceCloning|SpeechLanguages|AudioAnalysis|TranscriptionDraft|TranscriptionSelection|TranscriptionCleanup|LiveMedia|LayoutAudit)$' -require 'audio catalog leg' -require 'text to speech leg' -require 'voice library leg' -require 'voice cloning leg' -require 'speech languages leg' -require 'audio analysis leg' -require 'transcription selection leg' -require 'transcription cleanup leg' -require 'live media leg' -require 'speech controls leg' -require 'audio startup leg' -require 'audio output leg' -require 'microphone format leg' -require 'native ASR microphone leg'",
          "rationale": "Integrate the audio bundles through the existing webui-lane and retained evidence owners, not a new audio GUI gate. Each landed bundle has its own acceptance immediately; this final audio row requires every advertised journey and a real endpoint/model path, with actual audio bytes and selected voice/source identities. Reuse exact unchanged backend parity/quality evidence from the audio lane; do not rerun all training/GPU campaigns for a browser-only edit or count intercepted success as end-to-end inference. Keep desktop/narrow/software-keyboard layouts, focus, labels, progress, reachable Stop, playback/download and no lingering capture as explicit checks. Native NDJSON tests alone do not establish browser streaming. Report supported and unavailable models separately, and keep physical iOS/Android evidence with gui-device-signoff.",
          "depends_on": [
            "gui-text-to-speech/do",
            "gui-voice-cloning/do",
            "gui-speech-languages/do",
            "gui-audio-analysis/do",
            "gui-live-media/do",
            "gui-speech-controls/do",
            "gui-audio-output/do",
            "gui-audio-startup/do",
            "gui-audio-input/do",
            "gui-speech-cancellation/do",
            "gui-microphone-format/do",
            "gui-conversation-resize/do"
          ]
        }
      ]
    },
    {
      "id": "gui-local-signoff",
      "title": "Accept implemented GUI capability bundles incrementally and reuse them in local signoff",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Accept implemented GUI capability bundles incrementally and reuse them in local signoff",
          "status": "open",
          "verify": "go run ./cmd/webui-lane -run '^TestWebUIBrowser.*$' -require 'media capture leg' -require 'conversation control leg' -require 'draft lifecycle leg' -require 'mobile interaction leg' -require 'conversation navigation leg' -require 'conversation actions leg' -require 'attachment workflow leg' -require 'background work leg' -require 'transcription draft leg' -require 'video capture leg' -require 'workspace reachability leg' -require 'sampling settings leg' -require 'workspace refusal leg' -require 'library validation leg'",
          "rationale": "Owner direction 2026-09-13: accept capability bundles incrementally instead of holding local acceptance until live media, session recovery, speculative decoding, training and export, and styling all land. Each landed row's journey markers form a bundle the browser lane records as accepted evidence keyed by the inference surface digest and the webui file digests; this row runs the complete browser/model lane once per bundle boundary, retains independently accepted bundles, and reuses their recorded results when nothing in their surface changed, so later bundles add cost only for what they touch. Preserve every existing conversation, draft, mobile, navigation, actions, attachment and background journey; require the capture, transcription, video and sampling markers now and each later bundle's markers as it lands. Inspect desktop and narrow/keyboard light/dark captures after behavior passes; report actual browsers and models plus unsupported cases. Comprehensive signoff stays at the end under gui-capability-signoff, which consumes the accepted bundles rather than re-running them. This local acceptance cannot close physical-device signoff. Plan correction for executable incremental acceptance: this row now binds the landed baseline; its previous future-capability marker floor and prerequisite chain are retained in gui-capability-signoff. New audio capabilities each carry independent acceptance and culminate in gui-audio-acceptance, so preset TTS need not wait for cloning, all languages or phone hardware. Baseline evidence does not close known transcription defects.",
          "depends_on": [
            "gui-media-capture/do",
            "gui-gpu-admission/do",
            "gui-transcription-draft/do",
            "gui-video-capture/do",
            "gui-launch-reachability/do",
            "gui-sampling-contract/do",
            "gui-credentialless-get/do",
            "gui-refused-workspaces/do",
            "gui-library-validation/do",
            "gui-video-routes/do"
          ]
        }
      ]
    },
    {
      "id": "gui-capability-signoff",
      "title": "Accept the complete conversation workflow on the merged GUI worktree",
      "owner": "professional_overgo_gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Accept the complete conversation workflow on the merged GUI worktree",
          "status": "open",
          "verify": "OVERGO_DATA_ROOT=C:/Users/jeffm/professional_overgo_gui OVERGO_GUI_MOBILE_EVIDENCE=docs/gui/mobile-device-evidence.json go run ./cmd/webui-lane -run '^(TestWebUIBrowser|TestGUIMobileDeviceEvidence)' -require 'conversation control leg' -require 'draft lifecycle leg' -require 'mobile interaction leg' -require 'conversation navigation leg' -require 'conversation actions leg' -require 'attachment workflow leg' -require 'background work leg' -require 'physical mobile evidence leg' -require 'media capture leg' -require 'transcription draft leg' -require 'video capture leg' -require 'live media leg' -require 'workspace reachability leg' -require 'sampling settings leg' -require 'workspace refusal leg' -require 'library validation leg' -require 'agent recovery leg' -require 'session continuation leg' -require 'model merge leg' -require 'speculative decoding leg' -require 'craft floor leg' -require 'typeset leg' -require 'hardened states leg' -require 'dataset preview leg' -require 'adapter training leg' -require 'run export leg' -require 'preference training leg' -require 'audio catalog leg' -require 'text to speech leg' -require 'voice library leg' -require 'voice cloning leg' -require 'speech languages leg' -require 'audio analysis leg' -require 'transcription selection leg' -require 'transcription cleanup leg' -require 'speech controls leg' -require 'model preparation leg' -require 'audio startup leg' -require 'audio output leg' -require 'microphone format leg' -require 'native ASR microphone leg'",
          "rationale": "Each preceding browser test must emit its named journey marker after asserting behavior. Run the full real-model lane once for this acceptance, inspect desktop/phone light/dark captures and verify the real-device evidence. Include cold start, model/key setup, multi-turn chat, stop/disconnect/recovery, reload, history/branches, attachment reuse and Library/background operations. Keep credentials and model metadata out of the primary conversation. Report tested models, browsers, devices and unsupported/missing modality cases explicitly; missing supported evidence cannot count as complete. Retain all former gui-local-signoff end-state markers/dependencies here and require the complete audio bundle as well. Each implemented bundle may be accepted before all future capabilities land; unchanged evidence is reusable only through existing identity-aware owners. No physical-phone, voice-cloning, per-language quality or live-streaming claim follows from synthetic GUI success.",
          "depends_on": [
            "webui-polish-tabs/do",
            "gui-draft-lifecycle/do",
            "gui-redesign-acceptance/do",
            "gui-conversation-navigation/do",
            "gui-conversation-actions/do",
            "gui-attachment-workflow/do",
            "gui-background-work/do",
            "gui-device-signoff/do",
            "gui-local-signoff/do",
            "gui-media-capture/do",
            "gui-gpu-admission/do",
            "gui-transcription-draft/do",
            "gui-video-capture/do",
            "gui-live-media/do",
            "gui-launch-reachability/do",
            "gui-sampling-contract/do",
            "gui-credentialless-get/do",
            "gui-refused-workspaces/do",
            "gui-library-validation/do",
            "gui-video-routes/do",
            "gui-agent-sessions/do",
            "gui-chat-sessions/do",
            "gui-model-merge/do",
            "gui-speculative-decoding/do",
            "gui-style-design-document/do",
            "gui-style-craft-floor/do",
            "gui-style-typeset/do",
            "gui-style-hardened-states/do",
            "gui-dataset-preview/do",
            "gui-adapter-training/do",
            "gui-run-export/do",
            "gui-preference-training/do",
            "gui-audio-acceptance/do",
            "gui-speech-controls/do",
            "gui-model-preparation/do",
            "gui-audio-output/do",
            "gui-audio-startup/do",
            "gui-audio-input/do",
            "gui-speech-cancellation/do",
            "gui-microphone-format/do",
            "gui-conversation-resize/do"
          ]
        }
      ]
    },
    {
      "id": "gui-device-signoff",
      "title": "Record real iOS/Android and assistive-technology acceptance",
      "owner": "operator",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Record real iOS/Android and assistive-technology acceptance",
          "status": "open",
          "verify": "OVERGO_GUI_MOBILE_EVIDENCE=docs/gui/mobile-device-evidence.json go test ./internal/server -run '^TestGUIMobileDeviceEvidence$' -count=1",
          "rationale": "With actual hardware/operator participation, exercise iOS Safari and Android Chrome: software keyboard, orientation, safe areas, zoom, attachments, long replies, stop/recovery, drawer/Settings focus, and screen-reader announcements. Record browser/device versions, exact cases, observations and captures in the evidence file consumed by the validator implemented under gui-redesign-acceptance. Missing hardware or failures remain outstanding; Chromium emulation is not physical-device evidence. This step grants no permission to merge into master. Include real microphone/camera permissions, interruption/backgrounding, device switching, recorded audio/photo/video preview and supported transcription. Confirm HTTPS/LAN access on the phone without weakening authentication or CSP. Include text-file import, recorded/stored voice selection, long-document progress, microphone Stop/background cleanup, speaker/transcript seeking, generated WAV playback and download/share on physical iOS and Android. Require live partial transcripts only for an admitted browser transport; report each unsupported case explicitly.",
          "depends_on": [
            "gui-redesign-acceptance/do"
          ]
        }
      ]
    },
    {
      "id": "integrate-to-master",
      "title": "Publish subsequent accepted GUI capabilities only after separate owner approval",
      "owner": "operator",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Publish subsequent accepted GUI capabilities only after separate owner approval",
          "status": "open",
          "verify": "git merge-base --is-ancestor HEAD master \u0026\u0026 go build ./...",
          "rationale": "Master already contains the reviewed GUI baseline through 0219c646. This row now concerns subsequent GUI capability changes. Current authorization is master into this worktree plus local implementation. A future operator-approved publication must preserve master's plan, pin the approved GUI revision, verify that master contains it, then synchronize this lane. Bind the exact approved revision and merge checks before execution.",
          "depends_on": [
            "gui-capability-signoff/do"
          ]
        }
      ]
    },
    {
      "id": "audio-staged-surface",
      "title": "Coordinate inherited native audio and CPU adapter training boundaries with master",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Coordinate inherited native audio and CPU adapter training boundaries with master",
          "status": "open",
          "verify": "test 0 -eq $(grep -Ec '\"name\": \"(OpenAudioStream|NewTextTransform|PublishInputProjection|RestoreInputProjection)\"' docs/staged_surface.json)",
          "rationale": "Master now supplies production consumers for the four retained surfaces, including native transcription streaming and CPU projection checkpoint restore. Master must reconcile this historical row against current staged-surface evidence; preserve its identity and verifier floor here. Production integration does not establish held-out quality, browser live streaming or a GUI training workflow."
        }
      ]
    },
    {
      "id": "train-serve-loop",
      "title": "Master reference: connect training output to serving and evaluation",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "chat-sft-data",
          "title": "Render conversation datasets through the chat template with response-only loss masking",
          "status": "open",
          "verify": "test -f internal/trainingdata/conversation_test.go \u0026\u0026 go test ./internal/trainingdata -run '^TestConversationProcessorMasksResponses$' -count=1",
          "rationale": "Master-owned; the lane consumes the result in gui-dataset-preview and gui-adapter-training. Text training today turns a file into one record (internal/trainingworkflow/data.go:37-38) and JSONTextPairProcessor pairs input and target; no processor renders conversations through the model's template and masks the non-assistant tokens. Reuse the inference chat templates and the tokenizer; refuse a conversation the template cannot render."
        },
        {
          "id": "adapter-gguf",
          "title": "Train an adapter that the existing GGUF LoRA loader serves",
          "status": "open",
          "verify": "test -f internal/adaptertrain/gguf_adapter_test.go \u0026\u0026 go test ./internal/adaptertrain -run '^TestAdapterTrainingEmitsServableGGUFLoRA$' -count=1",
          "rationale": "Master-owned; the lane consumes the result in gui-adapter-training. internal/model/lora.go loads GGUF LoRA and aLoRA adapters and /lora-adapters serves them, while adapter training exists only per layer (internal/adaptertrain) and lora-extract does not scale; nothing trains an adapter into that format. Emit the adapter under Muon with rank and scale derived from the declared parameter budget, freeze the quantized base through the existing dequantize-on-load path, and record one compatibility claim on a small model. This is the cheapest closed train-serve-evaluate loop.",
          "depends_on": [
            "train-serve-loop/chat-sft-data"
          ]
        },
        {
          "id": "checkpoint-export",
          "title": "Publish a dense training checkpoint as a registered, servable GGUF",
          "status": "open",
          "verify": "test -f internal/trainingworkflow/checkpoint_export_test.go \u0026\u0026 go test ./internal/trainingworkflow -run '^TestCheckpointExportsServableModel$' -count=1",
          "rationale": "Master-owned; the lane consumes the result in gui-run-export. cmd/train ends at publishCheckpoint with F32 safetensors on disk (internal/trainingworkflow/workflow.go:256-258); no path registers the checkpoint, derives a recipe or converts it, and the bracket's post slice is keyed to the base model (internal/trainingworkflow/bracket.go:76-95). Convert through the existing hfconvert and gguf-quantize owners, register the result with the run in its lineage, and capture the bracket's post slice from the exported artifact."
        },
        {
          "id": "preference-device-evidence",
          "title": "Record device evidence for the DPO and GRPO objectives on one small model",
          "status": "open",
          "verify": "test -f internal/densecausal/preference_device_test.go \u0026\u0026 go test ./internal/densecausal -run '^TestPreferenceObjectivesDeviceEvidence$' -count=1",
          "rationale": "Master-owned; the lane consumes the result in gui-preference-training. ObjectiveDPO and ObjectiveGRPO are implemented and tested on the host only (internal/trainingworkflow/workflow.go:307) with no real-artifact evidence. Run each on the device lane on one small model with a declared preference or rollout dataset and record the claims.",
          "depends_on": [
            "train-serve-loop/chat-sft-data"
          ]
        }
      ]
    },
    {
      "id": "long-context-collapse",
      "title": "Master reference: long-context correctness and throughput; reconcile with decode-attention-per-key-cost and device-memory-retention",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: long-context correctness and throughput; reconcile with decode-attention-per-key-cost and device-memory-retention",
          "status": "open",
          "verify": "go test ./internal/longform -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        }
      ]
    },
    {
      "id": "gemma-12b-accuracy",
      "title": "Master reference: evaluate Gemma instruction models with chat-shaped and matched-checkpoint evidence",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: evaluate Gemma instruction models with chat-shaped and matched-checkpoint evidence",
          "status": "open",
          "verify": "go test ./internal/longform -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        }
      ]
    },
    {
      "id": "benchmark-completion",
      "title": "Master reference: complete MMLU-Pro and published/local-oracle comparisons; MATH excluded and the 27B full pass operator-owned",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "mmlu-pro-pass",
          "title": "Record MMLU-Pro (12032 cases) for every servable text model under both protocols, smallest first; the 27B runs as its own operator-launched pass with -budget 3h",
          "status": "open",
          "verify": "go run ./cmd/benchmark-report -check",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. MMLU-Pro is the suite the model cards report, so it is the comparison that matters; it is also the largest, and the 27B at about 0.7 s per case needs over two hours. The 27B pass is launched by the owner, never unattended by the loop, and the smaller models complete first.",
          "depends_on": [
            "benchmark-completion/dna-pass"
          ]
        },
        {
          "id": "published-comparison",
          "title": "Compare every recorded suite against its declared published references and the local lm-eval oracle runs, extend the reference declarations to every model that has a model card, and regenerate docs/BENCHMARK.md with a per-model comparison section",
          "status": "open",
          "verify": "go run ./cmd/benchmark-report -check",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. A benchmark number without its reference is a bare fact; the references are store declarations with sources and protocols, and the report prints measured beside published with the protocol difference in view. MATH stays excluded and the report says so.",
          "depends_on": [
            "benchmark-completion/mmlu-pro-pass"
          ]
        }
      ]
    },
    {
      "id": "model-regression-gate",
      "title": "Master reference: acquire complete regression coverage and select the model guard for execution changes",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: acquire complete regression coverage and select the model guard for execution changes",
          "status": "open",
          "verify": "go test ./internal/automationcheck -run 'ModelRegress' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        }
      ]
    },
    {
      "id": "model-regression-baseline",
      "title": "Master reference: accept full-catalog quality, throughput and memory regression baselines",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: accept full-catalog quality, throughput and memory regression baselines",
          "status": "open",
          "verify": "go run ./cmd/model-regress -check -repo overgodb-store",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "full-catalog",
          "title": "Master reference: Expand and re-accept the full baseline before broad simplification",
          "status": "open",
          "verify": "go test ./internal/longform -count=1 \u0026\u0026 test -f cmd/longform/accepted_baseline_test.go \u0026\u0026 go test ./cmd/longform -run '^TestAcceptedBaselineEvidence$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "model-regression-baseline/do",
            "device-memory-retention/do",
            "decode-attention-per-key-cost/do",
            "gemma-12b-accuracy/do",
            "modality-verification/media-report"
          ]
        }
      ]
    },
    {
      "id": "modality-verification",
      "title": "Master reference: finish E4B's complete modality bundle, then verify the remaining declared model capabilities and reports",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "capability-census",
          "title": "Publish the capability census: every registered model with its activated tasks, evidence tier, staleness, and on-disk presence, as a store record and a generated table",
          "status": "open",
          "verify": "go run ./cmd/overgodb-query -repo overgodb-store -verifications",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. The catalog holds about 28 models across 9 tasks and the verification matrix names 5; the census is the denominator every following row reports against. A stale activation is listed as stale, never dropped.",
          "depends_on": [
            "benchmark-completion/published-comparison"
          ]
        },
        {
          "id": "declared-smoke-expectations",
          "title": "Replace the non-empty-output smoke oracle with a declared expectation per model: an instruct model gets a chat probe with a substring oracle, a base model a likelihood floor, a domain model its domain probe; a model without a declaration is reported unverified",
          "status": "open",
          "verify": "go run ./cmd/smoke-lane",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. The verified-matrix row overclaimed correctness on a liveness check; a fix surfaced by the drill is admitted under the correct-as-you-go directive. The lane's exit code is the verdict and its audit line names every model that ran without an expectation.",
          "depends_on": [
            "modality-verification/capability-census"
          ]
        },
        {
          "id": "text-and-vision",
          "title": "Verify the chat, embedding, rerank, and vision-language activations (Qwen, gemma-4, MiniCPM, Qwen3-VL, SenseNova) through smoke-lane, cmd/embedding, cmd/rerank, and vqaparity or sensenovaparity with their declared oracles",
          "status": "open",
          "verify": "go run ./cmd/smoke-lane",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. These are the largest activation group; each owner already exists and reports UNAVAILABLE when an oracle or fixture is missing. Bind the exact lane sequence with -setverify when the row starts.",
          "depends_on": [
            "modality-verification/declared-smoke-expectations"
          ]
        },
        {
          "id": "image-and-video",
          "title": "Verify the image and video generation activations (Krea-2-Turbo, SimpleDiffusion, Wan2.1-T2V, LiveEdit, Un-0 latent video) through the composite generation lane and latentvideo-run against the media verifier fixtures, exporting samples",
          "status": "open",
          "verify": "go run ./cmd/composite-generation-lane",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. The media-gen campaign left measured phases, peaks, and verifier runs; this row re-runs them at the current commit so the media report reflects code that exists. Bind the exact lane sequence with -setverify when the row starts.",
          "depends_on": [
            "modality-verification/text-and-vision"
          ]
        },
        {
          "id": "speech-ocr-tabular-forecast",
          "title": "Verify the speech synthesis (pocket-tts), OCR (Unlimited-OCR, SenseNova infographic), tabular (tabular-train-probe), series forecast, and embodied (Hy-Embodied-RxBrain) activations through their owners, each against a declared oracle or reported unverified",
          "status": "open",
          "verify": "go run ./cmd/eval-lane",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. These activations are the ones the memory notes as stale ('active recipe lacks verified evidence'); the row either verifies them at the current commit or records exactly which oracle is missing as a finding. Bind the exact owner sequence with -setverify when the row starts.",
          "depends_on": [
            "modality-verification/image-and-video"
          ]
        },
        {
          "id": "media-report",
          "title": "Regenerate docs/MEDIA_REPORT.md from the store's verification records and link the benchmark report from it, so one generated document summarizes every model's verified modalities and benchmark rows",
          "status": "open",
          "verify": "go run ./cmd/compatibility -check",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. The report is generated by cmd/compatibility -update-media from store activations and verification claims; hand-written summaries are refused. Its check is the verdict that the document matches the store.",
          "depends_on": [
            "modality-verification/speech-ocr-tabular-forecast"
          ]
        },
        {
          "id": "decode-guard-readmission",
          "title": "Master reference: Complete outstanding text performance evidence independently of modality validation",
          "status": "open",
          "verify": "OVERGO_DATA_ROOT=C:/Users/jeffm/overgo go test ./cmd/longform -run '^TestAcceptedGuardCatalog$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "native-audio-streaming-intake",
          "title": "Master reference: Verify landed native audio production consumers and retire staged APIs",
          "status": "open",
          "verify": "test -f internal/capabilityruntime/audio_stream_integration_test.go \u0026\u0026 go test ./internal/capabilityruntime -run '^TestNativeAudioStreamIntegration$' -count=1 \u0026\u0026 test -f internal/audioparity/production_intake_test.go \u0026\u0026 go test ./internal/audioparity -run '^TestNativeAudioProductionIntake$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "media-evidence-binding",
          "title": "Master reference: Bind imported media results to their exact scope",
          "status": "open",
          "verify": "test -f cmd/compatibility/verification_workflow_test.go \u0026\u0026 go test ./cmd/compatibility -run '^TestMediaEvidenceBinding$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "experiment-resume",
          "title": "Master reference: Resume one media acquisition through idempotent publication",
          "status": "open",
          "verify": "test -f internal/agentloop/verification_workflow_test.go \u0026\u0026 go test ./internal/agentloop -run '^TestMediaExperimentResume$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "modality-verification/media-evidence-binding"
          ]
        },
        {
          "id": "media-capacity-plan",
          "title": "Master reference: Choose media execution from measured request capacity",
          "status": "open",
          "verify": "test -f internal/sensenovarecipe/verification_workflow_test.go \u0026\u0026 go test ./internal/sensenovarecipe -run '^TestMediaRequestCapacity$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "modality-verification/media-evidence-binding",
            "modality-verification/experiment-resume"
          ]
        }
      ]
    },
    {
      "id": "failure-recovery",
      "title": "Master reference: author rollout plans and prove control-plane and gate recovery",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "rollout-plan-author",
          "title": "Add the rollout-plan authoring door (recipe rollout-plan: baseline, candidate, admission evidence, cohort evidence, rollback authority) that derives the cohort share and observation contract from the admission evidence, so rollout projection, quarantine re-entry, and live rollback have a committed plan to run against (finding c9c88020)",
          "status": "open",
          "verify": "go test ./internal/runrecord -run 'TestRolloutPlan' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. Three control-plane owners parse a committed rollout plan and no command authors one; the phase-1 drill stopped on this prerequisite. It is construction admitted under the correct-as-you-go directive because the drill cannot run without it.",
          "depends_on": [
            "modality-verification/media-report"
          ]
        },
        {
          "id": "control-plane-drills",
          "title": "Exercise circuit-breaker escalation, candidate quarantine and re-entry, rollback to the predecessor recipe, peer timeout and partial response, and workflow restart with persistent obligations",
          "status": "open",
          "verify": "go build ./cmd/loop ./cmd/agent-loop",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. The control plane's refusal and recovery paths are capability claims like any other; each has an evidence door under cmd/loop and cmd/agent-loop. Bind the drill sequence via -setverify when it starts.",
          "depends_on": [
            "failure-recovery/rollout-plan-author"
          ]
        },
        {
          "id": "gate-recovery-drill",
          "title": "Interrupt a gate run against a scratch clone and recover through -recover-preparation, -recover-interrupted, and the watchdog",
          "status": "open",
          "verify": "go build ./cmd/gate",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. The gate is the most state-bearing process in the repository -- plan mutation, commit, and store writes in one transaction -- and its recovery paths deserve the same drill as checkpoints. Bind the scripted drill via -setverify when it starts.",
          "depends_on": [
            "failure-recovery/control-plane-drills"
          ]
        }
      ]
    },
    {
      "id": "simplify-prefill-paths",
      "title": "Master reference: simplify prefill execution only where parity and regression evidence permit",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: simplify prefill execution only where parity and regression evidence permit",
          "status": "open",
          "verify": "OVERGO_CUDA_TEST=1 go test ./internal/cuda/executor -run 'TensorCore|Staged' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        }
      ]
    },
    {
      "id": "simplify-command-surface",
      "title": "Master reference: consolidate probe/lane commands while retaining every behavior",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: consolidate probe/lane commands while retaining every behavior",
          "status": "open",
          "verify": "go run ./cmd/gate -preflight",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. Master 31bf60f9149d incorporates the removal of cmd/preflight; its remaining diagnostics use the existing gate -preflight owner and its current-change scope. Explicit -paths cmd,internal is available for a Go-source inventory when other changes are not staged. This diagnostic command is not acceptance credit and does not close the master-owned consolidation obligation."
        }
      ]
    },
    {
      "id": "simplify-execution-core",
      "title": "Master reference: reduce CUDA/inference duplication and remove the executor size exception",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: reduce CUDA/inference duplication and remove the executor size exception",
          "status": "open",
          "verify": "go run ./cmd/clone-census -summary -max-excess 10012",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        }
      ]
    },
    {
      "id": "simplify-checkpoint-locations",
      "title": "Master reference: relocate checkpoints through recorded locations without losing model availability",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: relocate checkpoints through recorded locations without losing model availability",
          "status": "open",
          "verify": "go run ./cmd/overgodb-query -repo overgodb-store -servable",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        }
      ]
    },
    {
      "id": "simplify-capability-report",
      "title": "Master reference: report measured capability and mechanism changes before and after simplification",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: report measured capability and mechanism changes before and after simplification",
          "status": "open",
          "verify": "go run ./cmd/benchmark-report -check",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        }
      ]
    },
    {
      "id": "campaign-closeout",
      "title": "Master reference: publish the full campaign's accepted evidence and reports",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "closeout",
          "title": "Master reference: publish the full campaign's accepted evidence and reports",
          "status": "open",
          "verify": "go run ./cmd/plan -status",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound. A campaign ends with the plan empty and every claim bound to evidence at one commit; the close-out row is where the loop proves it has nothing left to do.",
          "depends_on": [
            "failure-recovery/gate-recovery-drill"
          ]
        }
      ]
    },
    {
      "id": "plan-publication-cost",
      "title": "Master reference: Reduce recurring build and gate cost, then simplify dispatch and publication",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "memoize-fixed-phases",
          "title": "Master reference: Reuse immutable analysis while rechecking current publication authority",
          "status": "open",
          "verify": "test -f internal/gate/publication_contract_test.go \u0026\u0026 go test ./internal/gate -run '^TestPlanOnlyPublicationReusesUnchangedPhases$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-gate-selection/source-evidence-binding"
          ]
        },
        {
          "id": "cli-dependency-boundary",
          "title": "Master reference: Remove inference dependencies from generic CLI helpers",
          "status": "open",
          "verify": "test -f internal/clioptions/dependency_contract_test.go \u0026\u0026 go test ./internal/clioptions -run '^TestCLIInferenceDependencyBoundary$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "internal-consumer-contract",
          "title": "Master reference: Recognize real internal operations without requiring token executables",
          "status": "open",
          "verify": "test -f internal/codeprofile/consumer_contract_test.go \u0026\u0026 go test ./internal/codeprofile -run '^TestDeclaredInternalOperationConsumer$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "dispatch-claim",
          "title": "Master reference: Atomically claim work before issuing an executable dispatch",
          "status": "open",
          "verify": "test -f internal/plan/workflow_contract_test.go \u0026\u0026 go test ./internal/plan -run '^TestAtomicDispatchClaim$' -count=1 \u0026\u0026 test -f cmd/plan/workflow_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestDispatchClaimProjection$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "stop-state",
          "title": "Master reference: Honor explicit operator stops until scoped resumption across every consumer",
          "status": "open",
          "verify": "test -f internal/plan/workflow_contract_test.go \u0026\u0026 go test ./internal/plan -run '^TestStopStateContract$' -count=1 \u0026\u0026 test -f cmd/plan/workflow_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestStopStatusProjection$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "step-edit",
          "title": "Master reference: Apply typed step edits with validation and conflict detection",
          "status": "open",
          "verify": "test -f internal/plan/workflow_contract_test.go \u0026\u0026 go test ./internal/plan -run '^TestAtomicStepEdit$' -count=1 \u0026\u0026 test -f cmd/plan/command_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestPlanEditCommand$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "retained-gate-report",
          "title": "Master reference: Query retained gate phases and outcomes by commit",
          "status": "open",
          "verify": "test -f cmd/plan/command_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestRetainedGatePhaseReport$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "one-command-publication",
          "title": "Master reference: Publish a plan edit through the gate with one command",
          "status": "open",
          "verify": "test -f cmd/plan/command_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestPlanPublishCommand$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "command-help",
          "title": "Master reference: Expose actual flags and correct conflicting usage",
          "status": "open",
          "verify": "test -f cmd/overgodb-query/help_test.go \u0026\u0026 go test ./cmd/overgodb-query -run '^TestQueryHelpContract$' -count=1 \u0026\u0026 test -f cmd/plan/command_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestPlanHelpContract$' -count=1 \u0026\u0026 test -f cmd/finding/help_test.go \u0026\u0026 go test ./cmd/finding -run '^TestFindingHelpContract$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "dispatch-readiness",
          "title": "Master reference: Show cheap admission diagnostics with the task before costly preparation",
          "status": "open",
          "verify": "test -f internal/gate/publication_contract_test.go \u0026\u0026 go test ./internal/gate -run '^TestCheapAdmissionReadiness$' -count=1 \u0026\u0026 test -f cmd/plan/workflow_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestPromptAdmissionReadiness$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "frontier-diagnostics",
          "title": "Master reference: Keep valid frontier rows visible when advisory leases are stale",
          "status": "open",
          "verify": "test -f cmd/plan/workflow_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestFrontierLeaseDiagnostics$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "dispatch-contract",
          "title": "Master reference: Show a concise actionable contract and retain history by reference",
          "status": "open",
          "verify": "test -f internal/plan/workflow_contract_test.go \u0026\u0026 go test ./internal/plan -run '^TestDispatchContractBinding$' -count=1 \u0026\u0026 test -f cmd/plan/workflow_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestPromptContractProjection$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "structural-plan-policy",
          "title": "Master reference: Replace doctrine substring gates with semantic contract checks",
          "status": "open",
          "verify": "test -f internal/plan/workflow_contract_test.go \u0026\u0026 go test ./internal/plan -run '^TestStructuralCampaignPolicy$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "command-discovery",
          "title": "Master reference: Project command purpose, audience and flags into the API manifest",
          "status": "open",
          "verify": "test -f internal/apimanifest/command_discovery_test.go \u0026\u0026 go test ./internal/apimanifest -run '^TestCommandDiscoveryProjection$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "plan-publication-cost/command-help"
          ]
        },
        {
          "id": "verification-failure-summary",
          "title": "Master reference: Show failing tests and assertion tails while retaining raw evidence",
          "status": "open",
          "verify": "test -f cmd/plan/command_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestVerifyFailureSummary$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "scratch-probe-path",
          "title": "Master reference: Expose and verify the existing scratch-module workflow",
          "status": "open",
          "verify": "test -f cmd/plan/workflow_contract_test.go \u0026\u0026 go test ./cmd/plan -run '^TestScratchModuleWorkflow$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "merge-portability",
          "title": "Master reference: Make prepared merges portable across registered worktrees",
          "status": "open",
          "verify": "test -f cmd/plan/verification_workflow_test.go \u0026\u0026 go test ./cmd/plan -run '^TestPreparedMergePortability$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        }
      ]
    },
    {
      "id": "validation-gate-selection",
      "title": "Master reference: Determine unnecessary testing and deliver one measured gate reduction",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "source-evidence-binding",
          "title": "Master reference: Preserve checkpoint result identity when authorizing reuse",
          "status": "open",
          "verify": "test -f internal/automationcheck/reuse_authority_test.go \u0026\u0026 go test ./internal/automationcheck -run '^(TestCheckpointReuseAuthority|TestManifestExecutionBinding|TestPlanEvidenceLineage|TestGateReusesUnchangedCheckEvidence)$' -count=1 \u0026\u0026 test -f internal/gate/checkpoint_reuse_authority_test.go \u0026\u0026 go test ./internal/gate -run '^(TestCheckpointReuseCommitAuthority|TestCommitAdmissionUsesManifestPlan)$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "selection-cause-histogram",
          "title": "Master reference: Partition every selected package by its recorded selection cause",
          "status": "open",
          "verify": "test -f internal/gate/selection_cause_histogram_test.go \u0026\u0026 go test ./internal/gate -run '^TestSelectionCauseHistogram$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "suite-cost-ranking",
          "title": "Master reference: Rank individual test cost inside the two costliest packages",
          "status": "open",
          "verify": "test -f internal/gate/suite_cost_ranking_test.go \u0026\u0026 go test ./internal/gate -run '^TestSuiteCostRanking$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "reduction-contract",
          "title": "Master reference: Freeze the real cost target, the chosen mechanism, the invalidators and the mutation matrix",
          "status": "open",
          "verify": "test -f internal/gate/testing_reduction_contract_test.go \u0026\u0026 go test ./internal/gate -run '^TestTestingReductionContract$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-gate-selection/selection-cause-histogram",
            "validation-gate-selection/suite-cost-ranking"
          ]
        },
        {
          "id": "repair-iteration",
          "title": "Master reference: Make focused checkpoint publication stop forcing the whole integration suite",
          "status": "open",
          "verify": "test -f internal/gate/repair_iteration_test.go \u0026\u0026 go test ./internal/gate -run '^TestGateRepairBatchAcceptance$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-gate-selection/source-evidence-binding",
            "plan-publication-cost/memoize-fixed-phases"
          ]
        },
        {
          "id": "call-reach",
          "title": "Master reference: Implement the contract's chosen first reduction on the frozen target",
          "status": "open",
          "verify": "test -f internal/gate/first_reduction_test.go \u0026\u0026 go test ./internal/gate -run '^TestFirstReductionAcceptance$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-gate-selection/reduction-contract",
            "validation-gate-selection/repair-iteration"
          ]
        },
        {
          "id": "measured-adoption",
          "title": "Master reference: Review retained integration evidence and decide first-reduction adoption",
          "status": "open",
          "verify": "go test ./internal/gate -run '^(TestChangeSelectiveVerificationShadow|TestBatchedGateCostEvidence|TestIndependentPackageEvidenceAcceptance)$' -count=1 \u0026\u0026 test -f internal/gate/selection_payback_acceptance_test.go \u0026\u0026 go test ./internal/gate -run '^TestSelectionPaybackAcceptance$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-gate-selection/call-reach"
          ]
        },
        {
          "id": "common-owner-pilot",
          "title": "Master reference: Remove one duplicated automation API or executable with its callers",
          "status": "open",
          "verify": "go test ./internal/gate -run '^(TestChangeSelectionRequiredMatrix|TestIndependentPackageEvidenceAcceptance|TestGateStagesMechanicalRepairs|TestCandidateCheckoutOwnsIndexRefresh)$' -count=1 \u0026\u0026 test -f internal/gate/common_owner_consolidation_test.go \u0026\u0026 go test ./internal/gate -run '^TestCommonOwnerConsolidationAcceptance$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "recipe-node-reuse",
          "title": "Master reference: Reuse unchanged verification nodes across composed recipes",
          "status": "open",
          "verify": "go test ./internal/automationcheck -run '^(TestManifestExecutionBinding|TestPlanEvidenceLineage|TestGateReusesUnchangedCheckEvidence)$' -count=1 \u0026\u0026 test -f internal/gate/verification_recipe_reuse_test.go \u0026\u0026 go test ./internal/gate -run '^TestVerificationRecipeReuseAcceptance$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-gate-selection/source-evidence-binding"
          ]
        },
        {
          "id": "runtime-value-flow",
          "title": "Master reference: Resolve runtime paths and commands across one real call boundary",
          "status": "open",
          "verify": "go test ./internal/gate -run '^(TestRuntimeInputsUnsafeClassifications|TestRuntimeInputsPropagateThroughCallers|TestRuntimeInputsIndirectAndConditional|TestRuntimeReachBindingsAcceptance|TestChangeSelectionRequiredMatrix)$' -count=1 \u0026\u0026 test -f internal/gate/runtime_value_flow_test.go \u0026\u0026 go test ./internal/gate -run '^TestRuntimeValueFlowAcceptance$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-gate-selection/call-reach",
            "validation-gate-selection/measured-adoption"
          ]
        },
        {
          "id": "verification-sequence",
          "title": "Master reference: Construct verification DAGs from resolved impact obligations",
          "status": "open",
          "verify": "test -f internal/gate/verification_workflow_test.go \u0026\u0026 go test ./internal/gate -run '^TestAnalysisComposedVerification$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-gate-selection/measured-adoption",
            "validation-gate-selection/recipe-node-reuse"
          ]
        },
        {
          "id": "static-analysis-sharing",
          "title": "Master reference: Reuse shared static analysis with rule-specific invalidation",
          "status": "open",
          "verify": "test -f internal/repoanalysis/verification_workflow_test.go \u0026\u0026 go test ./internal/repoanalysis -run '^TestIncrementalStaticAnalysisContracts$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-gate-selection/measured-adoption"
          ]
        }
      ]
    },
    {
      "id": "evidence-storage-efficiency",
      "title": "Master reference: Reuse immutable evidence without copying every small file",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "small-record-layout",
          "title": "Master reference: Measure and decide whether to pack small immutable records",
          "status": "open",
          "verify": "go test ./internal/overgodb -run '^(TestBlobStorePublicationContract|TestObservationChunkPublication|TestSegmentedJournalChainAndRecovery|TestStreamingSnapshotRoundTrip)$' -count=1 \u0026\u0026 test -f internal/overgodb/small_record_layout_test.go \u0026\u0026 go test ./internal/overgodb -run '^TestSmallRecordLayoutDecision$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        }
      ]
    },
    {
      "id": "validation-batch-control",
      "title": "Master reference: Stabilize evidence commits; defer broader control policy until model evidence",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "evidence-commit-recovery",
          "title": "Master reference: Certify the complete evidence workflow across producer and supervisor crashes",
          "status": "open",
          "verify": "go test ./internal/gate -run '^(TestBatchEvidencePublicationFailure|TestTerminalEvidenceRestart|TestRecoverInterruptedStateCrashRetryBoundaries|TestAbandonedGateAdmission)$' -count=1 \u0026\u0026 test -f internal/gate/evidence_commit_recovery_test.go \u0026\u0026 go test ./internal/gate -run '^TestEvidenceCommitRecoveryAcceptance$' -count=1 \u0026\u0026 go test ./internal/overgodb -run '^(TestOpenContextWriterAdmission|TestTransactionWriter|TestSecondWriterFailsClosed)$' -count=1 \u0026\u0026 test -f internal/gate/evidence_commit_workflow_test.go \u0026\u0026 go test ./internal/gate -run '^TestEvidenceCommitWorkflow$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound."
        },
        {
          "id": "batch-promotion",
          "title": "Master reference: Promote batching only after recovery, safe selection and measured payback",
          "status": "open",
          "verify": "go test ./internal/gate -run '^(TestPreflightReportsValidateFindings|TestPreflightSeparatesSkippedChecks|TestPreflightNeverRepairsStore|TestPreflightRejectsCombinedInspection|TestGateStartIndexAdoptsStatRefreshUnderSameTree|TestGateStartIndexRefusesAndPreservesLaterUnplannedStaging)$' -count=1 \u0026\u0026 go test ./internal/gate -run '^(TestCheckpointPersistenceSurvivesKill|TestCheckpointMemoCoversEveryCommand|TestChangeSelectiveVerificationShadow|TestBatchedGateCostEvidence)$' -count=1 \u0026\u0026 go test ./internal/gate -run '^(TestIndependentPackageEvidenceAcceptance|TestGovernedBatchDeclarationAcceptance|TestProjectedMergePreflightBeforeVerification|TestGateReusesUnchangedCheckEvidence|TestPackageSelectiveReuse)$' -count=1 \u0026\u0026 test -f internal/gate/batch_promotion_test.go \u0026\u0026 go test ./internal/gate -run '^TestBatchPromotionControl$' -count=1 \u0026\u0026 go test ./internal/gate -run '^TestReusedCheckpointCompletionAuthorityAcceptance$' -count=1 \u0026\u0026 go test ./internal/repoanalysis -run '^(TestDocsHoldNoUnplannedBinaries|TestDocsInventoryPolicy)$' -count=1 \u0026\u0026 go test ./internal/gate -run '^TestPreflightRejectsUnplannedDocsBeforeStore$' -count=1 \u0026\u0026 go test ./internal/gate -run '^TestChangeSelectionUnownedRuntimeInputs$' -count=1 \u0026\u0026 go test ./internal/plan -run '^TestBatchFlush' -count=1 \u0026\u0026 go test ./internal/gate -run '^TestCandidateGoPhasesExcludeIgnoredSource$' -count=1 \u0026\u0026 go test ./internal/gate -run '^TestBatchEvidence' -count=1 \u0026\u0026 go test ./internal/gate -run '^(TestChangedPackageFailurePreventsBroadSweep|TestModernGoPreflightBindsCandidateCensus|TestStaticFailurePreventsAcceptance|TestGateRunsStaticChecksBeforeAcceptance|TestCheckpointEvidenceReuseAcrossRuns|TestPackageSelectiveReuse|TestTerminalEvidenceRestart)$' -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "validation-batch-control/evidence-commit-recovery",
            "modality-verification/media-report",
            "validation-gate-selection/measured-adoption"
          ]
        }
      ]
    },
    {
      "id": "device-memory-retention",
      "title": "Master reference: Bound retained device memory across changing context capacities",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: Bound retained device memory across changing context capacities",
          "status": "open",
          "verify": "go test ./internal/longform -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "model-regression-gate/do"
          ]
        }
      ]
    },
    {
      "id": "decode-attention-per-key-cost",
      "title": "Master reference: Recover small-head decode throughput without losing long-context correctness",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: Recover small-head decode throughput without losing long-context correctness",
          "status": "open",
          "verify": "go test ./internal/cuda/executor -run DecodeAttention -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "model-regression-gate/do"
          ]
        }
      ]
    },
    {
      "id": "final-model-validation",
      "title": "Master reference: Prove the final text execution surface and long-lived serving behavior",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Master reference: Prove the final text execution surface and long-lived serving behavior",
          "status": "open",
          "verify": "go test ./internal/longform -count=1",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "simplify-execution-core/do",
            "simplify-command-surface/do",
            "simplify-checkpoint-locations/do"
          ]
        }
      ]
    },
    {
      "id": "benchmark-27b",
      "title": "Master reference: Operator-launched full 27B benchmark",
      "owner": "master",
      "status": "open",
      "steps": [
        {
          "id": "mmlu-pro-pass",
          "title": "Master reference: Complete the operator-launched 27B MMLU-Pro pass",
          "status": "open",
          "verify": "test -f cmd/benchmark-report/accepted_coverage_test.go \u0026\u0026 OVERGO_DATA_ROOT=C:/Users/jeffm/overgo go test ./cmd/benchmark-report -run '^TestAcceptedOperator27BMMLUPro$' -count=1 \u0026\u0026 go run ./cmd/benchmark-report -check",
          "rationale": "Master-owned; reconcile with current master before execution. Retained verifier is a lower bound.",
          "depends_on": [
            "benchmark-completion/mmlu-pro-pass"
          ]
        }
      ]
    }
  ]
}
