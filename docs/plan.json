{
  "campaign": "Constrained RSI bootstrap: verified substrate, experiment plane, and automatic composition",
  "doctrine": "Follow skill.md. This file is the SINGLE plan authority: campaign design and execution dispatch live here and nowhere else. Every open step has a failable verifier; queue order is binding; plan complete means campaign complete. Generic authority beside retained specialized runtime is failure, not progress. Every migration must move all production callers and delete displaced orchestration, wrappers, fallbacks, compatibility paths, duplicate probes, and parallel authorities in the same gated wave. New shared code must replace existing ownership immediately. Verify numerical behavior, Live Edit behavior where applicable, code reduction, allocations, transfers, peak memory, and processing cost before completion. After every gated step, merge current master into this worktree before starting the next step.",
  "items": [
    {
      "id": "qwen35-hybrid-backward",
      "title": "Qwen3.5 hybrid training: backward for the hybrid recurrent decoder (dense/mixture machinery covers all but the recurrence; port-first from any GDN/recurrent VJP in either tree, invent the remainder); bounded observed smokes land cells for Qwen3.5-4B and Qwen3.5-9B-Q8, refusal findings 2304847a and the original hybridtrain finding close",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Qwen3.5 hybrid training: backward for the hybrid recurrent decoder (dense/mixture machinery covers all but the recurrence; port-first from any GDN/recurrent VJP in either tree, invent the remainder); bounded observed smokes land cells for Qwen3.5-4B and Qwen3.5-9B-Q8, refusal findings 2304847a and the original hybridtrain finding close",
          "status": "open",
          "verify": "go run ./cmd/repodb-query -repo repodb-store -verifications | grep 'Qwen3.5-4B' | grep -q training="
        }
      ]
    },
    {
      "id": "krea-flow-training",
      "title": "Krea-2-Turbo training: latent-flow finetune graph reusing the diffusionimage flow-matching objective against the Krea denoiser blocks; bounded observed smoke, cell recorded, refusal finding 5d2ab94c closes",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Krea-2-Turbo training: latent-flow finetune graph reusing the diffusionimage flow-matching objective against the Krea denoiser blocks; bounded observed smoke, cell recorded, refusal finding 5d2ab94c closes",
          "status": "open",
          "verify": "go run ./cmd/repodb-query -repo repodb-store -verifications | grep Krea | grep -q training="
        }
      ]
    },
    {
      "id": "wan-flow-training",
      "title": "Wan2.1-T2V training: text-to-video flow training graph (denoise objective over the wan transformer with frozen T5/VAE, SimpleDiffusion template); bounded observed smoke, cell recorded, refusal finding 421b6981 closes",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Wan2.1-T2V training: text-to-video flow training graph (denoise objective over the wan transformer with frozen T5/VAE, SimpleDiffusion template); bounded observed smoke, cell recorded, refusal finding 421b6981 closes",
          "status": "open",
          "verify": "go run ./cmd/repodb-query -repo repodb-store -verifications | grep Wan2.1 | grep -q training="
        }
      ]
    },
    {
      "id": "liveedit-flow-training",
      "title": "LiveEdit training: video-edit diffusion training graph over the SimpleDiffusion OT-flow template (conditioned denoise objective from the serving bridge tensors); bounded observed smoke, cell recorded, refusal finding f28aa306 closes",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "LiveEdit training: video-edit diffusion training graph over the SimpleDiffusion OT-flow template (conditioned denoise objective from the serving bridge tensors); bounded observed smoke, cell recorded, refusal finding f28aa306 closes",
          "status": "open",
          "verify": "go run ./cmd/repodb-query -repo repodb-store -verifications | grep LiveEdit | grep -q training="
        }
      ]
    },
    {
      "id": "bonsai-master-weight-lane",
      "title": "Bonsai-27B ternary training: implement a dequantized master-weight lane (f32 masters, ternary re-quantization for serving parity) so the GGUF artifact trains through Muon with derived hyperparameters; bounded observed smoke, cell recorded, refusal finding 21ee8a65 closes",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Bonsai-27B ternary training: implement a dequantized master-weight lane (f32 masters, ternary re-quantization for serving parity) so the GGUF artifact trains through Muon with derived hyperparameters; bounded observed smoke, cell recorded, refusal finding 21ee8a65 closes",
          "status": "open",
          "verify": "go run ./cmd/repodb-query -repo repodb-store -verifications | grep Bonsai | grep -q training="
        }
      ]
    },
    {
      "id": "gemma12b-training-port",
      "title": "gemma-4-12B training: port the reference's LIVE gemma_stream_cuda unified lane semantics (tiled-head CE, frozen tied embedding, MuonBridge geometry) onto overgo's stack for the fp8 serving artifact -- the -freeze-lexical big-vocab run; bounded observed smoke, cell recorded, the tightening lane's gemma12b refusal closes",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "gemma-4-12B training: port the reference's LIVE gemma_stream_cuda unified lane semantics (tiled-head CE, frozen tied embedding, MuonBridge geometry) onto overgo's stack for the fp8 serving artifact -- the -freeze-lexical big-vocab run; bounded observed smoke, cell recorded, the tightening lane's gemma12b refusal closes",
          "status": "open",
          "verify": "go run ./cmd/repodb-query -repo repodb-store -verifications | grep 'gemma-4-12B' | grep -q training=",
          "rationale": "DEMOTED TO CAPSTONE 2026-08-19: the tightening refusal doc shows closure needs the device-stream full-parameter port with an explicit fp8-to-master-weight precision/persistence policy (12B does not fit host f32; upstream built gemma_stream_cuda for exactly this). The adapter route is E4B-only (PLE tensors are native there, absent on 12B). Port scope: reference extmodel/training_cuda_windows.go GPUAlternatingWindowModel + RunMixedWindowDecoderTrainingStreamCUDA, tiled-head CE, frozen tied embedding, MuonBridge geometry, -freeze-lexical."
        }
      ]
    }
  ]
}
