/* Images: text-to-image as the composer's image-gen mode over its own thread. */
window.overgo.generationTab("image-gen", "image-gen", { placeholder: "describe the image to generate", sendLabel: "generate" });
