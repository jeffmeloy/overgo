{
  "campaign": "Literal authority and duplicate runtime tightening",
  "doctrine": "Counts route inspection; semantics decide consolidation. Recipes, artifact metadata, typed plans, and neutral operators own runtime policy. Constants name stable implementation contracts only. Test fixtures may carry local data but must not copy production policy or reconstruct production logic. Each wave migrates every targeted caller and deletes displaced loops, wrappers, and duplicate harnesses immediately. Preserve numerical behavior and wire contracts. Merge master after every gated commit.",
  "census_evidence": "evidence:sha256:84da019c5f63b742cf71e4f0a0b057e0c4c3328963b78de3cf4b4da83fbd7057",
  "items": [
    {
      "id": "modelswap-core",
      "title": "Model-swap supervisor: exclusive child-server lifecycle with readiness, in-flight draining, and idle timeout -- llama-swap as reference, overgo authorities as config",
      "owner": "gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Model-swap supervisor: exclusive child-server lifecycle with readiness, in-flight draining, and idle timeout -- llama-swap as reference, overgo authorities as config",
          "status": "open",
          "verify": "go test ./internal/modelswap -run TestModelSwapSupervisor -count=1"
        }
      ]
    },
    {
      "id": "modelswap-proxy",
      "title": "Swap proxy over the servable catalog: model-routed requests launch the right server, cmd/swap serves it",
      "owner": "gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Swap proxy over the servable catalog: model-routed requests launch the right server, cmd/swap serves it",
          "status": "open",
          "verify": "go test ./internal/modelswap -run TestModelSwapProxy -count=1"
        }
      ]
    },
    {
      "id": "gui-live-model-switch",
      "title": "The workbench switches models live through the swap proxy: the picker swaps instead of handing out relaunch commands",
      "owner": "gui",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "The workbench switches models live through the swap proxy: the picker swaps instead of handing out relaunch commands",
          "status": "open",
          "verify": "go test ./internal/modelswap ./internal/server -run 'TestModelSwap|TestServedModelHeader' -count=1"
        }
      ]
    }
  ]
}
