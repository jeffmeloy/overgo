overgo training verification refusal
models: TabFM-1.0-classification (model:sha256:928cb350becdc77cdb7a9e8c36deda88917bfd14a3091894a2dc516db58a2085),
TabFM-1.0-regression (model:sha256:bd5a615b0322a8f04a895038de6df6fbd71430eca750e1d792f31048654674a9)
finding: evidence:sha256:9e6ceaabc415e9503e7f82f387c28a7cffc12ba54135206844ddf619a01cf494
refusal: no trainable graph exists on either side of the port --
internal/tabularicl is forward/load/runtime only, and the reference
tabular set transformer (adaptive_new go/extmodel) ships no backward.
Architecturally TabFM is a TabPFN-family in-context learner: it predicts
by conditioning on in-context training rows at inference, so
per-deployment SGD contradicts its serving contract, and meta-training
the prior is a from-scratch pretraining campaign, not a bounded smoke.
closure routes (either closes the finding):
 1. port a set-transformer backward (ISAB/MAB attention VJP) over the
    seriesforecast trainer pattern -- packed tensors from artifact
    shapes, optimizer.NewStepper, probe command as production consumer
    -- and record a real training cell;
 2. record an explicit training-unsupported declaration for ICL-frozen
    models in the ledger and close the finding against it.
guard: the finding's check fails if a TabFM training cell ever appears
without one of the routes, so a silent cell is impossible.
no silent skip: this document and the parked finding are the sweep's
typed outcome for both TabFM rows.
