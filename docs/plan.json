{
  "campaign": "Constrained RSI bootstrap: verified substrate, experiment plane, and automatic composition",
  "doctrine": "Follow skill.md. This file is the SINGLE plan authority: campaign design and execution dispatch live here and nowhere else. Every open step has a failable verifier; steps declare depends_on and the queue order must respect them; plan complete means campaign complete by construction.",
  "items": [
    {
      "id": "training-verification-sweep",
      "title": "Training verification sweep, smallest models first: mirror the inference-validation path for training -- for each ledger model lacking a training cell (TimesFM-200M, TabFM classification/regression, SimpleDiffusion, Pocket-TTS, Un-0, LiveEdit, Unlimited-OCR, Wan2.1-1.3B, Qwen3.5-4B, gemma-4-E4B, SenseNova-U1-8B, Qwen3.5-9B-Q8, gemma-4-12B-fp8, Krea-2-Turbo, Bonsai-27B-Q1, plus RxBrain once its port lands) run a bounded observed training smoke (cmd/train with -max-wall, TrainObserver progress, -freeze-lexical for big-vocab device runs) on a fixed sft/fixture dataset, record a training CapabilityClaim with steps/tokens spans and measured wall/ctx/peak via compatibility -record-verification; where the architecture has no training path yet, either port the minimal trainable graph or record a typed refusal finding with a failing check -- no silent skips; work smallest to largest, one gated sub-row per model or per small batch",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Training verification sweep, smallest models first: mirror the inference-validation path for training -- for each ledger model lacking a training cell (TimesFM-200M, TabFM classification/regression, SimpleDiffusion, Pocket-TTS, Un-0, LiveEdit, Unlimited-OCR, Wan2.1-1.3B, Qwen3.5-4B, gemma-4-E4B, SenseNova-U1-8B, Qwen3.5-9B-Q8, gemma-4-12B-fp8, Krea-2-Turbo, Bonsai-27B-Q1, plus RxBrain once its port lands) run a bounded observed training smoke (cmd/train with -max-wall, TrainObserver progress, -freeze-lexical for big-vocab device runs) on a fixed sft/fixture dataset, record a training CapabilityClaim with steps/tokens spans and measured wall/ctx/peak via compatibility -record-verification; where the architecture has no training path yet, either port the minimal trainable graph or record a typed refusal finding with a failing check -- no silent skips; work smallest to largest, one gated sub-row per model or per small batch",
          "status": "open",
          "verify": "test $(go run ./cmd/repodb-query -repo repodb-store -verifications | grep -c training=) -ge 21",
          "rationale": "OWNER HOLD 2026-08-19: wait for the overgo_tightening worktree to finish and merge before executing this sweep. The tightening lane consolidates training trajectory evidence, resident training entry, and token rendering authority under a delete-the-displaced doctrine, so sweep work started now would ride machinery scheduled for displacement. After the merge, run the sweep against the merged training entry points; keep outputs claim-shaped (ledger records + committed evidence docs) so they survive machinery migrations. Verifier set non-vacuous 2026-08-19: 5 training cells exist, target is \u003e= 21 (16 missing models per the title list, RxBrain claiming through the rung14 lane)."
        }
      ]
    },
    {
      "id": "external-benchmark-evidence",
      "title": "External benchmark evidence: run the lm-evaluation-harness batch evaluator (C:/Users/jeffm/eval) on applicable text models as the independent judge, commit results with tool-and-version provenance, and record benchmark claims at capability-measured tier; applicability judged per model from the ledger (DNA and non-text models keep their own evaluation protocols)",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "External benchmark evidence: run the lm-evaluation-harness batch evaluator (C:/Users/jeffm/eval) on applicable text models as the independent judge, commit results with tool-and-version provenance, and record benchmark claims at capability-measured tier; applicability judged per model from the ledger (DNA and non-text models keep their own evaluation protocols)",
          "status": "open",
          "verify": "go run ./cmd/repodb-query -repo repodb-store -verifications | grep -qi benchmark",
          "rationale": "Deferred by owner direction 2026-08-18: native benchmark evaluation (MMLU-Pro, BBH/MuSR, TruthfulQA, instruction/structured evaluation, cmd/evaluate) is being built into overgo by the parallel tightening lane; this row completes against that machinery after merge rather than shelling out to the external Python harness."
        }
      ]
    },
    {
      "id": "hygiene-sweep",
      "title": "Hygiene sweep: repair gemma-4-12B stale activation, close discovery wholesale-failure robustness finding, GUI tour with plan recommendations",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Hygiene sweep: repair gemma-4-12B stale activation, close discovery wholesale-failure robustness finding, GUI tour with plan recommendations",
          "status": "open",
          "verify": "go run ./cmd/repodb-query -repo repodb-store -servable | grep -q gemma-4-12B",
          "rationale": "STATUS 2026-08-18: discovery wholesale-failure finding CLOSED (stale-tolerant Servable landed with TestServableReportsStaleActivationsWithoutFailing in the phase-d row). Gemma repair diagnosis: the recipe CLI resolves the model name to the models DIRECTORY while the activation points at checkpoints/overgo-hfconvert/gemma-4-12B-it-fp8-native.gguf; the active recipe lacks verified run evidence. Repair path: run the recipe verification flow against the native GGUF (recipe verify -input \u003csuite JSON\u003e) to produce gate evidence, then re-activate with -gate; two sibling stale activations (f5e632cd, fb09299d) repair the same way. GUI tour remains the third leg. ADDED 2026-08-18: remove the duplicate RxBrain port (git rm -r internal/rxbrain cmd/rxbrain-probe) -- superseded by the rung14 lane (patchtower/routedlm/vqaparity, 44/44 ladder, exact-golden claim); the delete was permission-blocked in the working session and needs owner-approved removal through the gate."
        }
      ]
    },
    {
      "id": "sealed-external-authority",
      "title": "Place goldens, evaluator definitions, promotion policy and champion aliases beyond candidate-worker reach through a distinct OS/service identity or remote protected authority; require external provenance naming the producing tool and version for any golden change",
      "status": "open",
      "steps": [
        {
          "id": "do",
          "title": "Place goldens, evaluator definitions, promotion policy and champion aliases beyond candidate-worker reach through a distinct OS/service identity or remote protected authority; require external provenance naming the producing tool and version for any golden change",
          "status": "open",
          "verify": "OVERGO_SEALED_AUTHORITY_TEST=1 go test ./internal/testevidence ./internal/protection -run '^TestCandidatePrincipalCannotWriteSealedAuthority$' -count=1 -v",
          "rationale": "Owner directive 2026-08-17: host sealing moved to the campaign end -- autonomous workers do not run yet, so the second OS principal and the sealing machinery arrive together when Phase C makes them load-bearing. Prior fixture-level machinery was backed out rather than committed unconsumed."
        }
      ]
    }
  ]
}
