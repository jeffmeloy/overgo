// Package mediacapability is the catalog of registrable, executable
// capabilities beyond chat inference: image, video and speech generation,
// forecasting, tabular and sequence-to-sequence models, thought banks and
// projections. Each capability resolves a model path into its inventory
// and recipe definition and executes a compiled program against the
// model bytes; the recipe command and the server's generation workspace
// share the catalog, so what the command verifies is what the page runs.
package mediacapability
